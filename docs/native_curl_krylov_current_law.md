# Native curl--Krylov current law: the operator below POWER, strain and spectral curvature

Status: **DRAFT STRUCTURAL THEOREM CANDIDATE — local identities proved algebraically below; no global-regularity claim.**

This note continues `native_curl_centered_defect_law.md` by asking a different question from the old owner architecture: *what is the smallest physical mechanism from which the apparent HH/strain/curvature phenomena are all read?*  The answer at the present level is an operator pair, not a case split.

Let

\[
C=\operatorname{curl},\qquad C^*=C,\qquad C^2=-\Delta
\]

on divergence-free fields, and let

\[
J_u v:=\mathbb P(u\times v).
\]

Then `J_u^*=-J_u` and `J_u u=0`.  With

\[
E=\|u\|_2^2,\quad H=\langle u,Cu\rangle,\quad
\lambda=H/E,\quad r=(C-\lambda)u,
\]

Navier--Stokes is exactly

\[
\boxed{u_t=J_u r-\nu C^2u.}
\]

The first term is a skew physical motion generated by the curl defect; the second is the positive square of the same intrinsic operator.  Pressure is already quotiented by the Leray projection.  No material, packet, shell, owner or analyst clock is present.

## 1. The critical Beltrami determinant is the Euler pair-shear capacity

Put

\[
Z=\|Cu\|_2^2,\qquad
B=\|r\|_2^2=Z-H^2/E,
\]

and define the scale-critical Gram determinant

\[
\boxed{\Delta:=EB=EZ-H^2.}
\]

If the state is decomposed into orthogonal curl eigenspaces

\[
u=\sum_a u_a,\qquad Cu_a=a u_a,\qquad E_a=\|u_a\|_2^2,
\]

then elementary variance algebra gives

\[
\boxed{\Delta=\sum_{a<b}(a-b)^2E_aE_b.}
\]

The same gap is exactly what drives the polarized Euler interaction.  For two curl eigenstates `Cu=a u`, `Cv=b v`,

\[
\mathcal N(u,v)
:=\frac12\big(J_uCv+J_vCu\big)
=\boxed{\frac{b-a}{2}\,\mathbb P(u\times v)}.
\]

Thus equal curl eigenvalues do not interact nonlinearly at all.  In an unordered spectral decomposition,

\[
J_uCu=\sum_{a<b}(b-a)\,\mathbb P(u_a\times u_b).
\]

So `Delta` is not an analyst-selected norm: it is the exact squared pairwise signed-curl shear capacity of the same coefficients used by the Euler quadratic law.  Actual nonlinear motion differs from this capacity only through the physical cross-product geometry and coherence of those pair currents.

The raw Lamb vector also obeys the critical capacity bound

\[
\|u\times\omega\|_{L^1}
=\|u\times r\|_{L^1}
\le \sqrt{EB}=\sqrt\Delta.
\]

This is only an `L^1` statement.  Promoting it to an `L^2` current bound would erase the genuine spatial-concentration problem and is forbidden below.

## 2. The second curl residual is the tangent gradient of defect

Assume `B>0` and set

\[
J:=\langle r,Cr\rangle,
\]

\[
\boxed{
h:=Cr-\frac BEu-\frac JB r.
}
\]

Self-adjointness of curl gives

\[
\langle u,Cr\rangle=B,
\]

so exactly

\[
\boxed{h\perp u,\qquad h\perp r,}
\]

and

\[
\boxed{
\|Cr\|_2^2
=\frac{B^2}{E}+\frac{J^2}{B}+\|h\|_2^2.
}
\]

The common energy--helicity tangent space is

\[
T_u\mathcal M_{E,H}
=\{v:\langle u,v\rangle=\langle Cu,v\rangle=0\}
=\{u,r\}^{\perp}.
\]

Since

\[
\frac12\nabla B=(C-\lambda)^2u,
\]

the tangent gradient is precisely

\[
\boxed{\nabla_T(B/2)=h=P_T C^2u.}
\]

Euler preserves `E,H`, so its velocity

\[
F_E=J_ur=\mathbb P(u\times r)
\]

lies in the same tangent space.  Defect growth is therefore not a separate "strain mechanism": it is the directional derivative of one state function along the one Euler tangent velocity,

\[
\boxed{
\frac12 B'_{NL}=\langle h,F_E\rangle.
}
\]

## 3. Vortex stretching is the self-adjoint curl/Euler commutator

Because `C` is self-adjoint and `J_u` is skew-adjoint, the commutator

\[
[C,J_u]=CJ_u-J_uC
\]

is self-adjoint.  Its quadratic form on the centered defect is

\[
\boxed{
\langle r,[C,J_u]r\rangle
=2\langle Cr,J_ur\rangle
=2\int r\cdot S r\,dx.
}
\]

Consequently the full exact defect law is

\[
\boxed{
B'
=\langle r,[C,J_u]r\rangle
-2\nu\|Cr\|_2^2.
}
\]

This is the operator identity beneath the historical `POWER | strain/ACTION` split.  The two labels are different readings of the same commutator current.  In a curl eigenbasis the commutator matrix element automatically carries the eigenvalue gap `a-b`, which is the same signed-frequency difference appearing in Waleffe/helical work.

## 4. Curl--Lanczos form: nonlinear complexity can enter only through the next intrinsic direction

Normalize

\[
q_0=u/\sqrt E,\qquad q_1=r/\sqrt B.
\]

Define

\[
\beta_1=\sqrt{B/E},\qquad
\alpha_1=J/B,
\]

and, when `h` is nonzero,

\[
\beta_2=\|h\|/\sqrt B,\qquad q_2=h/\|h\|.
\]

Then curl itself generates the first Lanczos relations

\[
\boxed{
Cq_0=\lambda q_0+\beta_1q_1,
}
\]

\[
\boxed{
Cq_1=\beta_1q_0+\alpha_1q_1+\beta_2q_2.
}
\]

The Euler velocity is automatically orthogonal to the first two directions,

\[
F_E\perp q_0,q_1.
\]

Thus the first nonlinear change of curl variance can pass through only one intrinsic gate:

\[
\boxed{
B'_{NL}
=2\beta_2B\sqrt E\,
\Gamma,
\qquad
\Gamma:=\langle q_2,\mathbb P(q_0\times q_1)\rangle.
}
\]

`Gamma` is not a new owner or probability.  It is the actual Hilbert-space coherence of the first three state-generated curl directions.  The old words phase, strain alignment and multi-triad coherence are different representations of this same scalar current.

If `h=0`, the curl-cyclic subspace generated by `u` has dimension at most two.  Equivalently on the torus the energy spectral measure is supported on at most two signed curl eigenvalues.  Then

\[
B'_{NL}=0
\]

at that instant, although the Euler velocity itself need not vanish.  Rank one (`r=0`) is the exact Beltrami case and the Euler force vanishes completely.  Rank three is therefore the first level on which curl variance can be created at first order.

## 5. One-line critical determinant balance

The determinant

\[
\Delta=EZ-H^2=EB
\]

is invariant under the Navier--Stokes scaling on `R^3`, unlike `B` alone.  Combining the energy law with the defect law gives, whenever `B>0`,

\[
\boxed{
\frac{d}{dt}\log\Delta
=\frac{2\int r\cdot S r}{B}
-2\nu\left(
\frac ZE+\frac{\|Cr\|_2^2}{B}
\right).
}
\]

In Lanczos variables this is

\[
\boxed{
(\log\Delta)'
=2\sqrt E\,\beta_2\Gamma
-2\nu\big(
\lambda^2+2\beta_1^2+\alpha_1^2+\beta_2^2
\big).
}
\]

This is the current reduced problem in one line.  Every nonlinear increase of the critical Beltrami area has one gate `sqrt(E) beta_2 Gamma`; viscosity sees the sum of squares of the same curl--Krylov geometry.  A global no-escape theorem is **not** obtained merely by writing this identity: `Gamma` can be amplified by spatial concentration and coherent participation of many closed triads.

## 6. Gram/Vandermonde hierarchy is the native spectral-complexity hierarchy

For `n>=0` define

\[
D_n
:=\det\big(\langle C^iu,C^ju\rangle\big)_{0\le i,j\le n}.
\]

This is the squared volume of the intrinsic curl--Krylov parallelepiped.  For a finite spectral energy law `rho`, Cauchy--Binet gives the exact physical meaning

\[
\boxed{
D_n
=\frac1{(n+1)!}
\int\cdots\int
\prod_{i<j}(a_i-a_j)^2
\prod_{i=0}^{n}d\rho(a_i).
}
\]

Thus `D_n` is not abstract determinant bookkeeping.  It is the mean squared signed-curl Vandermonde volume of `n+1` actual energy samples.

The first two cases are

\[
D_1=\Delta=EB,
\]

and

\[
\boxed{D_2=D_1\|h\|_2^2.}
\]

For pure viscosity all vectors `u,Cu,...` evolve under the same operator `-nu C^2`.  If `q_0,...,q_n` is their orthonormal Krylov basis, the Gram volume therefore obeys the exact hierarchy

\[
\boxed{
(\log D_n)'_{visc}
=-2\nu\sum_{j=0}^{n}\|Cq_j\|_2^2.
}
\]

Equivalently, if `K_n(a)=sum_{j=0}^n p_j(a)^2` is the Christoffel--Darboux kernel of the **state's own** curl spectral measure and `dS_NL` is the signed nonlinear modal-energy source,

\[
\boxed{
(\log D_n)'
=\int K_n(a)\,dS_{NL}(a)
-2\nu\sum_{j=0}^{n}\|Cq_j\|_2^2.
}
\]

Using the curl-spectral curvature law `dS_NL=partial_a^2 kappa`, the same statement is

\[
\boxed{
(\log D_n)'
=\int K_n''(a)\kappa(a)\,da
-2\nu\sum_{j=0}^{n}\|Cq_j\|_2^2.
}
\]

Energy, critical Beltrami defect and higher signed-curl complexity are therefore levels of one law, not separate cases.

## 7. Three-point constitutive law: amplitudes can be solved exactly

Now restrict to one actual closed helical triad with three distinct signed curl eigenvalues `a_0,a_1,a_2`, modal energies `e_i=|A_i|^2`, Waleffe coupling `g`, and physical phase `Phi`.  The cyclic work formula gives

\[
T_i=(a_j-a_k)R,
\qquad
R=4\operatorname{Re}(A_0A_1A_2\overline g).
\]

The curvature current is

\[
Q_\triangle:=\sum_i a_i^2T_i
=R\,(a_0-a_1)(a_0-a_2)(a_1-a_2).
\]

But for a three-node state

\[
D_2=e_0e_1e_2
\prod_{i<j}(a_i-a_j)^2.
\]

Hence the exact constitutive factorization is

\[
\boxed{
|Q_\triangle|
=4|g|\,|\cos\Phi|\,\sqrt{D_2}
\le2\sqrt{D_2}.
}
\]

This removes the modal amplitudes from the constitutive mystery: their entire contribution is the intrinsic three-frequency Krylov volume `sqrt(D_2)`.  What remains is Waleffe geometry and the actual phase/orientation of the current.

There is a second sharp amplitude theorem for the gross same-triad energy current.  Put `p_i=e_i/E`,

\[
V=\frac BE
=\sum_{i<j}p_ip_j(a_i-a_j)^2,
\]

and

\[
L=\sum_{i<j}|a_i-a_j|=2(a_{max}-a_{min}).
\]

Cauchy gives

\[
\frac{V}{p_0p_1p_2}
=\frac{(a_0-a_1)^2}{p_2}
+\frac{(a_0-a_2)^2}{p_1}
+\frac{(a_1-a_2)^2}{p_0}
\ge L^2.
\]

Thus

\[
\chi_{bary}:=L\sqrt{\frac{p_0p_1p_2}{V}}\le1.
\]

The total positive work equals the total negative work on the triad and satisfies

\[
\boxed{
Q_{gross}
=2|g||\cos\Phi|\,E\sqrt B\,\chi_{bary}
\le E\sqrt B.
}
\]

Equality in the amplitude factor has the rigid weights

\[
\boxed{
p_i=\frac{|a_j-a_k|}{L}.}
\]

After ordering `a_-<a_m<a_+`, the median signed-curl mode therefore carries exactly

\[
\boxed{p_m=1/2,}
\]

and the state-selected barycenter is `lambda=a_m`.  Maximal defect-to-current conversion is a rigid half-median / half-barycentric-image state, not an arbitrary amplitude accident.

## 8. The `0.59813` geometry is now derived from the native defect normalization

On the symmetric heterochiral strict-upward slice, normalize child radius to one and put both parent radii equal to `r in (1/2,1)`.  After the preceding sharp amplitude reduction the exact log-upward factor is

\[
\boxed{
\mathcal U(r)
=\frac{\sqrt{4r^2-1}}{2\sqrt2\,r(1+r)}
\log\frac1r.
}
\]

Its stationary equation is

\[
\boxed{
\log(1/r)(1+2r-4r^3)
=(1+r)(4r^2-1).
}
\]

The interior solution is

\[
r\approx0.59812960658,
\]

with

\[
\mathcal U(r)\approx0.12480257853.
\]

This explains the exploratory `0.59813` observed in the predecessor draft directly from Beltrami variance plus actual Waleffe geometry.  It is distinct from the old Mellin value `0.610904...`.

**Scope guard.**  This section proves the one-dimensional symmetric formula and its stationary point.  The present note does not yet claim an analytic proof that the full two-parent/helicity variational problem is globally forced onto the symmetric slice.  Numerical exhaustion strongly selects the heterochiral symmetric point, but that remaining geometric step must be proved rather than baptized.

## 9. Why the single-triad bound must not be globalized by fiat

For one three-node closed triad, the previous theorem gives `|Q_curv|<=2 sqrt(D2)`.  A tempting full-field statement would replace the single triad by the whole state.  That is not scale-correct under localized `R^3` concentration.

For a localized Navier--Stokes rescaling

\[
u_\ell(x)=\ell^{-1}u(x/\ell),
\]

the full nonlinear defect production and the global `sqrt(D2)` do not acquire the same concentration power.  Equivalently, in physical variables

\[
\frac{B'_{NL}}{2\sqrt{D_2}}
=\langle q_2,\mathbb P(q_0\times q_1)\rangle
=:\Gamma
\]

is a genuine spatial/coherent concentration factor.  It is bounded by a Waleffe constant on one isolated triad but not fixed by the curl spectral magnitudes alone.

Direct divergence-free Gaussian-packet experiments on the repository Fourier geometry show this factor increasing systematically as the packet is narrowed; this is exactly the expected concentration obstruction, not a reason to insert a new owner class.

Thus the current frontier is sharply reduced:

\[
\boxed{
\text{all spectral/strain creation}
\longrightarrow
\text{one curl commutator current}
\longrightarrow
\text{one Krylov coherence/concentration factor }\Gamma,
}
\]

against

\[
\boxed{\text{the quadratic viscous curl cost}.}
\]

The remaining problem is constitutive/dynamical: determine how the actual Navier--Stokes evolution constrains sustained concentration and alignment of this one current.  Case-by-case owner proliferation would obscure precisely the quantity which remains.

## 10. What is and is not claimed

The note establishes exact finite/smooth identities and a sharp one-triad amplitude law.  It does **not** claim:

- a universal full-field bound `|B'_NL|<=2 sqrt(D2)`;
- that spectral magnitudes determine phase/current direction;
- that `Gamma` is a probability, owner, event counter or reset currency;
- a positive minimum scale step;
- a finite gross-transfer budget;
- global regularity of 3D Navier--Stokes.

Its main structural conclusion is smaller and stronger than another branch theorem:

> **The apparent POWER/strain/curl-curvature mechanisms are one self-adjoint commutator current generated by a skew Euler rotation and measured against the same curl operator whose square is viscosity.  The first unresolved full-PDE freedom is coherent spatial concentration of that current, not another physical owner type.**

## 11. Exact RMS curl-scale speed: viscosity never pushes the scale upward

The previous determinant law has a direct scale-facing companion which does not need a dyadic shell.  Define the physical RMS curl frequency

\[
N^2:=\frac ZE
\]

and the global Beltrami-defect fraction

\[
\theta:=\frac BZ
=1-\frac{H^2}{EZ}\in[0,1].
\]

Also put

\[
\sigma_r:=\frac{\int r\cdot S r}{B}
\]

when `B>0`.  Since

\[
Z'_{NL}=2\int r\cdot S r,
\qquad E'_{NL}=0,
\]

while viscosity acts diagonally on the curl spectrum, exact differentiation gives

\[
\boxed{
(\log N)'
=\theta\,\sigma_r
-\nu\,
\frac{\operatorname{Var}_{\rho/E}(a^2)}{N^2}.
}
\]

The second term is nonpositive.  Therefore

\[
\boxed{(\log N)'_{visc}\le0.}
\]

Viscosity can damp the field, narrow its squared-frequency distribution, or leave a monochromatic radius unchanged; it cannot be the mechanism which moves the RMS curl scale upward.

More importantly, the Euler part is tied exactly to the critical determinant current:

\[
(\log\Delta)'_{NL}=2\sigma_r,
\]

so

\[
\boxed{
(\log N)'_{NL}
=\frac\theta2(\log\Delta)'_{NL}.
}
\]

Thus there is no independent nonlinear "scale-progress mechanism".  Euler can move the RMS curl scale upward only by the same commutator current which inflates the critical Beltrami determinant, with exact conversion efficiency `theta=B/Z`.  Near a Beltrami state (`theta` small), nonlinear scale motion is automatically suppressed; away from Beltrami, the same defect is directly visible to viscosity.

This does **not** yet bound the total positive variation of the Euler determinant current.  A hypothetical singular cascade could try to run an Euler--viscous ratchet: Euler repeatedly spreads the signed-curl state while viscosity removes energy and contracts/reweights it.  The no-escape question is therefore reduced further: can this one intrinsic ratchet sustain unbounded RMS curl scale in finite physical time while respecting the determinant/Krylov dissipation hierarchy?  No owner taxonomy is needed to state that question.

## 12. The critical determinant has exactly two intrinsic boundary faces

Let

\[
K:=\int |a|\,d\rho(a)=\|u\|_{\dot H^{1/2}}^2
\]

in the helical spectral normalization, and write

\[
K_+=\int_{a>0}a\,d\rho,
\qquad
K_-=\int_{a<0}(-a)\,d\rho.
\]

Then `K=K_++K_-` and `H=K_+-K_-`.  The critical Beltrami determinant has the exact nonnegative split

\[
\boxed{
\Delta=EZ-H^2
=\underbrace{(EZ-K^2)}_{\Delta_{rad}}
+\underbrace{(K^2-H^2)}_{\Delta_{hel}}.
}
\]

Here

\[
\Delta_{rad}=E^2\operatorname{Var}_{\rho/E}(|a|)\ge0
\]

is radial-magnitude diversity, while

\[
\boxed{\Delta_{hel}=4K_+K_-\ge0}
\]

is coexistence of the two helicity signs.

This turns two older-looking cases into boundary faces of one state geometry.  If `Delta_hel=0`, only one helicity sign is occupied and `K=|H|`; Euler conserves `H`, so the first nonlinear variation of the critical stock vanishes.  If `Delta_rad=0`, all occupied modes have one common radius and `K=R E`; Euler conserves `E`, so again the first nonlinear critical variation vanishes.  Real equiradial energy exchange may still occur, but it cannot grow the critical absolute-curl moment.

Therefore

\[
\boxed{
\text{nonlinear critical }\dot H^{1/2}\text{ growth requires both radial diversity and helicity-sign diversity.}
}
\]

This is not a dichotomy to route.  It says the critical source lives in the interior of the same Beltrami Gram geometry and vanishes on either intrinsic boundary face.  The `0.59813` heterochiral transport geometry should therefore be read as an interior constitutive optimization of this two-face state, not as a separately invented "heterochiral branch".

## 13. Universal tangent-volume law for every spectral observable

The preceding quadratic and critical identities are instances of one shorter statement.  Let `phi` be any real function on the occupied signed-curl spectrum and define the physical spectral stock

\[
M_\phi=\langle u,\phi(C)u\rangle.
\]

Euler preserves the two affine stocks generated by `1` and `a`.  Therefore only the component of `phi(C)u` tangent to the common `E,H` manifold can change under Euler.  Put

\[
g_\phi:=P_{\{u,Cu\}^{\perp}}\phi(C)u.
\]

Then exactly

\[
\boxed{
(M_\phi)'_{NL}=2\langle g_\phi,F_E\rangle,
\qquad F_E=\mathbb P(u\times Cu).
}
\]

Its squared size is encoded without choosing coordinates by the Gram determinant

\[
\boxed{
\mathcal D_\phi
:=\det\operatorname{Gram}(u,Cu,\phi(C)u)
=\Delta\,\|g_\phi\|_2^2.
}
\]

Thus an observable has no independent nonlinear source whenever it is affine on the occupied curl spectrum.  Energy and helicity are simply the universal affine kernel of the same tangent-volume rule.

For one three-node closed helical triad, something stronger happens.  Cauchy--Binet gives

\[
\mathcal D_\phi
=e_0e_1e_2
\det\!\begin{pmatrix}
1&a_0&\phi(a_0)\\
1&a_1&\phi(a_1)\\
1&a_2&\phi(a_2)
\end{pmatrix}^{\!2}.
\]

The cyclic work formula uses the very same oriented determinant.  Hence the exact universal constitutive law is

\[
\boxed{
\left|\sum_i\phi(a_i)T_i\right|
=4|g_\triangle|\,|\cos\Phi|\,
\sqrt{\mathcal D_\phi}.
}
\]

No separate proof mechanism is needed for each observable.  The Waleffe interaction converts the **state volume outside the affine energy--helicity plane** into the production of whichever spectral observable is being read.

Important instances are now one line apart:

- `phi=1`: `D_phi=0`, kinetic energy is invariant under Euler;
- `phi=a`: `D_phi=0`, signed helicity is invariant under Euler;
- `phi=a^2`: `D_phi=D_2`, giving enstrophy/vortex-stretching/Beltrami-curvature production;
- `phi=|a|`: `D_phi` is the critical `H^{1/2}` tangent volume.  It vanishes both on a one-helicity-sign support, where `|a|=+-a`, and on an equiradial support, where `|a|` is constant.

The last item explains the two boundary faces in Section 12 without a case classification: homochirality and equiradiality are simply two different ways for the same observable `|a|` to fall back into the affine invariant plane.

This is currently the shortest structural statement in the draft.  The remaining full-PDE difficulty is not to invent more observables; it is to control the coherent sum of these local oriented volumes when many Fourier triads share the same physical field.

## 14. The full spectral current has a native Fisher--Rao speed

The universal observable law has a full-state Hilbert-space formulation which identifies what the curl-spectrum equation is actually measuring.

Let `P_a` be the spectral projector of curl at a signed eigenvalue `a`, and put

\[
u_a=P_a u,\qquad E_a=\|u_a\|_2^2.
\]

For the Euler velocity `F_E`, the physical modal-energy source is

\[
S_a=2\operatorname{Re}\langle u_a,P_aF_E\rangle.
\]

The curl-cyclic subspace generated by the current state is

\[
\mathcal K_u=\overline{\{p(C)u\}}.
\]

For finitely many occupied curl eigenvalues this is exactly the span of the existing vectors `u_a`, one state direction inside each possibly highly degenerate curl eigenspace.  Decompose the one Euler velocity orthogonally,

\[
\boxed{
F_E=F_{spec}+F_{shape},
\qquad
F_{spec}=P_{\mathcal K_u}F_E.
}
\]

Then `F_shape` is first-order isospectral:

\[
\operatorname{Re}\langle u_a,P_aF_{shape}\rangle=0
\]

for every occupied node.  It can rotate angular/phase structure inside degenerate eigenspaces or seed new amplitudes, but it does not directly change the signed-curl energy measure at that instant.  Every first-order curl-spectral observable sees only `F_spec`.

Moreover,

\[
\boxed{
\|F_{spec}\|_2^2
=\frac14\sum_{a:E_a>0}\frac{S_a^2}{E_a}.
}
\]

This is the Fisher--Rao tangent speed of the **physical positive energy measure itself**.  No causal probability or normalization is introduced; it is simply Parseval for the projection of the actual Euler vector field along the existing energy directions.

Energy and helicity conservation say `F_spec` is orthogonal to the first two curl--Krylov directions.  For any spectral observable `phi`, Section 13 and Cauchy therefore give the universal speed law

\[
\boxed{
|(M_\phi)'_{NL}|
\le
2\sqrt{\frac{\mathcal D_\phi}{\Delta}}\,
\|F_{spec}\|_2.
}
\]

All apparent spectral sources compete for the same instantaneous Euler velocity.  There is not one independent current for enstrophy, another for critical growth and another for radial transport.

### Closed triad: the constitutive metric law becomes exact

For a three-node closed helical triad the spectral tangent space after the two affine constraints has dimension exactly one.  Hence every non-affine spectral observable is automatically collinear with the same source direction and the preceding Cauchy inequality is an equality.

More strongly, with physical cyclic work `T_i`,

\[
\boxed{
\mathcal A_{spec}
:=\frac14\sum_i\frac{T_i^2}{e_i}
=4|g_\triangle|^2\cos^2\Phi\;\Delta.
}
\]

Thus

\[
\boxed{
\|F_{spec}\|=2|g_\triangle||\cos\Phi|\sqrt\Delta
\le\sqrt\Delta.
}
\]

This identity is independent of the distribution of the three modal amplitudes.  It is the most compact three-node constitutive statement obtained so far:

- `Delta` is the intrinsic metric size of available signed-curl shear;
- `2|g|` is the physical Waleffe geometric efficiency;
- `cos Phi` is the current orientation;
- their product is the actual speed of the unique energy--helicity-preserving spectral motion.

The sharp observable-volume law of Section 13 is now a corollary: on a one-dimensional tangent space, an observable production is just its tangent-gradient length times this one physical speed.

For the full PDE, several triads contribute coherently to the same `S_a` before the square in `A_spec` is taken.  That coherent amplification, together with the vertical `F_shape` dynamics which continually changes future Waleffe geometry, is the genuine constitutive frontier.  It should not be renamed as a collection of source/strain/phase cases.

## 15. The signed curl current is a deterministic martingale spread plus quadratic killing

The three-node barycentric law has a more intrinsic interpretation than donor/recipient case language.  Order one nondegenerate closed-triad curl spectrum as

\[
a<m<b.
\]

On the spread orientation let `q>0` be the actual physical work leaving the median node.  Energy and helicity conservation force the unique recipient fractions

\[
\boxed{
p_- =\frac{b-m}{b-a},
\qquad
p_+=\frac{m-a}{b-a},
}
\]

so

\[
\boxed{p_-a+p_+b=m.}
\]

Thus the same-time physical work law is a martingale transport of signed curl:

\[
q\,\delta_m
\longrightarrow
q\,(p_-\delta_a+p_+\delta_b).
\]

This is **not** a stochastic temporal ancestry and introduces no FIFO/LIFO matching.  It is simply the unique positive transport table of the actual simultaneous three-mode energy work.

Its conditional variance is

\[
\boxed{
p_-(a-m)^2+p_+(b-m)^2=(m-a)(b-m).}
\]

Therefore the nonlinear enstrophy/Beltrami-defect production is literally the quadratic-variation rate of this signed-curl martingale spread:

\[
\boxed{
Z'_{\triangle,NL}=B'_{\triangle,NL}
=q\,(m-a)(b-m).
}
\]

For any observable `phi`, the same generator gives

\[
\boxed{
\frac{1}{q}(M_\phi)'_{\triangle,NL}
=p_-\phi(a)+p_+\phi(b)-\phi(m)
=(m-a)(b-m)\,\phi[a,m,b].
}
\]

This is the deterministic three-point Itô/Tanaka grammar already hidden in the divided-difference theorem:

- affine `phi` has zero curvature, giving energy/helicity conservation;
- `phi=a^2` reads quadratic variation, giving vortex stretching;
- `phi=|a|` reads the kink at the helicity interface `a=0`, giving critical `H^{1/2}` pair creation; on one-sign support it is affine and the source vanishes;
- every convex `phi` increases under a spread and decreases under the reverse contraction.

At continuum level the positive tent in the curl-curvature theorem is exactly the potential of this elementary martingale increment.  The signed curvature current `kappa` is a superposition of spread tents and reverse-spread tents.  Viscosity contributes the separate native killing

\[
-2\nu a^2\rho.
\]

The smallest spectral grammar is therefore now

\[
\boxed{
\text{positive energy state}
+\text{signed martingale curvature current}
+\text{Waleffe/Fisher constitutive speed}
+\text{quadratic viscous killing}.
}
\]

The only part not encoded by the one-dimensional curl state is the vertical shape motion which determines how coherently many physical triads can drive the next spread.

### Continuous no-free-progress corollary

The martingale geometry also removes the need for a fixed signed-good progress threshold on a strict heterochiral UV-frontier spread.  Normalize the high recipient radius to one, let `D` be the same-helicity median donor radius and `S` the opposite-helicity side-recipient radius.  Then

\[
W_d=(1+S)R,\qquad
W_c=(D+S)R,\qquad
W_s=(1-D)R,
\]

and the child upper-parent log-progress work is

\[
F_c=W_c\log\frac1{\max(D,S)}.
\]

For all physical `0<D,S<1`,

\[
\boxed{F_c\le2W_s.}
\]

Indeed if `max(D,S)=D`, then `D+S<=2D` and `D log(1/D)<=1-D`; if the maximum is `S`, then `D+S<=2S` and `S log(1/S)<=1-S<=1-D`.

So genuine forward log-scale action never occurs without a compulsory same-time positive nonforward sibling.  This is a continuous physical inequality, not an event tax.  The sibling energy remains in Navier--Stokes and may interact later, so `W_s` is **not** promoted to a globally expendable budget.  The remaining no-escape question is precisely whether repeated physical recycling/recombination can defeat the martingale-spread plus quadratic-killing structure.

## 16. Isolated triad phase is a rotation of one fixed nonlinear speed

The Fisher law can be lifted from modal-energy rates back to the three complex helical amplitudes.  In the common cyclic Waleffe gauge an isolated three-wave Euler system has

\[
\boxed{
\dot A_0=2(a_1-a_2)g\,\overline{A_1A_2},
}
\]

with the two cyclic companions.  Let `e_i=|A_i|^2`.  A direct sum gives

\[
\begin{aligned}
\sum_i|\dot A_i|^2
&=4|g|^2\big[(a_1-a_2)^2e_1e_2
 +(a_2-a_0)^2e_2e_0
 +(a_0-a_1)^2e_0e_1\big]\\
&=\boxed{4|g|^2\Delta}.
\end{aligned}
\]

So for fixed instantaneous modal energies and geometry the **total nonlinear amplitude speed is independent of the common triad phase**.

For each complex modal line, decompose `dot A_i` into the real radial component parallel to `A_i` and the quadrature component parallel to `iA_i`.  If

\[
P=A_0A_1A_2\bar g
=|g|\sqrt{e_0e_1e_2}\,e^{i\Phi},
\]

then exactly

\[
\boxed{
\mathcal A_{rad}=4|g|^2\Delta\cos^2\Phi,
\qquad
\mathcal A_{phase}=4|g|^2\Delta\sin^2\Phi,
}
\]

and

\[
\boxed{
\mathcal A_{rad}+\mathcal A_{phase}=4|g|^2\Delta.
}
\]

The first term is precisely the Fisher--Rao spectral energy speed of Section 14.  The second changes complex phase/shape while carrying no instantaneous modal-energy work.  Therefore phase cancellation does not switch the nonlinearity off: it rotates the same physical nonlinear velocity from energy redistribution into state reorientation.

There is also an exact isolated-triad phase invariant.  Differentiate the same cubic product:

\[
\dot P
=2|g|^2\big[
(a_1-a_2)e_1e_2
+(a_2-a_0)e_2e_0
+(a_0-a_1)e_0e_1
\big]\in\mathbb R.
\]

Hence

\[
\boxed{\operatorname{Im}(A_0A_1A_2\bar g)=\text{constant}}
\]

for the isolated Euler triad.

Together with energy and helicity, this reduces the three-wave energy dynamics to one real coordinate.  Indeed

\[
(\operatorname{Re}P)^2
=|g|^2e_0e_1e_2-(\operatorname{Im}P)^2,
\]

while the two affine invariants make two of the `e_i` affine functions of the third.  The isolated transfer therefore moves in a compact one-dimensional cubic potential and reaches turning points where `Re P=0`; it cannot supply a one-way unbounded curl escape by itself.

This does **not** give a global triad-by-triad invariant in Navier--Stokes.  In the full field, one Fourier/helical amplitude belongs to many simultaneous triads.  Their vertical shape velocities change each other's phases and destroy the isolated cubic invariant.  The important reduction is conceptual:

\[
\boxed{
\text{global cascade requires cooperative triad interference to continually reorient otherwise self-contained three-wave currents.}
}
\]

The remaining primitive problem is therefore not a list of owner types.  It is whether this cooperative reorientation can stay coherent strongly enough, for long enough, against the same quadratic curl killing, to drive an infinite pre-singular martingale-spread chain.

## 17. Critical growth is radial variance times sign--radius decorrelation, not a crude helicity product

The nonnegative split `Delta=Delta_rad+Delta_hel` identifies two boundary faces, but it does **not** imply a product bound on the critical source.  Direct adversarial spectral states disprove the tempting estimate `E D_|a| <= Delta_rad Delta_hel` by large factors.  The exact variable is a covariance.

Define

\[
L:=\int a|a|\,d\rho(a)
\]

and the centered signed-radius covariance numerator

\[
C_{a,|a|}:=EL-HK.
\]

Then direct Gram algebra gives

\[
\boxed{
E\,\mathcal D_{|a|}
=\Delta\,\Delta_{rad}-C_{a,|a|}^2.
}
\]

When `Delta Delta_rad>0`, put

\[
\varrho
:=\frac{EL-HK}{\sqrt{\Delta\Delta_{rad}}}.
\]

This is exactly the correlation coefficient between signed curl `a` and radius `|a|` under the normalized physical energy measure.  Therefore

\[
\boxed{
\mathcal D_{|a|}
=\frac{\Delta\Delta_{rad}}{E}(1-\varrho^2),
}
\]

and the squared critical tangent-gradient length is

\[
\boxed{
\|g_{|C|}\|_2^2
=\frac{\Delta_{rad}}{E}(1-\varrho^2).
}
\]

This is more informative than the amount of opposite helicity by itself.  A one-sign state has `|a|=+-a`, so `|varrho|=1` and the critical tangent volume vanishes.  An equiradial state has `Delta_rad=0`.  More generally, even with both helicity signs present, critical production is weak when sign and radius are almost perfectly locked.

For the full Euler spectral velocity, Section 14 now gives the exact geometric bound

\[
\boxed{
|K'_{NL}|
\le
2\sqrt{\frac{\Delta_{rad}}{E}}\,
\sqrt{1-\varrho^2}\,
\|F_{spec}\|_2.
}
\]

For one closed triad the one-dimensional tangent space makes this an equality after inserting the Waleffe speed.  Thus critical `H^{1/2}` growth is not an independent source mechanism: it is the part of the one spectral Euler velocity visible only when radial diversity and sign--radius decorrelation coexist.

## 18. The PDE itself supplies an interaction volume and a critical Reynolds barrier

The full-field obstruction identified above can be measured without choosing a packet, window, shell or coherent atom.  Let

\[
\mathcal A_{spec}:=\|F_{spec}\|_2^2
=\frac14\int\left(\frac{dS_{NL}}{d\rho}\right)^2d\rho
\]

in the discrete/absolutely-continuous sense of Section 14, and define

\[
\boxed{
V_{spec}:=\frac{\Delta}{\mathcal A_{spec}}
}
\]

when the spectral Euler current is nonzero.  Under the Navier--Stokes rescaling on `R^3`, `Delta` is critical while `A_spec` scales as inverse volume, so `V_spec` transforms exactly as a physical volume.  It is an **interaction volume inferred from the actual PDE current itself**, not a localization chosen by the analyst.

The determinant law and Cauchy give

\[
(\log\Delta)'
\le
2\beta_2\sqrt{\frac{\mathcal A_{spec}}B}
-2\nu(\lambda^2+2\beta_1^2+\alpha_1^2+\beta_2^2).
\]

Complete the square only in the intrinsic next-curl coefficient `beta_2`:

\[
\boxed{
(\log\Delta)'
\le
\frac{\mathcal A_{spec}}{2\nu B}
-2\nu(\lambda^2+2\beta_1^2+\alpha_1^2).
}
\]

Since

\[
N^2:=\frac ZE=\lambda^2+\beta_1^2,
\]

a necessary condition for positive growth of the critical determinant is

\[
\frac{\mathcal A_{spec}}{\nu^2N^2B}>4.
\]

Define the entirely physical, scale-invariant spectral Reynolds number

\[
\boxed{
\operatorname{Re}_{spec}
:=\frac{\sqrt{\mathcal A_{spec}}}{\nu N\sqrt B}
=\frac1\nu\sqrt{\frac{E}{N^2V_{spec}}}.
}
\]

Then

\[
\boxed{
\Delta' >0
\quad\Longrightarrow\quad
\operatorname{Re}_{spec}>2.
}
\]

Equivalently, the PDE-defined critical interaction mass

\[
\boxed{
\mathfrak M_{spec}
:=\frac{E}{N^2V_{spec}}
=\frac{\mathcal A_{spec}}{N^2B}
}
\]

must satisfy

\[
\boxed{\mathfrak M_{spec}>4\nu^2.}
\]

This recovers the familiar critical-mass dimensional combination without inserting a spatial packet.  The threshold `2` is not an event gate: it is the algebraic point at which the one Euler spectral speed can first overcome the quadratic curl killing after the exact next-Krylov coefficient has been optimized.

There is an even more primitive overlap lower bound.  Projection and Leray contraction imply

\[
\mathcal A_{spec}
\le\|F_E\|_2^2
\le\|u\times r\|_2^2.
\]

Thus

\[
V_{spec}
\ge
\frac{EB}{\|u\times r\|_2^2},
\]

so any state with `Re_spec>2` is necessarily concentrating the actual velocity--defect overlap as well.  This is the precise full-PDE freedom which the isolated Waleffe bound does not control.

The global no-escape question has therefore compressed to a single dynamical issue:

> **Can a Navier--Stokes trajectory keep its intrinsic spectral Reynolds number above the critical value while the PDE-defined interaction volume repeatedly collapses toward zero, despite the same curl geometry feeding quadratic viscous killing and despite isolated triad currents being self-contained martingale/phase systems?**

No additional owner type is needed to state the remaining problem.

## 19. Curl Nijenhuis curvature is the full-field source behind the three-frequency law

The scalar signed-curl picture has an operator origin.  Let

\[
[u,v]=(u\cdot\nabla)v-(v\cdot\nabla)u
\]

be the Lie bracket of mean-zero divergence-free vector fields and keep

\[
C=\operatorname{curl},\qquad J_u v=\mathbb P(u\times v).
\]

Then, before any spectral decomposition,

\[
\boxed{C J_u v=-[u,v].}
\]

Thus the Euler interaction is the inverse-curl image of the native Lie-algebra noncommutativity.  The object which detects whether this bracket really opens a new curl level is the Nijenhuis torsion of `C`,

\[
T_C(u,v)
=[Cu,Cv]-C([Cu,v]+[u,Cv])+C^2[u,v].
\]

If `Cu=a u` and `Cv=b v`, then exactly

\[
\boxed{T_C(u,v)=(C-a)(C-b)[u,v].}
\]

Consequently its component at a child curl eigenvalue `c` is

\[
\boxed{P_cT_C(u,v)=(c-a)(c-b)P_c[u,v].}
\]

The torsion vanishes when the bracket output stays on either parent curl level.  Such a degenerate interaction can still rotate physical state inside an existing curl eigenspace, but it cannot create first-order signed-curl spectral curvature.  A genuinely third curl level is exactly a torsionful event.

The symmetric curvature obtained from this antisymmetric torsion is

\[
\boxed{
\mathscr K_C(x,y)=T_C(Cx,y)-T_C(x,Cy).
}
\]

On a pair of parent eigenfields its child-`c` component carries

\[
(a-b)(c-a)(c-b),
\]

the same three-frequency Vandermonde factor already found in the closed-triad curvature current.  In particular, if `T_c` is the physical energy work at one root of a closed triad, then

\[
\boxed{
Q_\triangle=(c-a)(c-b)T_c.
}
\]

So the old scalar identity is exactly the root-wise Nijenhuis torsion multiplier.

There is a full-field identity, not merely a triad formula.  Define

\[
\Omega(x,y,z)=\langle C^{-1}x,[y,z]\rangle.
\]

Because `CJ_yz=-[y,z]`,

\[
\Omega(x,y,z)=-\int x\cdot(y\times z)\,dx,
\]

so `Omega` is totally alternating.  Expanding `T_C(Cu,u)` and using only this alternating three-form gives

\[
\begin{aligned}
\langle C^{-1}u,\mathscr K_C(u,u)\rangle
&=2\langle C^{-1}u,T_C(Cu,u)\rangle\\
&=6\Omega(C^2u,Cu,u).
\end{aligned}
\]

But Euler vorticity obeys `omega_t=[omega,u]`, hence

\[
\dot Z_{NL}=2\Omega(C^2u,Cu,u).
\]

Therefore

\[
\boxed{
\dot Z_{NL}
=\dot B_{NL}
=\frac13\langle C^{-1}u,\mathscr K_C(u,u)\rangle
=2\int r\cdot S r\,dx.
}
\]

This is the exact collapse sought here: **physical-space strain stretching, signed-curl curvature production, three-frequency Waleffe Vandermonde geometry, and the failure of curl eigenspaces to close under the divergence-free Lie bracket are one and the same nonlinear curvature.**

The Fourier-Galerkin referee checked the global Nijenhuis pairing on several independent smooth states/resolutions; the ratio
`<C^-1 u,K_C(u,u)> / Z'_NL` was `3` to relative residual below `1.1e-15`.  These numerical checks are evidence for the implementation convention; the displayed operator proof is the theorem-level reason.

## 20. Exact critical live balance and the radial impedance law

Let

\[
K=\int |a|\,d\rho
\]

be the critical `H^{1/2}` stock.  The one full spectral Euler velocity has squared speed `A_spec`.  Let `g_K` be the tangent projection of `|C|u` off the affine invariant plane `span{u,Cu}` and write

\[
\gamma
=\frac{\langle g_K,F_{spec}\rangle}
{\|g_K\|\,\|F_{spec}\|}
\in[-1,1].
\]

This is not a new phase variable: it is simply the Hilbert angle between the actual spectral current and the unique instantaneous direction which increases `K` most rapidly.

Use the dimensionless state factors

\[
\theta=\frac{B}{Z},
\qquad
\eta=1-\frac{K^2}{EZ}
=\frac{\Delta_{rad}}{EZ},
\qquad
d=1-\varrho^2.
\]

Then the tangent-volume identity gives exactly

\[
\left.\dot K\right|_{NL}
=2\gamma\sqrt{\theta\eta d\,B}\,\sqrt{\mathcal A_{spec}}.
\]

For viscosity put

\[
M_3=\int |a|^3\,d\rho,
\qquad
\mu_3=\frac{M_3}{EN^3}.
\]

The exact full critical balance is therefore

\[
\boxed{
\frac{\dot K}{2\nu EN^3}
=\gamma\,\mathrm{Re}_{spec}\sqrt{\theta\eta d}-\mu_3.
}
\]

The radial moments themselves now supply a second intrinsic constraint.  Log-convexity

\[
Z^2\le K M_3
\]

gives

\[
\boxed{
\mu_3\ge\frac1{\sqrt{1-\eta}}.
}
\]

Introduce the radial coefficient of variation

\[
x=\sqrt{\frac{\eta}{1-\eta}}.
\]

Then any positive critical growth must satisfy

\[
\boxed{
\gamma\,\mathrm{Re}_{spec}\sqrt{\theta d}
>x+\frac1x
\ge2.
}
\]

This is a single continuous **radial impedance law**.  A nearly monochromatic radial state has `x<<1`: it has too little radial tangent geometry to grow `K`.  An excessively broad radial state has `x>>1`: its cubic viscous moment is too large.  The least expensive geometry is the balanced point `x=1`, equivalently `eta=1/2`, and even there the effective spectral Reynolds drive must exceed `2`.

Thus

\[
\boxed{
\dot K>0\quad\Longrightarrow\quad \mathrm{Re}_{spec}>2.
}
\]

The threshold is outer-sharp at the level of positive moment geometry: equality in `Z^2<=KM_3` is approached by a radial law supported near zero and one high radius, with half the energy on each radial level.  Simultaneous saturation of the Navier--Stokes Waleffe geometry is a stricter constitutive question and is **not** asserted.

The same refinement sharpens the determinant barrier.  With

\[
\chi=\frac{\alpha_1^2}{N^2},
\]

completing the square in the exact Lanczos law gives the state-dependent necessary condition

\[
\boxed{
\dot\Delta>0
\quad\Longrightarrow\quad
\mathrm{Re}_{spec}>2\sqrt{1+\theta+\chi},
}
\]

before any further alignment loss is used.

## 21. The remaining question is now a cooperative-alignment problem, not an owner problem

At this stage the dangerous instantaneous state has to satisfy all of the following **multiplicatively in one law**, not as a list of branches:

\[
\text{non-Beltrami defect}
\times
\text{radial diversity}
\times
\text{sign--radius decorrelation}
\times
\text{spectral-current alignment}
\times
\text{coherent interaction concentration}
\]

strong enough to beat the cubic/quadratic viscous moments of the same curl distribution.

A Galerkin NS snapshot illustrates why `Re_spec>2` alone is not the mechanism.  One tested state at `nu=0.002` had

`Re_spec ~= 2.62`, `theta ~= 0.904`, `eta ~= 0.128`, `d ~= 0.974`, `gamma ~= 0.32`,

but its live nonlinear factor was only about `0.28` against `mu3 ~= 1.206`, so the critical stock was still decreasing.  Lowering viscosity eventually reverses the sign exactly where the displayed live balance predicts.

The global question has therefore become sharper:

> **Can the vertical/ispectral Euler motion of overlapping triads continually rotate the one spectral current so that its critical alignment stays strong, while at the same time maintaining the defect/radial/decorrelation geometry and the shrinking PDE-defined interaction volume required by `Re_spec>2`, for infinitely many increasingly small scales?**

The next step should attack that cooperative alignment directly.  It should not reintroduce source/strain/HH cases: those have already collapsed into the Nijenhuis/Fisher/current geometry above.

## 22. The curl spectrum is an exact replicator law driven by one nonlinear fitness

The Fisher formulation can be written without any geometric metaphor.  At an occupied signed-curl level `a`, let

\[
E_a=\|P_a u\|_2^2,
\qquad
S_a=2\operatorname{Re}\langle P_a u,P_aF_E\rangle,
\]

and define the actual Euler fitness

\[
\boxed{f(a)=\frac{S_a}{2E_a}.}
\]

Then the complete first-order nonlinear spectral law is simply

\[
\boxed{\partial_t\rho\big|_{NL}=2f\rho.}
\]

Energy and helicity conservation are exactly

\[
\boxed{\int f\,d\rho=0,\qquad \int a f\,d\rho=0,}
\]

and the native Fisher action is

\[
\boxed{\mathcal A_{spec}=\int f^2\,d\rho.}
\]

Viscosity is not another spectral mechanism.  It adds the universal quadratic negative fitness

\[
\boxed{\partial_t\rho=2(f(a)-\nu a^2)\rho.}
\]

For the normalized energy probability `p=rho/E`,

\[
\boxed{
\partial_t p
=2\bigl(f(a)-\nu(a^2-N^2)\bigr)p.
}
\]

Thus the exact relative selection law is

\[
\boxed{
\frac12\frac d{dt}\log\frac{p(a)}{p(b)}
=f(a)-f(b)-\nu(a^2-b^2).
}
\]

A high-curl population can outrun a lower one only if the **actual nonlinear fitness advantage** beats a quadratic viscous disadvantage.  The open constitutive question is therefore not whether diffusion is strong at high frequency; it is how the Lie/Waleffe dynamics can manufacture and maintain such a fitness advantage.

Expand `f` in the orthonormal curl--Krylov polynomials for the current physical measure,

\[
f=\sum_{n\ge2}c_np_n.
\]

The first two coefficients vanish by the two affine null laws, and

\[
\boxed{\mathcal A_{spec}=\sum_{n\ge2}c_n^2.}
\]

Because `a^2` belongs to `span{p_0,p_1,p_2}`, the nonlinear enstrophy/Nijenhuis current sees **only `c_2`**.  All `c_{n>=3}` are spectral reconfiguration directions: they alter the shape of the curl distribution and hence future `c_2`, but they do not change enstrophy at first order.  Viscosity, whose fitness is exactly `-nu a^2`, also lives in the first three Krylov directions.

Actual Galerkin states falsify a tempting finite-dimensional shortcut: in three tested smooth states, `c_2^2/A_spec` was only about `9--10%`, `c_3^2/A_spec` about `15--19%`, and between roughly `22%` and `68%` of the Fisher action remained beyond Krylov level `24`.  The full PDE genuinely uses a high-dimensional reconfiguration score.  No low-order closure is asserted.

The curvature potential is the Green-potential form of the same score.  Since the signed source `2f rho` annihilates `1` and `a`,

\[
\boxed{
\kappa(x)=\int |x-a|\,f(a)\,d\rho(a)
}
\]

(up to the already-fixed normalization convention), and `partial_x^2 kappa=2f rho`.  Hence the curvature equation and the replicator equation are the same physical law in additive and multiplicative coordinates.

## 23. One Lamb field, then orthogonal projections

There is an even shorter physical ontology before the curl spectrum is read.  Put

\[
\boxed{L=u\times\omega,\qquad \omega=Cu.}
\]

The rotational Navier--Stokes equation is

\[
u_t=\mathbb P L-\nu C^2u.
\]

The raw Lamb field has the exact Helmholtz split

\[
\boxed{
L=(I-\mathbb P)L+F_E,
\qquad
\|L\|_2^2=\|(I-\mathbb P)L\|_2^2+\|F_E\|_2^2.
}
\]

The gradient term is the Bernoulli/pressure reaction required by incompressibility; it is not an independent nonlinear source.  The solenoidal term is the one Euler velocity.  Its helical modal action then splits exactly as

\[
\boxed{
\|F_E\|_2^2
=A_{spec}+A_{within}+A_{phase}+A_{birth}.
}
\]

Here `A_spec` changes total energy on existing curl eigenspaces, `A_within` redistributes amplitude among modes with the same signed curl eigenvalue, `A_phase` rotates occupied complex amplitudes, and `A_birth` creates first-order amplitude at currently zero coordinates.  This is a Pythagorean identity, not a classification of physical owners.

The same Lamb field also satisfies

\[
\boxed{\nabla\times L=-[u,\omega],}
\]

so its curl is the Lie transport of vorticity.  Applying the curl Nijenhuis construction to this bracket yields the spectral curvature identity of Section 19.  Pressure, Lie transport, phase/shape motion, Fisher spectral motion and vortex stretching are therefore successive projections or derivatives of **one nonlinear field `u x omega`**.

Finally the critical Gram determinant controls the `L^1` size of this field.  With the global Beltrami defect `r=(C-lambda)u`,

\[
|L|=|u\times r|,
\]

and Cauchy gives

\[
\boxed{
\|L\|_1^2\le E\|r\|_2^2=\Delta.
}
\]

Since `A_spec<=||F_E||_2^2<=||L||_2^2`, the PDE-defined interaction volume obeys the packet-free lower chain

\[
\boxed{
V_{spec}=\frac{\Delta}{A_{spec}}
\ge\frac{\Delta}{\|L\|_2^2}
\ge\frac{\|L\|_1^2}{\|L\|_2^2}.
}
\]

The last quotient is the participation volume of the actual Lamb activity.  Thus collapse of `V_spec` necessarily entails **physical-space concentration of the one nonlinear field itself**.  This identifies the remaining obstacle without choosing a packet, cell or shell.

## 24. No spectral-only monotone can contain the missing mechanism

The previous reduction also gives a useful impossibility theorem.  Fix one physical nondegenerate closed triad, its wavevectors/helicities and all three modal energies.  The cyclic current is proportional to the real phase quadrature.  Shifting the common physical triad phase by `pi` reverses that current while preserving the whole signed-curl energy measure.

For every spectral observable `phi`,

\[
\dot M_\phi=Q_\triangle\,\phi[a_0,a_1,a_2].
\]

Therefore, whenever the second divided difference is nonzero, the same spectral state admits both signs of instantaneous Euler production through two physical phase orientations.  Only affine `phi`, whose second divided difference vanishes identically, are protected for every state.

So there is no universal monotone scalar depending only on `rho` beyond the affine energy/helicity invariants.  Any no-escape theorem must retain the **orientation/current dynamics** which `rho` alone forgets.  This rules out replacing the remaining constitutive problem by an invented spectral entropy.

## 25. UV log progress is bounded by the same Nijenhuis quadratic variation

For the strict heterochiral UV spread of Section 15, restore the child radius `M` and use signed curl levels

\[
-SM<DM<M,
\qquad 0<D,S<1,\qquad D+S>1.
\]

Per common positive current `R`, conservation gives

\[
W_d=(1+S)R,
\qquad
W_c=(D+S)R,
\qquad
W_s=(1-D)R.
\]

The positive nonlinear enstrophy/Nijenhuis curvature of this same martingale spread factorizes exactly:

\[
\boxed{
Q_\triangle
=M^2(1-D)(1+S)(D+S)R.
}
\]

The actual high-child log progress is

\[
F_{log}=(D+S)R\log\frac1{\max(D,S)}.
\]

On the entire strict physical triangle,

\[
\boxed{M^2F_{log}\le Q_\triangle.}
\]

The proof is one-dimensional.  If `D>=S`, then `D>1/2`, `S>1-D`, and

\[
-\log D\le(1-D)(2-D)< (1-D)(1+S).
\]

If `S>=D`, then `S>1/2` and

\[
-\log S\le1-S^2\le(1-D)(1+S).
\]

The constant `1` is sharp at the degenerate boundary `D->1`, `S->0` approached from `D+S>1`.  Thus no uniform strict loss is hidden here.  What is exact is deeper: **every unit of genuine UV log displacement is dominated by quadratic variation of the same signed-curl martingale current.**

## 26. One impedance grammar appears in two independent escape observables

The determinant and critical-stock balances are not merely analogous.  After their native variables are used they have the same algebraic shape.

For the determinant, write the Euler spectral velocity in the curl--Lanczos frame.  Only its `q2` coefficient `c2` contributes to `Delta` growth.  Put

\[
\gamma_2=\frac{c_2}{\sqrt{\mathcal A_{spec}}},
\qquad
y=\frac{\beta_2}{N},
\qquad
\theta=\frac{\beta_1^2}{N^2},
\qquad
\chi=\frac{\alpha_1^2}{N^2}.
\]

The exact determinant law becomes

\[
\boxed{
\frac{(\log\Delta)'}{2\nu N^2}
=\gamma_2\,\mathrm{Re}_{spec}\,y
-\bigl(1+\theta+\chi+y^2\bigr).
}
\]

Hence positive determinant growth requires

\[
\boxed{
\gamma_2\,\mathrm{Re}_{spec}
>y+\frac{1+\theta+\chi}{y}
\ge2\sqrt{1+\theta+\chi}.
}
\]

The geometric opening `y` is simultaneously the lever that lets the Euler current leave the first two curl--Krylov directions and a square appearing in viscous killing.  Sending `y` to zero turns off nonlinear leverage; sending it to infinity makes the viscous term dominant.  The least expensive state is an interior balance.

For the critical stock, Section 20 gives independently

\[
\boxed{
\gamma_K\,\mathrm{Re}_{spec}\sqrt{\theta d}
>x+\frac1x,
\qquad
x=\sqrt{\frac{\eta}{1-\eta}}.
}
\]

Again too little opening (`x->0`) removes the radial geometry needed for critical growth, while too much (`x->infinity`) raises the cubic viscous moment.  Its optimal impedance is the interior value `2`.

This repeated structure suggests a native principle rather than two estimates:

> **The same geometric degree of freedom which opens a non-affine Euler response enters viscous exposure at the next power.  Nonlinear gain is linear in the opening; viscous impedance is convex in it.**

The statement above is theorem-level for `Delta` and `K`; a universal theorem for every observable is not claimed.  The repetition is nevertheless important because the two derivations use different native objects: Lanczos opening for `Delta`, radial moment log-convexity for `K`.

## 27. Adversarial guards: three tempting shortcuts are false

The reduction above was deliberately stress-tested against several attractive but non-native conclusions.

### Holonomy alone is not a three-dimensional tax

A search over genuine additive diamonds found noncoplanar states with normalized scalar triple volume as large as about `0.96` and gauge-invariant helical phase holonomy below `1e-9` for suitable helicity assignments.  Therefore

`genuinely 3D => uniformly positive phase holonomy`

is false.  The older phase-holonomy theorem remains correct on a supplied diamond with supplied holonomy; it must not be promoted to a universal 3D obstruction.

When all four diamond routes were additionally required to remain genuinely forward and moderately efficient, constrained optimization showed a different pattern: driving holonomy toward zero forced the normalized triple volume toward zero in the tested sign sector.  At holonomy about `2.8e-6`, the best sampled all-forward configuration with roughly `0.77` of the old single-edge productivity had triple volume about `1.1e-6`.  This is **exploratory evidence for a compatibility law**, not a theorem; no quantitative gap is used above.

### Pressure projection is not a universal nonlinear tax

The Helmholtz split in Section 23 is an ontology statement, not a lower-loss theorem.  A random helical parent-pair stress found the Leray/raw-source action fraction ranging from about `2.3e-5` to `0.999999999999`.  Thus pressure can remove almost all or almost none of one raw interaction depending on geometry.  No fixed pressure fraction is available for no-escape.

### The nonlinear fitness is not low Krylov rank

Actual dealiased Galerkin states put substantial Fisher action arbitrarily far down the finite curl--Lanczos chain.  The first productive coefficient `c2` was only about ten percent of action in the sampled states and large tails remained past level `24`.  Hence a closure `f approximately c2 p2+c3 p3` is false even for smooth moderate states.

### Reynolds above two is necessary, not sufficient

One actual state at `nu=0.002` had `Re_spec` about `2.62` while its critical stock still decreased.  Its alignment/radial factors reduced the live drive to about `0.28`, below the actual cubic viscous factor about `1.21`.  The scalar `Re_spec` is therefore only the outer concentration shell.  The true instantaneous object is the live product in Section 20.

These failures all point in the same direction: the missing theorem cannot be recovered by adding a static branch or a new scalar threshold.  It must constrain the **persistence of the coupled Lamb/fitness orientation**.

## 28. Current frontier: persistence of a concentrated productive Lamb score

The chain of exact reductions now reads

\[
\boxed{
L=u\times\omega
\longrightarrow
F_E=\mathbb P L
\longrightarrow
f(a)
\longrightarrow
\kappa
\longrightarrow
\{\dot\Delta,\dot K,\text{radial flux},\ldots\}.
}
\]

Every arrow is a projection, conditional score or functional-calculus readout; none creates a new physical owner.

The viscosity side is equally short:

\[
\boxed{-\nu C^2u\quad\Longleftrightarrow\quad -\nu a^2\text{ fitness}.}
\]

Consequently a hypothetical critical escape must maintain, at increasingly large curl scale, all of the following properties of **one and the same Lamb field**:

1. enough physical-space concentration to keep `V_spec` small and `Re_spec` large;
2. enough non-Beltrami/curl-spread geometry to make the desired tangent direction exist;
3. enough sign--radius decorrelation for the critical kink `|a|` to be non-affine;
4. enough alignment of the high-dimensional Euler fitness with the productive tangent coordinate rather than reconfiguration coordinates;
5. enough temporal persistence to repeat this after the state has changed under the current itself and under quadratic viscous selection.

The first four are now measured by exact dimensionless factors.  The fifth is the genuinely unresolved piece.  Neither a static spectral functional, an isolated-triad estimate, a phase-holonomy threshold nor a packet taxonomy can supply it.

The next research question is therefore narrow:

> **What intrinsic evolution law prevents a concentrated Lamb field from keeping its curl-spectral fitness aligned with the productive Nijenhuis direction for an infinite sequence of shrinking interaction volumes?**

A successful answer should act directly on the current/score dynamics.  If it instead reintroduces a list of source/strain/HH/reuse cases, it has moved upward in representation rather than downward toward the PDE mechanism.

## 29. One alternating Euler generator, but no Nambu--Poisson overclaim

The reductions above admit a shorter full-field formulation.  Put

\[
\mathcal E(u)=\frac12\|u\|_2^2,
\qquad
\mathcal H(u)=\frac12\langle u,Cu\rangle,
\qquad
\mathcal Z(u)=\frac12\|Cu\|_2^2.
\]

On divergence-free functional gradients define only the physical alternating triple form

\[
\mathfrak T(F,G,H)
=
\int \nabla F\cdot(\nabla G\times\nabla H)\,dx.
\]

Since

\[
\nabla\mathcal E=u,
\qquad
\nabla\mathcal H=Cu,
\qquad
\nabla\mathcal Z=C^2u,
\]

the Navier--Stokes evolution reads weakly, for every sufficiently smooth functional `F`,

\[
\boxed{
\frac d{dt}F(u)
=
\mathfrak T(F,\mathcal E,\mathcal H)
-\nu\langle\nabla F,\nabla\mathcal Z\rangle.
}
\]

Equivalently at the vector level,

\[
\boxed{u_t=J_uCu-\nu C^2u,\qquad J_uv=\mathbb P(u\times v).}
\]

Thus the conservative and dissipative pieces use the first three quadratic moments of one self-adjoint operator `C`: the Euler part is generated by the energy/helicity pair, while viscosity is the `L^2` gradient descent of enstrophy.

This note deliberately does **not** promote `mathfrak T` to a global Nambu--Poisson bracket.  A direct-sum finite-dimensional model of the same pointwise triple bracket violates the Nambu fundamental identity.  One exact polynomial counterexample on two three-dimensional blocks had

`F1=-2 e+b f`, `F2=-2 b d f`, `G1=2 c-b c+a`, `G2=2 d`, `G3=2 a`,

with fundamental-identity defect `32 d-16 b d`.  The alternating generator identity above is the physical theorem; the stronger formal label is not used.

## 30. The full PDE has a canonical curl-spectral three-current before Fourier-triad bookkeeping

Let `P_a` be the spectral projector of the self-adjoint curl operator and write

\[
u=\sum_a u_a,
\qquad
Cu_a=a u_a.
\]

On the mean-zero divergence-free range of `C` define

\[
\Omega(x,y,z)
=
\langle C^{-1}x,[y,z]\rangle
=-\int x\cdot(y\times z)\,dx.
\]

It is totally alternating.  For three ordered signed-curl nodes

\[
a<b<c
\]

define the canonical physical three-current

\[
\boxed{
\tau_{abc}:=-2\Omega(u_a,u_b,u_c)
=2\int u_a\cdot(u_b\times u_c)\,dx.
}
\]

Then the **individual** Euler energy rates contributed by that one spectral triple are

\[
\boxed{
(S_a,S_b,S_c)
=
\tau_{abc}(c-b,\ a-c,\ b-a).
}
\]

The two affine null laws are pointwise in this representation:

\[
S_a+S_b+S_c=0,
\qquad
aS_a+bS_b+cS_c=0.
\]

If `tau>0`, the median node is the donor and the donor mass is `tau(c-a)`.  The recipient fractions are exactly

\[
\frac{c-b}{c-a},
\qquad
\frac{b-a}{c-a},
\]

so the old barycentric martingale spread is not a special Waleffe fixture; it is the spectral-theorem decomposition of the full Euler PDE.

For every spectral observable `phi`, put

\[
D_\phi(a,b,c)
=
\det\!\begin{pmatrix}
1&1&1\\
a&b&c\\
\phi(a)&\phi(b)&\phi(c)
\end{pmatrix}.
\]

Then

\[
\boxed{
\left.\frac d{dt}\int\phi(a)\,d\rho(a)\right|_{NL}
=
\sum_{a<b<c}\tau_{abc}D_\phi(a,b,c).
}
\]

Since

\[
D_\phi(a,b,c)
=(b-a)(c-a)(c-b)\,\phi[a,b,c],
\]

the second-divided-difference law, the martingale law and the Nijenhuis/Vandermonde law are the same identity in three notations.

The curvature potential can therefore be reconstructed directly from the full spectral three-current:

\[
\boxed{
\kappa(\lambda)
=
\sum_{a<b<c}
\tau_{abc}(c-a)\Theta_{a,b,c}(\lambda),
}
\]

with the barycentric tent from the curl-curvature note.  A Fourier/helical triad decomposition is one microscopic disintegration of this current, not its ontology.

A direct dealiased Galerkin referee used `62` occupied signed-curl eigenspaces and `5,718` nonzero spectral triples.  Reconstructing **every node rate** from the displayed three-current gave relative `L^2` residual `7.48e-16`; the largest absolute node residual was `7.63e-17`.  The same current reproduced `phi=a^2`, `|a|`, `a^3` and `exp(0.07a)` responses with relative residuals from `3e-16` to `1.5e-14`.

## 31. Curl-spectral bundle equation: transfer and reorientation are two components of one cross product

Write each occupied spectral component in polar form

\[
u_a=\sqrt E\,q_a\,\widehat u_a,
\qquad
q_a\ge0,
\qquad
\|\widehat u_a\|_2=1,
\qquad
\sum_aq_a^2=1.
\]

Thus `q_a^2=E_a/E` is the physical energy fraction, not an analyst probability.  Define the alternating normalized overlap

\[
\boxed{
\chi_{abc}
=
\sqrt E\int\widehat u_a\cdot
(\widehat u_b\times\widehat u_c)\,dx.
}
\]

For `a<b<c` the Euler part of the Hellinger base velocity is

\[
\begin{aligned}
\dot q_a&=\chi_{abc}(c-b)q_bq_c,\\
\dot q_b&=\chi_{abc}(a-c)q_aq_c,\\
\dot q_c&=\chi_{abc}(b-a)q_aq_b.
\end{aligned}
\]

Summing all triples and adding viscosity gives the exact normalized NS base equation

\[
\boxed{
\dot q_a
=
\sum_{b<c}\chi_{abc}(c-b)q_bq_c
-\nu(a^2-N^2)q_a,
\qquad
N^2=\sum_a a^2q_a^2.
}
\]

Antisymmetry makes the Euler vector field tangent to both constraints automatically:

\[
\sum_aq_a\dot q_a^{NL}=0,
\qquad
\sum_a a q_a\dot q_a^{NL}=0.
\]

The viscous term is exactly the tangent gradient descent of `N^2` on the Hellinger unit sphere.

Now let

\[
F_a=P_aF_E,
\qquad F_E=\mathbb P(u\times Cu).
\]

The same pair interaction splits inside the output eigenspace as

\[
P_a(\widehat u_b\times\widehat u_c)
=
\left(\int\widehat u_a\cdot
(\widehat u_b\times\widehat u_c)\right)\widehat u_a
+W_{a;bc},
\qquad W_{a;bc}\perp\widehat u_a.
\]

The first component is the `q_a` energy motion above.  The second gives the exact fiber equation, for `q_a>0`,

\[
\boxed{
\dot{\widehat u}_a
=
\frac{\sqrt E}{q_a}
\sum_{b<c}(c-b)q_bq_c W_{a;bc}.
}
\]

Viscosity disappears from this direction equation because `-nu a^2 u_a` is purely radial inside each curl eigenspace.  Therefore **spectral transfer and isospectral reorientation are radial/tangential components of the same projected cross product**, not two physical owner types.

There is a global Pythagorean identity:

\[
\boxed{
\|F_E\|_2^2
=\mathcal A_{spec}+\mathcal A_{vert},
\qquad
\mathcal A_{vert}
=\sum_a E_a\|\dot{\widehat u}_a\|_2^2.
}
\]

On the actual Galerkin referee above,

`A_spec=0.4312582067`, `A_vert=5.7820820487`, `||F_E||^2=6.2133402554`,

with Pythagorean residual `2.9e-16`; the normalized fiber derivative computed from the full NS RHS agreed with the pure Euler perpendicular component to `3.1e-16`.  The complete coupled bundle `q` RHS agreed nodewise with the full NS state to relative residual `5.6e-16`.

### The interaction volume is a self-return norm of the first two curl-Krylov directions

Put

\[
q_0=\frac{u}{\sqrt E},
\qquad
q_1=\frac{(C-\lambda)u}{\sqrt B},
\qquad
\mathcal K_u=\overline{\{p(C)u\}}.
\]

The normalized Lamb vector is

\[
\ell_u
:=\mathbb P(q_0\times q_1)
=-C^{-1}[q_0,q_1].
\]

Since `K_u` is a reducing subspace for self-adjoint `C`,

\[
\boxed{
F_{spec}
=\sqrt{EB}\,P_{\mathcal K_u}\ell_u,
}
\]

and therefore

\[
\boxed{
V_{spec}^{-1}
=\|P_{\mathcal K_u}\ell_u\|_2^2,
\qquad
\mathrm{Re}_{spec}
=\frac{\sqrt E}{\nu N}
\|P_{\mathcal K_u}\ell_u\|_2.
}
\]

This is the operator meaning of the interaction volume: it is the inverse squared strength with which the Lie bracket of the first two intrinsic curl-Krylov directions returns to the state's own curl-cyclic subspace.

The same Galerkin state gave `V_spec=8.8311609051`; the operator projection and the old Fisher definition agreed to relative residual `4.1e-16`.

## 32. Jacobi completion: vertical motion is the closure defect of the reduced spectral Lie algebra

The helicity pairing

\[
B(x,y)=\langle C^{-1}x,y\rangle
\]

is ad-invariant on the mean-zero divergence-free Lie algebra.  Indeed total alternation of `Omega` gives

\[
B([x,y],z)+B(y,[x,z])=0.
\]

Consequently the Cartan-type three-form

\[
\Omega(x,y,z)=B(x,[y,z])
\]

is a closed Lie-algebra `3`-cocycle:

\[
\boxed{d\Omega=0.}
\]

A low-bandwidth full PDE referee evaluated all six terms of `dOmega` on four independent smooth divergence-free fields.  The terms were individually order `1`, while their sum was `-1.11e-16`, relative residual `2.87e-17`; the simultaneous Jacobi residual was `4.45e-16`.

In a fixed `L^2`-orthonormal curl eigenbasis `Ce_I=lambda_I e_I`, let

\[
f_{IJK}=\Omega(e_I,e_J,e_K).
\]

Then

\[
\langle e_I,[e_J,e_K]\rangle
=\lambda_I f_{IJK},
\]

and Jacobi gives the frequency-weighted quadratic compatibility

\[
\boxed{
\sum_M\lambda_M
\big(
 f_{MJK}f_{LIM}
+f_{MKI}f_{LJM}
+f_{MIJ}f_{LKM}
\big)=0.
}
\]

Thus overlapping triad couplings are **not independent coefficients**.  Their products satisfy a tetrahedral/Jacobi cancellation before any probabilistic or packet argument.

There is an equally direct reduced-space statement.  Let `P=P_Ku`, `Q=I-P`, and for `x,y in K_u` define

\[
[x,y]_K=P[x,y],
\qquad
R(x,y)=Q[x,y].
\]

Projecting the full Jacobi identity yields

\[
\boxed{
J_K(x,y,z)
=-P\big(
[x,R(y,z)]+[y,R(z,x)]+[z,R(x,y)]
\big).
}
\]

So the projected spectral bracket can fail Jacobi **only through the vertical closure defect which it itself generates**.  Moreover

\[
P\ell_u=-C^{-1}P[q_0,q_1],
\qquad
Q\ell_u=-C^{-1}Q[q_0,q_1].
\]

Horizontal transfer and vertical Jacobi completion are therefore the `C^{-1}`-orthogonal decomposition of one Lie bracket.

This gives an operator-level interpretation of cooperation/frustration.  It does **not** yet give an amplitude gap: `dOmega=0` is compatible with large concentrated triple forms.  A random real three-wave search even produced horizontal fractions near `0.885` in very low-activity states, so no universal positive vertical fraction is asserted.

## 33. Euler is Lax conjugation; in material coordinates NS vorticity is heat in the deformed metric

There is a second exact operator reduction.  For Euler vorticity

\[
\omega=Cu,
\qquad
\omega_t=[\omega,u].
\]

Since `ad` is a Lie-algebra representation,

\[
\boxed{
\partial_t\operatorname{ad}_\omega
=
[\operatorname{ad}_\omega,\operatorname{ad}_u].
}
\]

Thus, formally on any class where the flow-map pushforward is defined,

\[
\boxed{
\operatorname{ad}_{\omega(t)}
=
\Phi_{t*}\operatorname{ad}_{\omega(0)}\Phi_{t*}^{-1}.
}
\]

The Euler part is a similarity/conjugation flow of the vorticity action.  Norm amplification can still occur because the volume-preserving pushforward on vector fields is not `L^2` unitary; the law is therefore an isospectral/non-normal statement, not a regularity proof.

With viscosity,

\[
\partial_t\operatorname{ad}_\omega
=
[\operatorname{ad}_\omega,\operatorname{ad}_u]
-\nu\operatorname{ad}_{C^2\omega}.
\]

The same geometry becomes especially transparent for the vorticity two-form `varpi=d u^flat`.  Use the Euclidean Laplacian convention with Fourier symbol `-|k|^2`.  Navier--Stokes gives

\[
(\partial_t+\mathcal L_u)\varpi
=\nu\Delta_{g_0}\varpi.
\]

Let `Phi_t` be the material flow and set

\[
\widetilde\varpi=\Phi_t^*\varpi,
\qquad
g_t=\Phi_t^*g_0.
\]

Naturality of the Hodge/component Laplacian under pullback gives the exact material equation

\[
\boxed{
\partial_t\widetilde\varpi
=\nu\Delta_{g_t}\widetilde\varpi.
}
\]

There is no explicit vortex-stretching source left: stretching has become the time dependence of the material metric.  Its evolution is

\[
\boxed{
\partial_tg_t
=\Phi_t^*(\mathcal L_u g_0)
=2\Phi_t^*S,
\qquad
\det g_t=1.
}
\]

So the same strain which amplifies physical vorticity is exactly the metric velocity seen by material diffusion.  In this representation the nonlinear feedback is

\[
\boxed{
\widetilde\varpi
\longrightarrow u
\longrightarrow g_t
\longrightarrow \Delta_{g_t}
\longrightarrow \widetilde\varpi,
}
\]

not a source/strain/HH taxonomy.

This law still does not close regularity.  A volume-preserving pullback metric can become highly anisotropic; its fixed-coordinate ellipticity may deteriorate in the stretched direction even while reciprocal compression strengthens diffusion transversely.  The missing estimate must exploit the fact that `g_t` is not arbitrary: it is generated by the same vorticity/Lie current being diffused.

## 34. The no-escape frontier is now one persistence integral

The energy equation supplies its own parabolic clock:

\[
\boxed{
-\frac{\dot E}{E}=2\nu N^2,
\qquad
\int_{t_0}^{t_1}N^2dt
=\frac1{2\nu}\log\frac{E(t_0)}{E(t_1)}
}
\]

on every interval with positive endpoint energies.  No event count or analysis clock is needed.

The exact critical balance from Section 20 also gives

\[
\boxed{
\frac{1}{2\nu N^2}\frac d{dt}\log K
=
\frac{
\gamma\,\mathrm{Re}_{spec}\sqrt{\theta\eta d}-\mu_3
}{\sqrt{1-\eta}}.
}
\]

Hence a critical escape over a finite positive-energy interval cannot come merely from having `Re_spec>2` at many isolated times.  It must make the **dimensionless productive self-return factor** on the right accumulate without control on an energy clock of finite total length.

All pieces of that factor except self-return persistence are now exact state geometry:

- `theta`: non-Beltrami opening;
- `eta`: radial opening;
- `d`: sign--radius non-affinity;
- `gamma`: alignment with the productive tangent direction;
- `mu3`: convex cubic viscous impedance;
- `Re_spec`: the self-return norm `sqrt(E)||P_K ell_u||/(nu N)`.

The remaining question is therefore no longer “which owner occurs next?”  It is

\[
\boxed{
\text{Can the Lie/Lamb self-return }
P_{\mathcal K_u}C^{-1}[q_0,q_1]
\text{ remain productively aligned while its interaction volume collapses,}
}
\]

\[
\boxed{
\text{despite Jacobi/Cartan compatibility, vertical closure motion, and the fact that the same strain deforms the material heat metric?}
}
\]

A proof that the positive part of this normalized persistence integral is finite would be a genuine no-escape theorem.  No such estimate is claimed here.  The point of the reduction is that the unresolved mechanism is now one operator-level persistence problem rather than a list of PDE representations.

## 35. One master commutator generates every curl-spectral observable

The preceding spectral laws have a single operator parent.  For any real Borel/smooth functional-calculus observable `phi(C)` for which the pairings exist, put

\[
M_\phi(u)=\langle u,\phi(C)u\rangle.
\]

Since `J_u^*=-J_u` and `J_u u=0`, the Euler contribution is

\[
\begin{aligned}
\dot M_\phi^{NL}
&=2\langle\phi(C)u,J_uCu\rangle\\
&=2\langle u,\phi(C)J_uCu\rangle\\
&=\boxed{2\langle u,[\phi(C),J_u]Cu\rangle}.
\end{aligned}
\]

Thus **every non-affine curl observable is a commutator response of the same Lamb operator**.  In spectral projectors,

\[
\boxed{
P_a[\phi(C),J_u]P_b
=(\phi(a)-\phi(b))P_aJ_uP_b.
}
\]

For `a!=b`, this factors through the one primitive curl commutator:

\[
\boxed{
P_a[\phi(C),J_u]P_b
=\phi[a,b]\,P_a[C,J_u]P_b,
\qquad
\phi[a,b]=\frac{\phi(a)-\phi(b)}{a-b}.
}
\]

The second-divided-difference/Nijenhuis three-frequency law appears when the same identity is closed around one alternating spectral triple.  Hence the hierarchy

`energy/helicity -> enstrophy -> critical |C| -> arbitrary phi(C)`

is not a hierarchy of mechanisms.  It is functional calculus applied to one failure of the Euler Lamb operator to commute with curl.

For the critical observable `phi(a)=|a|`, write

\[
\Lambda=|C|,
\qquad
\mathsf J=\operatorname{sgn}(C)=C\Lambda^{-1}
\]

on the mean-zero divergence-free range.  Since `Lambda` and the Leray projector commute,

\[
[J_u,\mathsf J]
=\mathbb P[M_{u\times},\mathsf J]
\]

on divergence-free inputs.  The commutator therefore has the physical Calderon form: its kernel contains the velocity increment `u(x)-u(y)`.  A constant velocity commutes exactly with `mathsf J`.  Uniform sweeping is consequently absent from the critical-growth operator before any estimate is taken.

## 36. Critical NS is a damped Krein boost, not a list of helicity cases

Use the positive critical coordinate

\[
\boxed{y=\Lambda^{1/2}u.}
\]

Then

\[
K=\|y\|_2^2,
\qquad
H=\langle y,\mathsf Jy\rangle,
\qquad
\mathsf J^*=\mathsf J,
\qquad
\mathsf J^2=I.
\]

The Euler generator on this coordinate is

\[
L_u
=\Lambda^{1/2}J_u\mathsf J\Lambda^{1/2}.
\]

A direct adjoint calculation gives the exact Krein-skew identity

\[
\boxed{L_u^*\mathsf J+\mathsf J L_u=0.}
\]

Decompose it into its ordinary skew and symmetric parts:

\[
\boxed{
A_u=\frac12\Lambda^{1/2}\{J_u,\mathsf J\}\Lambda^{1/2},
\qquad
S_u=\frac12\Lambda^{1/2}[J_u,\mathsf J]\Lambda^{1/2}.
}
\]

Then

\[
A_u^*=-A_u,
\qquad
[A_u,\mathsf J]=0,
\]

while

\[
S_u^*=S_u,
\qquad
\{S_u,\mathsf J\}=0.
\]

So `A_u` rotates inside the two helicity-sign sectors and cannot change the positive critical norm.  `S_u` is purely off-diagonal between them and is the **only** Euler operator which can grow `K`.

Full Navier--Stokes in the critical coordinate is therefore

\[
\boxed{
y_t=A_uy+S_uy-\nu\Lambda^2y.
}
\]

All three pieces have distinct algebraic signatures:

- `A_u`: skew-adjoint, helicity-sign even;
- `S_u`: self-adjoint, helicity-sign odd;
- `-nu Lambda^2`: self-adjoint negative, helicity-sign even.

The critical curvature height is exactly the Rayleigh numerator of the boost:

\[
\boxed{
\kappa(0)=\langle y,S_uy\rangle,
\qquad
\dot K^{NL}=2\langle y,S_uy\rangle=2\kappa(0).
}
\]

Let `y_+` and `y_-` be the two spectral sign projections and put `K_+=||y_+||^2`, `K_-=||y_-||^2`.  Since `S_u` is off-diagonal,

\[
\boxed{
\dot K_+^{NL}=\dot K_-^{NL}=\kappa(0),
}
\]

which is precisely the earlier opposite-helicity pair law.  It is not an additional phenomenon: it is the sector form of a Krein boost preserving the indefinite Euler metric

\[
H=K_+-K_-.
\]

Viscosity adds

\[
\dot K_+|_\nu=-2\nu\|\Lambda y_+\|_2^2,
\qquad
\dot K_-|_\nu=-2\nu\|\Lambda y_-\|_2^2.
\]

Thus the critical problem has collapsed to **one hyperbolic boost competing with one quadratic positive operator**.

## 37. The productive score is just normalized curvature height, and radial geometry is a forced-gradient law

Let

\[
p_a=E_a/E,
\qquad
r_a=|a|,
\qquad
m=\mathbb E_p r=K/E,
\qquad
N^2=\mathbb E_p r^2,
\qquad
\sigma^2=\operatorname{Var}_p(r).
\]

On occupied levels the Euler fitness is `f_a=S_a/(2E_a)`.  Since `int f dp=0`,

\[
\dot m^{NL}=2\operatorname{Cov}_p(r,f).
\]

Define the **single productive score**

\[
\boxed{
R_*
=\frac{\operatorname{Cov}_p(r,f)}{\nu N^2\sigma}.
}
\]

Using `dot K_NL=2 kappa(0)` and

\[
\Delta_{rad}=EZ-K^2=E^2\sigma^2,
\]

this is exactly

\[
\boxed{
R_*
=\frac{\kappa(0)}{\nu N^2\sqrt{EZ-K^2}}.
}
\]

Thus the earlier product

`critical alignment * Re_spec * sqrt(defect fraction * decorrelation)`

was only a factorization of one normalized curvature height.

The normalized spectral law gives an even shorter critical-scale balance.  Put

\[
x=\sigma/m,
\qquad
\mathcal I_r
=\frac{\operatorname{Cov}_p(r,r^2)}{N^2m}.
\]

On the physical energy-loss clock

\[
d\tau_E=-d\log E=2\nu N^2dt,
\]

one has **exactly**

\[
\boxed{
\frac d{d\tau_E}\log\frac KE
=R_*x-\mathcal I_r.
}
\]

The sole viscous moment inequality is

\[
(\mathbb E r^2)^2\le(\mathbb E r)(\mathbb E r^3),
\]

which is equivalent to

\[
\boxed{\mathcal I_r\ge x^2.}
\]

Hence no radial case split remains:

\[
\boxed{
\frac d{d\tau_E}\log\frac KE
\le R_*x-x^2
\le\frac{R_*^2}{4}.
}
\]

The exact defect from the optimized upper is itself positive geometry:

\[
\frac{R_*^2}{4}
-\frac d{d\tau_E}\log\frac KE
=
\left(x-\frac{R_*}{2}\right)^2
+\left(\mathcal I_r-x^2\right),
\]

with

\[
\boxed{
\mathcal I_r-x^2
=\frac{m\,\mathbb E r^3-N^4}{N^2m^2}
=\frac{
\sum_{i<j}p_ip_j r_ir_j(r_i-r_j)^2
}{N^2m^2}.
}
\]

So approaching the upper bound forces two simultaneous rigidities: the radial coefficient of variation must track `R_*/2`, and the positive radial law must approach a two-level `0/R` geometry.  This observation is important because the next theorem shows that the zero-radius limit is exactly where critical helicity boost becomes Galilean transport and loses strength.

Integrating gives the intrinsic conditional no-escape estimate

\[
\boxed{
\log\frac{(K/E)(t)}{(K/E)(t_0)}
\le
\frac14\int_{\tau_E(t_0)}^{\tau_E(t)}R_*^2\,d\tau_E.
}
\]

Therefore critical mean-curl escape requires the normalized curvature height `R_*` to fail `L^2` on the PDE's own finite energy-loss clock.  This is a current-persistence criterion, not an event count.

## 38. Sharp Galilean null law for the critical helicity-flip boost

The commutator form of `S_u` supplies the missing behavior at the low-radius edge without invoking a cutoff.  Consider one Fourier triangle

\[
q+\ell-k=0
\]

and a unit helical velocity mode at radius `q`.  Let the critical input at `ell` have helicity `s` and the output at `k` have helicity `-s`.  Same-sign input/output has zero matrix element in `S_u` because `S_u` anticommutes with `mathsf J`.

For the helicity-flip matrix element, cyclic invariance of the Waleffe triple product gives

\[
\boxed{
|\langle h_{k,-s},S_{u_q}h_{\ell,s}\rangle|
=2\sqrt{|k||\ell|}\,|g(q,\ell,-k)|\,|a_q|
}
\]

for one unit-normalized helical coefficient `a_q` of the advecting velocity.

Let

\[
m=\frac{|k|+|\ell|}{|q|}>1,
\qquad
x=\frac{|k|-|\ell|}{|q|},
\qquad |x|<1.
\]

The positive sum/difference form of Heron is

\[
A_\triangle
=\frac{|q|^2}{4}
\sqrt{(m^2-1)(1-x^2)}.
\]

Because the output/input helicities are opposite, maximizing over the helicity of the advecting mode gives the signed-frequency factor

\[
|q|+\big||k|-|\ell|\big|
=|q|(1+|x|).
\]

Therefore

\[
\frac{2\sqrt{|k||\ell|}|g|}{|q|}
=\frac{1+|x|}{2\sqrt2}
\sqrt{
\frac{(m^2-1)(1-x^2)}{m^2-x^2}
}.
\]

For fixed `x` the last square-root increases with `m` and tends to `sqrt(1-x^2)`.  The remaining one-variable maximization

\[
(1+x)\sqrt{1-x^2},
\qquad 0\le x<1,
\]

has its unique maximum at `x=1/2`.  Hence the sharp supremum is

\[
\boxed{
2\sqrt{|k||\ell|}|g(q,\ell,-k)|
\le
\frac{3\sqrt6}{16}|q|.
}
\]

The constant is approached by `m->infinity`, `|x|->1/2`; it is not obtained by a finite collinear triangle.  A `500,000`-triangle stress saw no violation, and direct comparison against the repository closed-form Waleffe coupling on another `8,000` random physical triangles passed the same bound.

This is an exact **Galilean derivative law**.  No matter how large the target curl radius becomes, one fixed large-scale mode can feed the critical helicity-changing boost only through its gradient scale `|q||a_q|`.  A uniform mode (`q=0`) is pure sweeping and commutes with the helicity involution exactly.

Thus the radial near-extremizer and the nonlinear driver pull in opposite directions:

- radial viscous impedance is cheapest when one participating radius is driven toward zero;
- the critical boost produced by that radius simultaneously loses strength linearly and becomes transport symmetry.

This does not by itself close the full many-mode problem: arbitrarily many strain-producing modes can cooperate.  But it removes the possibility of an indefinitely reusable **fixed low-frequency catalyst**.  Any escape which keeps the boost comparable to `nu N^2` must make the strain-producing spectrum itself climb or concentrate.  That is a continuous operator statement, not a new low/high owner branch.

## 39. Critical viscosity and the absolute-curl companion complete one exact Hilbert square

There is a second exact critical balance which does not pass through radial moments.  Let

\[
\Lambda=|C|,
\qquad
R_u=\mathbb P(u\times\Lambda u).
\]

`R_u` is an intrinsic **dual/companion field**, not a new physical Euler force: it is obtained by inserting the gradient of the actual critical observable `K/2` in place of the helicity gradient in the same Lamb operator.  The physical critical source satisfies

\[
\begin{aligned}
\kappa(0)
&=\langle\Lambda u,u\times\omega\rangle\\
&=-\langle\omega,R_u\rangle.
\end{aligned}
\]

Put

\[
A=\Lambda^{1/2}\omega,
\qquad
B=\Lambda^{-1/2}R_u.
\]

Then

\[
\|A\|_2^2=M_3,
\qquad
\langle A,B\rangle=-\kappa(0).
\]

The full critical balance therefore has the exact completed-square form

\[
\boxed{
\dot K
=-2\nu\left\|A+\frac{B}{2\nu}\right\|_2^2
+\frac{1}{2\nu}\|B\|_2^2.
}
\]

Equivalently,

\[
\boxed{
\dot K
=-2\nu\left\|
\Lambda^{1/2}\omega
+\frac{1}{2\nu}\Lambda^{-1/2}\mathbb P(u\times\Lambda u)
\right\|_2^2
+\frac1{2\nu}\left\|
\Lambda^{-1/2}\mathbb P(u\times\Lambda u)
\right\|_2^2.
}
\]

This is an identity, not Young's inequality inserted after the fact.  The positive upper remainder is scale-critical in spacetime.  In particular

\[
\dot K>0
\quad\Longrightarrow\quad
\|B\|_2>\nu\|A\|_2,
\]

but the converse is false because the Hilbert angle matters.  On the direct Galerkin referee,

`||B||/(nu||A||) ~= 9.57`

while `K' ~= -0.0291`; the square identity still matched the direct PDE rate to `1.1e-15`.  Thus `||B||/(nu||A||)` is only a necessary activity threshold, never a causal or sufficient criterion.

The same identity has a two-point interpretation.  For the positive periodic kernel of `Lambda`, let `D omega(x,y)=omega(x)-omega(y)` and `A_u(x,y)=u(x) x u(y)`.  The fractional Dirichlet form gives

\[
\kappa(0)=\langle A_u,D\omega\rangle_{pair},
\qquad
M_3=\|D\omega\|_{pair}^2.
\]

Only the exact/coboundary component of the antisymmetric pair-area field can pair with `D omega`; its cycle component is critical-work-invisible.  The triangle circulation

\[
A_u(x,y)+A_u(y,z)+A_u(z,x)
=(u(x)-u(z))\times(u(y)-u(z))
\]

is a Galilean-invariant velocity-triangle area.  Thus the physical-space version of the same law again contains an intrinsic frustration: arbitrary pairwise velocity areas cannot all be realized as one vorticity increment field.  This cochain interpretation is structural; no quantitative cycle-gap sufficient for global regularity is claimed here.

## 40. The smallest fixed-basis equation: one Cartan tensor plus diagonal heat

Choose once and for all a real orthonormal curl eigenbasis

\[
Ce_I=\lambda_Ie_I
\]

and write

\[
u=\sum_I z_Ie_I.
\]

The time-independent structure tensor is

\[
\boxed{f_{IJK}=\Omega(e_I,e_J,e_K),}
\]

which is totally alternating and, for the physical divergence-free Lie algebra, satisfies the weighted Jacobi identities from Section 32.

The entire Euler vector field is then

\[
\boxed{
\dot z_I^{NL}
=-\frac12\sum_{J,K}
(\lambda_K-\lambda_J)f_{IJK}z_Jz_K.
}
\]

Equivalently, for one stored ordered index triple `I<J<K`, its contributions are

\[
\begin{aligned}
\dot z_I&=-(\lambda_K-\lambda_J)f_{IJK}z_Jz_K,\\
\dot z_J&=+(\lambda_K-\lambda_I)f_{IJK}z_Iz_K,\\
\dot z_K&=-(\lambda_J-\lambda_I)f_{IJK}z_Iz_J.
\end{aligned}
\]

Navier--Stokes adds only

\[
\boxed{-\nu\lambda_I^2z_I.}
\]

This is arguably the shortest finite/Galerkin grammar reached in this programme:

\[
\boxed{
\text{fixed alternating Cartan tensor}
+\text{signed-curl gaps}
+\text{diagonal quadratic heat}.
}
\]

All phases, Waleffe edges, spectral curvature, martingale spreads, horizontal transfer and vertical reorientation are contractions/projections of this fixed tensor equation.  The tensor does **not** evolve with the state.

Antisymmetry gives the two Euler quadratic null laws immediately.  It also gives a Liouville property: the Euler component of `dot z_I` contains no self-coordinate `z_I`, hence

\[
\boxed{\operatorname{div}_zF_E=0.}
\]

For a finite Galerkin system,

\[
\boxed{
\operatorname{div}_zF_{NS}
=-\nu\sum_I\lambda_I^2,
}
\]

with the real-coordinate multiplicities understood.  This uniform phase-volume contraction is not a bound on an individual trajectory and is **not** used as a regularity argument.  Its role here is ontological: the many-triad network is not a dynamically changing rulebook; it is one fixed Lie/Cartan interaction tensor seen through a dissipative diagonal metric.

## 41. Two final falsification guards on the persistence frontier

Two attractive simplifications were explicitly rejected while developing the preceding laws.

First, the higher curl-Krylov Christoffel kernels are not universally convex.  Already at level `n=2`, a positive eight-node spectral state produced `K_n''=-0.0482` at one interior signed-curl point.  Therefore martingale spread does **not** monotonically increase every higher Krylov volume.  The hierarchy remains an exact observable family, not a tower of monotone Lyapunov functions.

Second, the normalized curvature score

\[
R_*=\frac{\kappa(0)}{\nu N^2\sqrt{EZ-K^2}}
\]

is not itself monotone.  One deterministic smooth Galerkin orbit had negative `R_*` decreasing throughout the sampled interval, while a separate adversarial set of `30` normalized smooth random states had positive initial derivatives in all `30` trials, with the largest measured derivative about `20.9` in the same physical-time normalization.  This is falsification evidence against a universal scalar monotonicity claim, not a statistical theorem.

The remaining no-escape task is therefore genuinely dynamical:

\[
\boxed{
\text{control the persistence/action of the productive Krein self-return boost,}
}
\]

not its instantaneous sign.  Any successful theorem must use the constraints which survive every falsification above: fixed Cartan/Jacobi compatibility, Galilean commutator null structure, vertical closure motion, the finite energy-loss clock, and the fact that the same strain deforms the material heat metric.

## 42. The remaining escape cost is one productive Fisher action

The normalized curvature score has a final regression interpretation which removes all intermediate factor names.  On the physical curl-energy law

\[
p_a=E_a/E
\]

let

\[
f_a=\frac{S_a}{2E_a}
\]

be the actual Euler spectral fitness on occupied levels and `r_a=|a|`.  Energy conservation gives `E_p f=0`.  The critical curvature height is

\[
\boxed{
\kappa(0)=E\operatorname{Cov}_p(r,f).
}
\]

Since

\[
EZ-K^2=E^2\operatorname{Var}_p(r),
\]

define the **productive Fisher action**

\[
\boxed{
\mathcal A_{prod}
:=\frac{\kappa(0)^2}{EZ-K^2}
=\frac{\operatorname{Cov}_p(r,f)^2}{\operatorname{Var}_p(r)}.
}
\]

This is exactly the squared norm of the one-dimensional regression component of Euler fitness which points along absolute curl radius.  The full Euler spectral Fisher action per unit energy is

\[
\operatorname{Var}_p(f)=\mathcal A_{spec}/E,
\]

so ordinary Cauchy gives

\[
\boxed{
0\le\mathcal A_{prod}\le\mathcal A_{spec}/E.
}
\]

The quotient

\[
\frac{\mathcal A_{prod}}{\mathcal A_{spec}/E}
=\operatorname{Corr}_p(r,f)^2
\]

when both variances are nonzero is the literal fraction of spectral Fisher activity which is productive for changing the mean absolute curl scale.  All orthogonal spectral activity can be dynamically important for future reorientation, but it does not instantaneously increase `K/E`.

The forced-gradient law of Section 37 now becomes

\[
\boxed{
\frac d{dt}\log\frac KE
\le
\frac{\mathcal A_{prod}}{2\nu N^2}.
}
\]

Equivalently,

\[
\boxed{
\log\frac{(K/E)(t)}{(K/E)(t_0)}
\le
\frac1{2\nu}
\int_{t_0}^t
\frac{\kappa(0,s)^2}
{N(s)^2\,[E(s)Z(s)-K(s)^2]}\,ds.
}
\]

This gives a precise necessary condition for critical mean-curl escape:

\[
\boxed{
K/E\to\infty
\quad\Longrightarrow\quad
\int^T
\frac{\kappa(0,t)^2}
{N(t)^2\,[E(t)Z(t)-K(t)^2]}\,dt
=\infty,
}
\]

with the quotient read by continuous tangent limit on exact radial-affine boundary states.  In energy-clock form it is simply

\[
\boxed{
4\,d\log(K/E)\le R_*^2\,d\tau_E.
}
\]

So every logarithmic unit of critical-scale growth carries a minimum productive-action cost.  The unresolved global theorem is no longer to classify how growth occurs; it is to prove that the actual Cartan/Krein/Navier--Stokes dynamics cannot accumulate infinite productive Fisher action on a finite positive-energy interval.

That statement is intentionally the endpoint of this note.  Any further decomposition should be accepted only if it derives a bound on this one action, not merely because it supplies another correct representation of the same current.

## 43. Orthogonal Cartan motion plus `H^{-1}_curl` energy-gradient heat

There is a shorter metric form of the full equation which is useful precisely because it does **not** manufacture a new owner.  On mean-zero divergence-free fields put

\[
\langle v,w\rangle_{-1,C}
:=\langle C^{-1}v,C^{-1}w\rangle_{L^2}.
\]

For the kinetic energy functional

\[
\mathcal E(u)=\frac12\|u\|_2^2
\]

the gradient in this metric is exactly

\[
\boxed{\nabla_{-1,C}\mathcal E=C^2u.}
\]

The Euler velocity is

\[
X_E(u)=J_uCu,
\qquad J_uv=\mathbb P(u\times v),
\]

and energy conservation is the exact metric orthogonality

\[
\boxed{
\langle X_E,C^2u\rangle_{-1,C}
=\langle X_E,u\rangle_2=0.
}
\]

Hence Navier--Stokes is

\[
\boxed{
u_t=X_E(u)-\nu\nabla_{-1,C}\mathcal E,}
\]

with the Pythagorean speed law

\[
\boxed{
\|u_t\|_{-1,C}^2
=\|X_E\|_{-1,C}^2+\nu^2\|Cu\|_2^2.
}
\]

At the same time

\[
\boxed{\mathcal E'=-\nu\|Cu\|_2^2.}
\]

This is not an unrestricted metriplectic/Nambu claim.  It says only what the PDE says exactly: the fixed Cartan/Lie nonlinear motion is tangent to the energy level set, while the quadratic heat is the metric gradient direction.  The two do not cancel in the energy-gradient direction.

There is an equally important negative statement.  Pure viscosity acts on each curl eigenspace as

\[
u_a'=-\nu a^2u_a.
\]

Therefore the normalized eigenspace direction `n_a=u_a/||u_a||` has zero viscous derivative.  For a normalized triple orientation

\[
\chi_{abc}=\Omega(n_a,n_b,n_c)
\]

one has

\[
\boxed{\left.\dot\chi_{abc}\right|_\nu=0.}
\]

Heat damps the amplitudes which support a Cartan current; it does **not** directly dephase the normalized triple geometry.  Any no-escape argument which assumes automatic viscous phase randomization is therefore using a fictitious mechanism.

## 44. The master Sobolev strain-transfer law

The critical sech law is not an isolated trick.  It is the midpoint of one operator identity governing the whole Sobolev interval between energy and enstrophy.

Write

\[
\Lambda=|C|,
\qquad
\mathsf J=\operatorname{sgn}C,
\qquad
A_u:=\mathbb P\circ S(u).
\]

A direct vector-calculus identity gives, on divergence-free test fields,

\[
\boxed{[C,J_u]=2A_u.}
\]

Indeed

\[
C J_uv=\nabla\times(u\times v)
=(v\cdot\nabla)u-(u\cdot\nabla)v,
\]

while after Leray projection

\[
J_uCv
=-\mathbb P\big((\nabla u)^Tv+(u\cdot\nabla)v\big),
\]

and subtraction leaves `2 P(Sv)`.  Thus the master curl commutator is exactly the physical strain operator, not merely a quadratic-form analogue.

For

\[
y_s=\Lambda^su,
\qquad
G_s=\Lambda^sJ_uC\Lambda^{-s},
\]

the nonlinear `H^s` growth is generated by `Sym G_s`.  Split the strain operator by the helicity involution,

\[
A_{\rm even}=\frac12(A_u+\mathsf J A_u\mathsf J),
\qquad
A_{\rm odd}=\frac12(A_u-\mathsf J A_u\mathsf J),
\]

and put

\[
D=\operatorname{ad}_{\log\Lambda}.
\]

Then spectral functional calculus gives the exact operator law

\[
\boxed{
\operatorname{Sym}G_s
=
\frac{\sinh((s-\tfrac12)D)}{\sinh(D/2)}A_{\rm even}
-
\frac{\cosh((s-\tfrac12)D)}{\cosh(D/2)}A_{\rm odd}.
}
\]

The first quotient is read by its removable limit `2s-1` at equal radii.  On a curl block with output radius `r`, input radius `q`, the formula is elementary: same-helicity strain is multiplied by

\[
\frac{\sinh((s-\tfrac12)\log(r/q))}
{\sinh(\tfrac12\log(r/q))},
\]

whereas opposite-helicity strain is multiplied by

\[
-\frac{\cosh((s-\tfrac12)\log(r/q))}
{\cosh(\tfrac12\log(r/q))}.
\]

Three distinguished exponents are therefore not separate mechanisms:

\[
\boxed{\operatorname{Sym}G_0=-A_u,}
\]

\[
\boxed{\operatorname{Sym}G_1=\mathsf J A_u\mathsf J,}
\]

and at the unique midpoint

\[
\boxed{
\operatorname{Sym}G_{1/2}
=-\operatorname{sech}(D/2)A_{\rm odd}.
}
\]

This is a structural explanation for the distinguished role of `H^{1/2}` which is stronger than dimensional scaling alone.  At `s=1/2` **every helicity-preserving strain block disappears exactly**, while the helicity-flipping part is continuously suppressed by log-scale separation.  Energy, critical growth and enstrophy are three metric readings of one physical strain operator.

Random finite spectral matrices with dimensions through `50` and log-radius spans of twelve orders of magnitude reproduce the displayed law to relative residuals around `2e-16`; this is a convention referee, not the proof.

## 45. Critical growth is a sech/Poisson average of physical odd strain

At the midpoint the kernel between positive radii `r,q` is

\[
\boxed{
\operatorname{sech}\!\left(\frac12\log\frac rq\right)
=
\frac{2\sqrt{rq}}{r+q}.
}
\]

Equivalently, if

\[
\mathcal L_\Lambda(X)=\Lambda X+X\Lambda,
\]

then

\[
[J_u,\mathsf J]
=-2\mathcal L_\Lambda^{-1}\Pi_{\rm odd}[C,J_u],
\]

and hence the critical self-adjoint boost is exactly

\[
\boxed{
\Sigma_c
:=\operatorname{Sym}G_{1/2}
=-2\Lambda^{1/2}
\int_0^\infty
 e^{-t\Lambda}A_{\rm odd}e^{-t\Lambda}\,dt
\,\Lambda^{1/2}.
}
\]

The same kernel also has the unitary log-scale representation

\[
\boxed{
\operatorname{sech}(D/2)A
=
\int_{\mathbb R}
\operatorname{sech}(\pi\tau)
\Lambda^{i\tau}A\Lambda^{-i\tau}\,d\tau.
}
\]

Since `sech(pi tau) d tau` is a probability measure, the critical map is a contraction in operator norm and, more generally, in every unitarily invariant norm for which the finite-dimensional approximation is legitimate.  It is not a hard shell selection.

There is also a collective separated-block law.  If one curl-radius block lies below `R` and the other above `exp(L)R`, Neumann expansion of the Sylvester inverse gives

\[
\boxed{
\|P_{\le R}\Sigma_cP_{\ge e^LR}\|
\le
\min\!\left\{1,\operatorname{csch}\frac L2\right\}
\|P_{\le R}A_{\rm odd}P_{\ge e^LR}\|.
}
\]

Thus deep signed-curl separation is exponentially suppressed at the **operator block** level.  Combined with the independent sharp Galilean null law for a low advecting Fourier mode, strong critical growth has an intrinsic tri-locality: the two critical helicity radii cannot be widely separated without the sech tax, and an almost-uniform advecting mode loses its boost linearly in its own wave number.  No signed-good threshold is needed to state either fact.

The loophole is also explicit.  Many strain-producing modes can still cooperate through physical concentration; no universal collective low-mode `ell^1` bound is asserted.

### A canonical Poisson scale supplied by the state

Put

\[
w_t=e^{-t\Lambda}\Lambda u,
\qquad
K=\langle u,\Lambda u\rangle.
\]

Then

\[
\boxed{
d\mu_u(t)=\frac{2\|w_t\|_2^2}{K}\,dt
}
\]

is a probability measure because

\[
2\int_0^\infty\|e^{-t\Lambda}\Lambda u\|_2^2dt=K.
\]

If `m=K/E`, its mean scale is exactly

\[
\boxed{
\mathbb E_{\mu_u}t=\frac{E}{2K}=\frac1{2m}.
}
\]

Equivalently, choose a curl radius `r` with critical weight `rE_r/K`, then choose an exponential scale of rate `2r`.  This is a cutoff-free scale law generated by the actual state.

The critical boost Rayleigh rate is the corresponding strain average:

\[
\boxed{
\frac{\kappa(0)}K
=-\int_0^\infty
\frac{\langle w_t,A_{\rm odd}w_t\rangle}{\|w_t\|_2^2}
\,d\mu_u(t).
}
\]

So `kappa(0)/K` is not an abstract alignment coefficient.  It is the state-weighted average of physical odd-strain Rayleigh quotients over an intrinsic Poisson scale whose mean is exactly half the inverse mean curl.

## 46. The Poisson filter is subordinate to the same heat operator used by viscosity

The preceding scale law is tied to viscosity at the operator level.  With `Lambda^2=C^2`, the standard subordination identity is

\[
\boxed{
 e^{-t\Lambda}
=
\frac{t}{2\sqrt\pi}
\int_0^\infty
s^{-3/2}e^{-t^2/(4s)}e^{-sC^2}\,ds.
}
\]

The scalar identity was checked numerically over widely separated parameters to about `3e-15`; spectral calculus is the theorem.

Thus the critical Euler boost is a mixture of **heat-smoothed physical odd strain operators generated by the very same `C^2` which appears in Navier--Stokes viscosity**.  This is a genuine collapse of two pieces of the old architecture:

- the nonlinear critical scale filter is generated by `|C|`;
- `|C|` is the square root of the viscous generator `C^2`;
- Poisson smoothing is subordinated to quadratic heat.

No actual future heat evolution is being substituted for the Navier--Stokes trajectory here.  The statement is functional calculus at one time.  Its significance is that the parabolic scale `N^{-2}` is already encoded in the operator which converts physical strain into critical growth; a separately invented natural-window ontology is not primitive.

## 47. The mean-curl balance has an exact resolvent Hilbert square

The productive-Fisher bound of Section 42 can be sharpened without introducing a new branch.  Put

\[
m=K/E,
\qquad
s_m=(\Lambda-m)u.
\]

The exact heat metric for the mean absolute curl is

\[
\boxed{
D_m
:=\langle s_m,(\Lambda+m)s_m\rangle
=M_3-mZ
=E\,\mathbb E_p[(r-m)^2(r+m)].
}
\]

Since the Euler energy source is tangent,

\[
\langle s_m,F_E\rangle=\kappa(0),
\]

and therefore

\[
\boxed{
 m'=\frac2E\big(\kappa(0)-\nu D_m\big).
}
\]

Let `F_spec` be the curl-cyclic projection of the Euler velocity.  Because `s_m` lies in that cyclic subspace, the complete square is exact:

\[
\boxed{
\begin{aligned}
m'={}&-\frac{2\nu}{E}
\left\|
(\Lambda+m)^{1/2}s_m
-\frac1{2\nu}(\Lambda+m)^{-1/2}F_{spec}
\right\|_2^2\\
&+\frac1{2\nu E}
\left\|(\Lambda+m)^{-1/2}F_{spec}\right\|_2^2.
\end{aligned}
}
\]

Consequently

\[
\boxed{
(\log m)'
\le
\frac{\kappa(0)^2}{2\nu E m D_m}
\le
\frac{1}{2\nu E m}
\left\|(\Lambda+m)^{-1/2}F_{spec}\right\|_2^2.
}
\]

The first upper is the scalar-optimal one for the actual current `kappa(0)`; the second is the full dual resolvent action.  Log-convexity gives

\[
D_m\ge \frac{N^2}{m}\frac{EZ-K^2}{E},
\]

so this resolvent bound is never weaker than the previous variance-only productive-Fisher upper.

Again the same Poisson semigroup appears:

\[
\boxed{
\left\|(\Lambda+m)^{-1/2}F_{spec}\right\|_2^2
=
\int_0^\infty e^{-mt}
\left\|e^{-t\Lambda/2}F_{spec}\right\|_2^2dt.
}
\]

Thus the critical strain filter and the sharp mean-curl forcing metric are two functional-calculus readings of the same `|curl|` semigroup.

There is **no uniform Cauchy gap** hidden here.  Adversarial closed three-node currents drove the weighted resolvent Cauchy fraction above `0.99999995` on extreme amplitude states.  The theorem is the exact state-dependent heat metric and action cost, not a fictitious universal loss constant.

## 48. Revised frontier: one midpoint strain operator, one heat metric, one persistence problem

After the preceding reductions, the critical part of the full equation can be stated with very little ontology.  The one operator

\[
\boxed{
\Sigma_c(u)
=-\operatorname{sech}\!\left(\frac12\operatorname{ad}_{\log|C|}\right)
\big(\mathbb P S(u)\big)_{\rm odd}
}
\]

is the entire self-adjoint Euler generator of `H^{1/2}` growth.  With `y=|C|^{1/2}u`,

\[
\boxed{
K'=2\langle y,\Sigma_c(u)y\rangle
-2\nu\||C|y\|_2^2.
}
\]

Everything previously called heterochiral critical production, opposite-helicity pair creation, `kappa(0)`, productive Fisher alignment and critical Krein boost is a reading of this same midpoint strain operator.

The operator law also says exactly what a hypothetical escape must defeat simultaneously:

1. `Sigma_c` has no helicity-even component at all;
2. its helicity-odd component is a contraction of physical strain;
3. cross-curl blocks are exponentially suppressed in log-scale separation;
4. almost-uniform advecting modes are killed by the independent Galilean commutator null;
5. the state samples this strain through a canonical Poisson scale with mean `1/(2m)`;
6. that Poisson semigroup is subordinate to the same quadratic heat generator used by viscosity;
7. mean-curl growth pays the exact `(Lambda+m)` resolvent heat metric.

These are simultaneous properties of one operator law, not seven proof cases.

They still do **not** prove global regularity.  Spatially concentrated, many-mode strain can in principle make the midpoint operator large, and the exact resolvent Cauchy law can approach saturation.  The remaining theorem has become narrower:

> **Can the self-generated physical strain keep the Poisson-averaged helicity-odd midpoint operator positively aligned with the critical state for infinite productive resolvent action, while the same `|C|` calculus continuously exposes that state to `C^2` heat?**

A satisfactory next theorem should bound that persistence directly.  Returning to owner labels, packet clocks or a fixed catalog of transfer cases would move away from the operator mechanism exposed here.

### The critical midpoint is the unique blockwise depletion point

The master law gives a precise statement stronger than scaling criticality.  Fix any pair of positive curl radii and vary `s` over `[0,1]`.  On a helicity-even block,

\[
\left|
\frac{\sinh((s-\tfrac12)x)}{\sinh(x/2)}
\right|
\]

is minimized by the value `0` at `s=1/2`.  On a helicity-odd block,

\[
\frac{\cosh((s-\tfrac12)x)}{\cosh(x/2)}
\ge
\operatorname{sech}(x/2),
\]

again with equality at the midpoint.  Therefore

\[
\boxed{
H^{1/2}\text{ is the unique Sobolev metric between energy and enstrophy}
\text{ which minimizes every blockwise symmetric Euler growth coefficient.}
}
\]

At that midpoint the symmetric generator anticommutes with the helicity involution, so in block form

\[
\Sigma_c=
\begin{pmatrix}
0&B\\
B^*&0
\end{pmatrix}.
\]

Writing `K_+,K_-` for the two critical helicity stocks gives the immediate continuous depletion law

\[
\boxed{
|\kappa(0)|
=|\langle y,\Sigma_cy\rangle|
\le 2\|B\|\sqrt{K_+K_-}
=\|\Sigma_c\|\sqrt{K^2-H^2}.
}
\]

Thus exact or near homochirality loses critical leverage continuously; no homochiral branch threshold is primitive.  Together with `M_3>=K^3/E^2`, positive critical growth necessarily requires

\[
\boxed{
\frac{\|\Sigma_c\|}{\nu (K/E)^2}
>
\frac{K}{\sqrt{K^2-H^2}}.
}
\]

This is only a necessary instantaneous condition.  In the strongly mixed regime `|H|/K -> 0` its right-hand side approaches one, so the unresolved difficulty remains the persistence of a large concentrated midpoint strain operator.

## 49. The dangerous midpoint operator itself satisfies a forced operator heat equation

The preceding laws identify the critical symmetric generator

\[
\Sigma_c(u)
=-\operatorname{sech}\!\left(\frac12\operatorname{ad}_{\log\Lambda}\right)
(\mathbb P S(u))_{\rm odd}.
\]

This map is **linear in `u`**.  It is also translation-covariant: every Fourier matrix element between output `k` and input `l` is proportional to a velocity coefficient at the difference wavevector `q=k-l`, multiplied by fixed geometric/curl functional-calculus factors.

Let

\[
D_j=-i\partial_j
\]

be the three commuting momentum operators.  For any such translation-covariant kernel,

\[
[D_j,[D_j,\Sigma_c]]_{k l}
=(k_j-l_j)^2(\Sigma_c)_{k l}.
\]

Since `C^2=-Delta` multiplies the generating velocity coefficient `u_q` by `|q|^2`, one has the exact covariance identity

\[
\boxed{
\Sigma_c(C^2u)
=
\sum_{j=1}^3[D_j,[D_j,\Sigma_c(u)]].
}
\]

Therefore the **full Navier--Stokes evolution of the dangerous operator itself** is

\[
\boxed{
\partial_t\Sigma_c(u)
=
\Sigma_c(F_E(u))
-\nu\Delta_{\rm op}\Sigma_c(u),
\qquad
\Delta_{\rm op}B:=\sum_j[D_j,[D_j,B]].
}
\]

The first term is the one nonlinear regeneration/reorientation source.  The second is genuine heat on operator kernels.

For `tau>=0`,

\[
(e^{-\tau\Delta_{\rm op}}B)_{kl}
=e^{-\tau|k-l|^2}B_{kl}.
\]

Equivalently,

\[
\boxed{
 e^{-\tau\Delta_{\rm op}}B
=
\int_{\mathbb R^3}G_\tau(x)
T_xBT_x^{-1}\,dx,
}
\]

where `T_x` is physical translation.  Thus operator heat is a convex Gaussian average of unitary translation conjugations and is contractive in operator norm.  A finite Fourier referee reproduced the double-commutator factor to relative residual `7.6e-17`.

This clarifies a subtle point.  Viscosity does not rotate a normalized curl-eigenspace direction and therefore does not directly change normalized Cartan orientation.  Nevertheless it damps the **operator coefficient carrying that orientation** at exactly the spatial frequency `|k-l|` of the velocity mode which creates it.  Coherence is not randomized; its carrier is heat-eroded.

Uniform sweeping is the zero-frequency endpoint of the same law.  A constant velocity has `k-l=0` in its operator kernel and creates no critical boost by the Galilean commutator null.  More generally, a pure constant translation only conjugates the current operator unitarily and cannot amplify its norm.

The no-escape question can therefore be rewritten yet again, now as a single forced-dissipative operator equation:

> **Can the self-generated source `Sigma_c(F_E)` replenish a critical midpoint strain operator at the same increasingly high translation frequencies at which `nu Delta_op` removes it, with enough positive Rayleigh alignment to make the productive resolvent action diverge?**

This is stricter than saying “strain must be large.”  The dangerous strain must be regenerated in the correct helicity-odd, log-scale-local, translation-frequency sector of one heat-damped operator.

## 50. `H^{1/2}` is the positive modulus of helicity, and Euler is pseudo-unitary there

The midpoint law has a basis-free origin which is even shorter than the block calculation.  The helicity operator is self-adjoint and has the polar decomposition

\[
\boxed{C=\mathsf J\Lambda,
\qquad
\Lambda=|C|>0,
\qquad
\mathsf J=\operatorname{sgn}C.}
\]

Thus the critical positive quadratic form

\[
\boxed{K=\langle u,\Lambda u\rangle}
\]

is not an analyst-selected replacement for the indefinite helicity

\[
H=\langle u,Cu\rangle.
\]

It is the **canonical positive modulus of the helicity form itself**.

Freeze the state `u` and write the Euler velocity operator as

\[
G_u=J_uC.
\]

Since `J_u^*=-J_u`, one has the exact operator identity

\[
\boxed{G_u^*C+CG_u=0.}
\]

This is stronger than checking conservation after inserting the particular state: the whole frozen Euler operator is skew with respect to the indefinite helicity form.  Conjugating by the positive modulus,

\[
L_u=\Lambda^{1/2}G_u\Lambda^{-1/2},
\]

gives

\[
\boxed{L_u^*\mathsf J+\mathsf J L_u=0.}
\]

Hence along an Euler trajectory the linear cocycle generated by `L_u(t)` is pseudo-unitary.  If `U(t)` solves

\[
\dot U=L_u(t)U,
\qquad U(0)=I,
\]

then

\[
\boxed{U(t)^*\mathsf J U(t)=\mathsf J.}
\]

In every finite Galerkin realization this forces reciprocal singular values.  Indeed

\[
U^{-1}=\mathsf J U^*\mathsf J,
\]

so the singular-value multiset of `U^{-1}` equals that of `U`; after ordering,

\[
\boxed{s_j(U)s_{n+1-j}(U)=1.}
\]

Thus Euler cannot create positive critical expansion in every direction.  Every noncompact critical amplification is a hyperbolic boost accompanied by reciprocal contraction.  This is the propagator version of the local block law `Sigma_c J + J Sigma_c=0`.

It is **not** a regularity theorem.  A single trajectory vector can continually align with expanding singular directions of a time-dependent pseudo-unitary cocycle.  The missing theorem is precisely what prevents that alignment from persisting while the expanding directions themselves are being regenerated by a heat-damped, self-generated strain operator.

The same Lie transport has a physical metric interpretation.  On divergence-free vector fields

\[
\boxed{
\operatorname{Sym}_{L^2}(\operatorname{ad}_u)
=-\mathbb P S(u).
}
\]

Thus physical strain is the symmetric metric-deformation part of material Lie transport.  The master Sobolev law of Section 44 is the exact change of this one metric-deformation operator as the vorticity Lie transport is represented in the Sobolev metrics interpolating between kinetic energy and enstrophy.  The critical modulus metric is the unique midpoint where the compact/noncompact split is maximally depleted.

## 51. Uniform sweeping is pure unitary conjugation of the dangerous operator

The Galilean null can also be stated dynamically.  Let `U` be a constant velocity.  Since

\[
\mathbb P(U\times\omega)=-(U\cdot\nabla)u,
\]

a uniform sweeping contribution evolves the state by translation.  Because `Sigma_c` is linear and translation-covariant,

\[
\boxed{
\Sigma_c(-(U\cdot\nabla)u)
=-i[U\cdot D,\Sigma_c(u)],
\qquad D=-i\nabla.
}
\]

The solution of this part alone is

\[
\Sigma_c(t)
=e^{-itU\cdot D}\Sigma_c(0)e^{itU\cdot D},
\]

so every unitarily invariant norm is exactly unchanged.  A finite translation-covariant matrix referee gave relative residual `1.5e-16`, and direct conjugation preserved operator norm to roundoff.

This is the dynamical counterpart of the single-edge factor `|q|`: uniform advection may move the dangerous operator through space, but it cannot regenerate its boost amplitude.  Any regeneration beyond unitary conjugation is caused by **velocity deformation**, not sweeping.  The Galilean quotient is therefore built into the operator equation itself rather than imposed as a separate branch.

## 52. Midpoint regeneration has no external source: objective strain, pressure reorientation and Betchov amplitude

The operator forcing in Section 49 can be read directly in physical space.  Write

\[
A=\nabla u=S+\Omega,
\qquad
F_E=-\mathbb P((u\cdot\nabla)u).
\]

For the Euler pressure convention

\[
F_E=-(u\cdot\nabla)u-\nabla p,
\]

the exact strain of the Euler acceleration is

\[
\boxed{
S(F_E)
=-(u\cdot\nabla)S-S^2-\Omega^2-\nabla^2p.
}
\]

Thus, if `H_c` denotes the fixed linear midpoint map from physical strain to `Sigma_c`,

\[
\boxed{
\Sigma_c(F_E)
=-\mathcal H_c\big((u\cdot\nabla)S+S^2+\Omega^2+\nabla^2p\big).
}
\]

There is no independent forcing object hidden on the right.  Transport, quadratic deformation and the incompressibility pressure reaction of the same velocity gradient are the entire Euler regeneration law.  Viscosity is the operator heat term already isolated in Section 49.

In the frame rotating with the local antisymmetric gradient, the objective strain derivative is

\[
\boxed{
S^\circ
= -S^2-\Omega^2-\nabla^2p+[S,\Omega]+\nu\Delta S.
}
\]

The two reorientation terms have exact amplitude orthogonality properties.  Pointwise,

\[
\boxed{S:[S,\Omega]=0,}
\]

and on the periodic domain

\[
\boxed{\int S:\nabla^2p\,dx=0.}
\]

The pressure Hessian can therefore change the future orientation/eigenframe of strain without doing direct global `L^2` strain work.  It is a constraint reaction, not a separate energy source.

The amplitude source itself collapses further through the Betchov identity.  Periodic incompressibility gives

\[
\int\operatorname{tr}(\nabla u)^3dx=0,
\]

and since

\[
\operatorname{tr}(S\Omega^2)=\frac14\omega\cdot S\omega,
\]

one obtains

\[
\boxed{
\int\operatorname{tr}S^3dx
=-\frac34\int\omega\cdot S\omega\,dx.
}
\]

Together with `||omega||_2^2=2||S||_2^2`, the full global strain-amplitude equation is

\[
\boxed{
\frac12\frac d{dt}\|S\|_2^2
=-\frac23\int\operatorname{tr}S^3dx
-\nu\|\nabla S\|_2^2.
}
\]

A direct dealiased Fourier referee gave `tr(S^3)+(3/4) omega.S.omega` residual `2.0e-15`, pressure-work residual `2.7e-16`, corotation-work residual `1.5e-16`, and `||omega||^2/(2||S||^2)=1` to roundoff.

This separates two intrinsic tasks without turning them into branches:

- **amplitude regeneration** of physical strain is cubic self-deformation of the same gradient;
- **orientation regeneration** can be performed by pressure/corotation but cannot mint global strain `L^2` stock directly.

The distinction matters because actual Galerkin states also falsify the tempting implication `critical growth => enstrophy growth`.  Among `80` smooth random dealiased states, examples occurred in all four sign quadrants of `(K'_NL,Z'_NL)`, including

\[
K'_{NL}>0,\qquad Z'_{NL}<0.
\]

So critical height `kappa(0)` may temporarily exploit/reorient existing strain even while total enstrophy amplitude falls.  Any no-escape theorem must control the **persistence** of that reorientation/concentration, not replace the midpoint current by the enstrophy current.

## 53. Critical blow-up has an endpoint-energy-free `H^{-1/2}` spectral action cost

The mean-curl resolvent law is useful when one wants the scale `m=K/E`, but the critical stock `K` itself has an even shorter exact square.  Let `F_spec` be the actual Euler velocity projected onto the curl-cyclic subspace of the current state and put

\[
A=\Lambda^{3/2}u,
\qquad
B=\Lambda^{-1/2}F_{spec}.
\]

Because `Lambda u` is curl-cyclic,

\[
\boxed{
\langle A,B\rangle
=\langle\Lambda u,F_{spec}\rangle
=\kappa(0),
\qquad
\|A\|_2^2=M_3.
}
\]

Therefore the **full** critical balance completes one Hilbert square:

\[
\boxed{
K'
=-2\nu\left\|
\Lambda^{3/2}u-rac1{2\nu}\Lambda^{-1/2}F_{spec}
\right\|_2^2
+
\frac1{2\nu}\|\Lambda^{-1/2}F_{spec}\|_2^2.
}
\]

This is sharper than completing the square with the full Euler/Lamb velocity because all isospectral phase/shape motion has already been orthogonally removed.  It is also Galilean clean: uniform sweeping changes phases but creates no curl-spectral energy velocity.

Two immediate bounds are

\[
\boxed{
K'\le
\frac{\kappa(0)^2}{2\nu M_3}
\le
\frac1{2\nu}\|\Lambda^{-1/2}F_{spec}\|_2^2,
}
\]

and hence

\[
\boxed{
(\log K)'
\le
\frac1{2\nu K}\|\Lambda^{-1/2}F_{spec}\|_2^2.
}
\]

This criterion does not use the endpoint energy clock.  In particular,

\[
\boxed{
K(t)\to\infty
\quad\Longrightarrow\quad
\int^T
\frac{\|\Lambda^{-1/2}F_{spec}(t)\|_2^2}{K(t)}dt
=\infty.
}
\]

On an occupied signed-curl level let `f(a)=S_a/(2E_a)` and define the **critical energy probability**

\[
\boxed{
\pi_a=\frac{|a|E_a}{K}.
}
\]

Then

\[
\boxed{
\frac1K\|\Lambda^{-1/2}F_{spec}\|_2^2
=
\mathbb E_\pi\left[\left(\frac{f(a)}{|a|}\right)^2\right].
}
\]

Thus critical blow-up requires infinite accumulated squared Euler fitness measured in units of the local curl frequency, under the physical probability which defines the critical stock itself.  This is another formulation of the same persistence problem, not a new owner.

The scalar-optimal action

\[
\frac{\kappa(0)^2}{K M_3}
=
\frac{(\mathbb E_\pi f)^2}{\mathbb E_\pi |a|^2}
\]

is smaller still; Cauchy gives the displayed `H^{-1/2}` action as its full spectral envelope.  No uniform gap between them is claimed.

## 54. One shifted Hilbert square governs the entire Sobolev hierarchy

The critical `H^{-1/2}` square is the midpoint of a second master law.  For any real `s` define

\[
K_s=\|\Lambda^s u\|_2^2,
\qquad
\langle v,w\rangle_{s-1}
=\langle\Lambda^{s-1}v,\Lambda^{s-1}w\rangle_2.
\]

For the functional `K_s/2`, the gradient in this shifted metric is always the **same operator**:

\[
\boxed{
\nabla_{H^{s-1}}\frac{K_s}{2}=\Lambda^2u=C^2u.
}
\]

Indeed

\[
\langle\Lambda^2u,v\rangle_{s-1}
=\langle\Lambda^{s+1}u,\Lambda^{s-1}v\rangle
=\langle\Lambda^{2s}u,v\rangle.
\]

Thus quadratic viscosity is simultaneously the gradient descent of every Sobolev quadratic, provided one reads each stock in its naturally shifted metric.  There is no separate viscous mechanism at energy, criticality and enstrophy.

Let `F_spec` again denote the one actual curl-spectral Euler velocity and put

\[
A_s=\Lambda^{s+1}u,
\qquad
B_s=\Lambda^{s-1}F_{spec}.
\]

Then

\[
\langle A_s,B_s\rangle
=\langle\Lambda^{2s}u,F_{spec}\rangle
\]

is exactly half of the nonlinear `K_s` rate, and full Navier--Stokes gives

\[
\boxed{
K_s'
=-2\nu\left\|
A_s-\frac{B_s}{2\nu}
\right\|_2^2
+
\frac1{2\nu}\|B_s\|_2^2.
}
\]

Therefore

\[
\boxed{
(\log K_s)'
\le
\frac1{2\nu K_s}
\|\Lambda^{s-1}F_{spec}\|_2^2.
}
\]

This family has a particularly simple state interpretation.  On occupied curl levels let

\[
f(a)=\frac{S_a}{2E_a}
\]

and define the Sobolev energy probability

\[
\boxed{
\pi_s(a)=\frac{|a|^{2s}E_a}{K_s}.
}
\]

Then for **every** `s`,

\[
\boxed{
\frac{\|\Lambda^{s-1}F_{spec}\|_2^2}{K_s}
=
\mathbb E_{\pi_s}
\left[\left(\frac{f(a)}{|a|}\right)^2\right].
}
\]

The local escape currency is therefore the same throughout the Sobolev hierarchy:

\[
\boxed{\text{Euler spectral fitness per unit curl frequency } f/|a|.}
\]

Changing `s` only changes which physical energy population samples that one currency.  In particular

- `s=0`: kinetic energy stock and the `H^{-1}` shifted metric; the nonlinear cross vanishes by energy conservation;
- `s=1/2`: the positive modulus of helicity and the `H^{-1/2}` shifted metric;
- `s=1`: enstrophy and the ordinary `L^2` metric.

Sections 44 and 54 now give two sides of one law.  Section 44 says how the **symmetric Euler generator** changes with the Sobolev metric; this section says how the **same quadratic heat** is the gradient and how the same local fitness action pays for growth in that metric.  The critical midpoint is distinguished because the Euler side is blockwise minimized there while the viscous gradient structure remains exact.

## 55. An isolated physical triad has a sharp scale-free critical action ceiling

The master `H^{-1/2}` action makes it possible to answer one old question without any recursion: **can one closed Waleffe triad become a blow-up engine merely by moving to arbitrarily high Fourier scale?**  The answer is no.

Let the physical triad have positive radii `r_i`, signed curl values `a_i=s_i r_i`, modal energies `e_i`, and Waleffe magnitude `|g|`.  With one common phase orientation the energy rates are

\[
T_i
=4|g|\cos\Phi\,\sqrt{e_0e_1e_2}\,(a_j-a_k).
\]

Its critical shifted spectral action is therefore

\[
\mathcal A_{-1/2,\triangle}
=\frac14\sum_i\frac{T_i^2}{r_i e_i}
=4|g|^2\cos^2\Phi
\sum_i\frac{(a_j-a_k)^2}{r_i}e_je_k.
\]

The key theorem is the **root geometric inequality**

\[
\boxed{
\frac{8|g|^2(a_j-a_k)^2}{r_i(r_j+r_k)}\le1
}
\]

for every root `i` of every physical Fourier triangle and every helicity assignment.

A compact proof avoids helicity case taxonomy.  Put

\[
p=r_j+r_k,
\qquad
\alpha=\frac{r_i}{p}\in(0,1),
\qquad
\beta=\frac{|r_j-r_k|}{r_i}\in[0,1).
\]

If the parent helicities are opposite, the worst signed factors are `|a_j-a_k|=p` and `|s_ir_i+s_jr_j+s_kr_k|<=r_i+|r_j-r_k|`.  Heron's formula reduces the root inequality to

\[
(1-\alpha^2\beta^2)^2
\ge
\alpha(1-\alpha^2)(1-\beta^2)(1+\beta)^2.
\]

Set `alpha=tanh A`, `beta=tanh B`.  After clearing the positive hyperbolic denominators this becomes

\[
(\cosh2A+\cosh2B)^2
\ge
2e^{2B}\sinh2A,
\]

which follows from

\[
(\cosh2A+\cosh2B)^2
\ge4\cosh2A\cosh2B
\ge2e^{2B}\sinh2A.
\]

If the parent helicities agree, the dangerous signed difference is only `|r_j-r_k|`; after interchanging the two normalized variables its left side is smaller by the factor `alpha beta<=1`.  The opposite child sign also only lowers the remaining signed sum.  Thus the same inequality covers all sign assignments.

Multiply the root inequality by `e_j e_k/2` and sum over the three roots.  Since

\[
E_\triangle K_\triangle
=\sum_i r_ie_i^2
+\sum_{i<j}(r_i+r_j)e_ie_j,
\]

one obtains

\[
\boxed{
\mathcal A_{-1/2,\triangle}
\le
\frac12\sum_{i<j}(r_i+r_j)e_ie_j
\le
\frac12 E_\triangle K_\triangle.
}
\]

The constant `1/2` is a sharp supremum.  It is approached by a low--high--high triangle `(1,epsilon,1)` at the degenerate boundary, together with amplitudes satisfying `epsilon << delta << 1` so the low mode carries almost all energy while one high mode carries the critical cross term.  High-precision decimal arithmetic showed the root ratio tending to one with deficit asymptotic to `2 epsilon` in the symmetric boundary family; random physical searches reached action ratio above `0.4986` before the analytic proof was used.

Combining with the exact square of Section 53 gives, for an isolated viscous closed triad,

\[
\boxed{
(\log K_\triangle)'
\le
\frac{E_\triangle(t)}{4\nu}
\le
\frac{E_\triangle(0)}{4\nu}.
}
\]

Therefore

\[
\boxed{
K_\triangle(t)
\le
K_\triangle(0)
\exp\!\left(\frac{E_\triangle(0)t}{4\nu}\right),
}
\]

with **no dependence on the absolute Fourier scale**.  An isolated triad cannot create finite-time critical escape by climbing toward the ultraviolet.

This does not globalize by summing triads.  In the full PDE, several Cartan/Waleffe faces contribute to the same spectral velocity **before the square is taken**.  The cross terms in

\[
\left\|\Lambda^{-1/2}\sum_\triangle F_{spec,\triangle}\right\|^2
\]

are the genuine coherent-cooperation loophole.  The canonical 3-cocycle/Jacobi law constrains those faces, but the present theorem does not yet bound their collective constructive interference.  The global no-escape problem has therefore narrowed once more: **not single-triad UV productivity, but persistence of coherent cross-triad action.**

## 56. Coherent cooperation is one representation-free productive action volume

The isolated-triad theorem shows exactly what is missing globally.  Define first the full critical spectral-action volume

\[
\boxed{
V_K
:=
\frac{EK}{2\|\Lambda^{-1/2}F_{spec}\|_2^2}.
}
\]

The exact square of Section 53 becomes

\[
\boxed{
(\log K)'
\le
\frac{E}{4\nu V_K}.
}
\]

For one physical closed triad Section 55 gives `V_K>=1` in the repository Fourier-volume normalization.  The full PDE need not satisfy this.  Different Cartan/Waleffe faces contribute to `F_spec` before the square is taken, so constructive cross terms can make `V_K` smaller than the isolated value.  No choice of triad decomposition enters the definition of `V_K` itself.

There is a sharper volume which deletes spectral reconfiguration irrelevant to `K`.  Put

\[
\boxed{
\mathcal A_K^{prod}
:=\frac{\kappa(0)^2}{M_3},
\qquad
V_K^{prod}
:=\frac{EK}{2\mathcal A_K^{prod}}
=\frac{EKM_3}{2\kappa(0)^2}.
}
\]

Cauchy gives

\[
\mathcal A_K^{prod}
\le\|\Lambda^{-1/2}F_{spec}\|_2^2,
\qquad
\boxed{V_K^{prod}\ge V_K.}
\]

This fixes an important ontology issue.  A homochiral state may have large radial spectral traffic and finite `V_K`, but `kappa(0)=0`; its productive volume is infinite, exactly as critical conservation requires.

The scalar-optimal completion of the critical balance is

\[
\boxed{
(\log K)'
\le
\frac{E}{4\nu V_K^{prod}}.
}
\]

Define also the **critical viscous volume**

\[
\boxed{
V_\nu
:=\frac{EK}{2\nu^2M_3}.
}
\]

Then the full critical balance itself, not an inequality, is

\[
\boxed{
\frac{K'}{2\nu M_3}
=
\operatorname{sgn}(\kappa(0))
\sqrt{\frac{V_\nu}{V_K^{prod}}}
-1.
}
\]

Equivalently the productive Reynolds number is

\[
\boxed{
\operatorname{Re}_K^{prod}
:=\frac{|\kappa(0)|}{\nu M_3}
=\sqrt{\frac{V_\nu}{V_K^{prod}}}.
}
\]

Thus positive critical growth is exactly

\[
\boxed{
\kappa(0)>0
\quad\text{and}\quad
V_K^{prod}<V_\nu.
}
\]

This is the shortest critical generation--dissipation statement currently visible in the programme: **one nonlinear productive volume races one viscous volume**.  The previous factors `Re_spec`, radial diversity, decorrelation and alignment are factorizations of this one race.

Both volumes have genuine physical-volume covariance.  Under the Navier--Stokes dilation

\[
u_\lambda(x,t)=\lambda u(\lambda x,\lambda^2t),
\]

at one time

\[
E\mapsto\lambda^{-1}E,
\quad K\mapsto K,
\quad M_3\mapsto\lambda^2M_3,
\quad \kappa(0)\mapsto\lambda^2\kappa(0),
\]

so

\[
\boxed{
V_K,\ V_K^{prod},\ V_\nu
\mapsto\lambda^{-3}
(V_K,V_K^{prod},V_\nu).
}
\]

The Reynolds ratio is scale invariant.  Moreover

\[
\boxed{
\int E(t)\,V_K^{prod}(t)^{-1}\,dt
}
\]

is invariant under the full space--time NS scaling, and finite-time divergence of `K` requires this action integral to diverge.

### A true dilation falsifies any global single-triad volume floor

A direct Galerkin probe used a fixed smooth divergence-free packet `U=curl A` and the exact dilation

\[
A_\sigma(x)=A(x/\sigma),
\qquad
u_\sigma(x)=\sigma^{-1}U(x/\sigma),
\]

embedded in the periodic box.  Unlike an earlier exploratory Gaussian with unscaled internal oscillations, this is a true one-shape dilation.  Amplitude normalization was immaterial because the volume ratios are amplitude invariant.

For the total critical action volume the resolved results were

| `sigma` | `V_K` | `2||Lambda^-1/2 F_spec||^2/(EK)` |
|---:|---:|---:|
| 0.50 | 1.1844 | 0.8443 |
| 0.40 | 0.64725 | 1.5450 |
| 0.32 | 0.34891 | 2.8661 |
| 0.27 | 0.21617 | 4.6261 |
| 0.23 | 0.13677 | 7.3116 |

At `sigma=0.32`, resolutions `96^3` and `128^3` agreed to about `1e-12`.  Thus coherent multi-mode states genuinely cross below the isolated-triad floor `V_K=1`; this is not aliasing or a bookkeeping artifact.

More significantly, the **productive** volume followed the physical dilation almost exactly:

| `sigma` | `V_K^prod` | `V_K^prod/sigma^3` |
|---:|---:|---:|
| 0.50 | 40.1664 | 321.33 |
| 0.40 | 20.5657 | 321.34 |
| 0.32 | 10.5297 | 321.34 |
| 0.27 | 6.32493 | 321.34 |
| 0.23 | 3.90974 | 321.34 |

So the productive quantity is not merely named a volume by dimensional analogy: an actual fixed-shape concentration reads it as one.

### Neither action volume is a heat Lyapunov function

There is another important falsification guard.  Pure viscosity does **not** monotonically enlarge these volumes.  In `36` random smooth Galerkin states, a small heat step increased the total coherence `1/V_K` in all samples.  On `120000` isolated triads the derivative of `1/V_K` had both signs.  For the sharper productive coherence `1/V_K^prod`, `160000` isolated-triad samples again produced both signs.

This does not contradict operator heat.  Heat damps every translation-frequency carrier, but it simultaneously reweights the state against which nonlinear productivity is normalized.  The exact theorem is the two-volume race and the forced operator heat equation, **not** monotonicity of a normalized volume scalar.

The global frontier can therefore be stated without triad counting:

> **Can the full Cartan/Navier--Stokes dynamics keep collapsing the representation-free productive action volume below the simultaneously shrinking viscous volume for enough scale-critical action time, while the dangerous midpoint operator itself is being heat-eroded at the translation frequencies which carry that concentration?**

That is now the coherent multi-triad problem in one sentence.

## 57. The critical equation is one scale-invariant Reynolds operator minus identity

The two-volume race has a still shorter operator form.  Recall the self-adjoint, helicity-odd midpoint generator

\[
\Sigma_c(u)
=-\operatorname{sech}\!\left(\frac12\operatorname{ad}_{\log\Lambda}\right)
(\mathbb P S(u))_{odd}.
\]

Move to the critical **dissipation coordinate**

\[
\boxed{z=\Lambda y=\Lambda^{3/2}u}
\]

and define the dimensionless critical Reynolds operator

\[
\boxed{
\mathscr R_c(u)
:=
u^{-1}\Lambda^{-1}\Sigma_c(u)\Lambda^{-1}.
}
\]

It satisfies

\[
\boxed{
\mathscr R_c^*=\mathscr R_c,
\qquad
\{\mathscr R_c,\mathsf J\}=0.
}
\]

The full critical balance is then exactly

\[
\boxed{
K'
=2\nu\langle z,(\mathscr R_c(u)-I)z\rangle.
}
\]

No completion of squares or auxiliary threshold is needed to read this formula.  In the variable in which critical viscosity is measured, **quadratic heat is the identity operator**, and all possible Euler amplification is one self-adjoint Reynolds operator.

The scalar productive Reynolds number is merely its Rayleigh quotient:

\[
\boxed{
\frac{\kappa(0)}{\nu M_3}
=
\frac{\langle z,\mathscr R_c(u)z\rangle}{\|z\|_2^2}.
}
\]

Consequently

\[
\boxed{
K'>0
\iff
\frac{\langle z,\mathscr R_c z\rangle}{\|z\|^2}>1.
}
\]

The volume ratio of Section 56 is exactly the square of the absolute Rayleigh quotient:

\[
\boxed{
\frac{V_\nu}{V_K^{prod}}
=
\left|
\frac{\langle z,\mathscr R_c z\rangle}{\|z\|^2}
\right|^2.
}
\]

Because `mathscr R_c` anticommutes with the helicity involution, its spectrum is symmetric about zero in every finite Galerkin realization.  A positive eigenvalue capable of beating heat is accompanied by a negative one of the same magnitude.  The obstruction is not absence of contracting directions; it is persistence of the actual critical state inside the expanding spectral subspace of a **self-generated, time-dependent Reynolds operator**.

This operator is invariant under the natural Navier--Stokes scaling up to the unitary spatial dilation which identifies the rescaled Hilbert spaces.  The scalar Rayleigh quotient and `V_nu/V_prod` are therefore exactly scale invariant.

### Heat and sweeping cannot create a supercritical eigenvalue by themselves

Since `Lambda` commutes with the momentum operators `D_j`, the forced operator heat equation of Section 49 conjugates directly to `mathscr R_c`:

\[
\boxed{
\partial_t\mathscr R_c
=
\nu^{-1}\Lambda^{-1}\Sigma_c(F_E)\Lambda^{-1}
-\nu\Delta_{op}\mathscr R_c.
}
\]

The pure operator-heat semigroup is a unital positive Gaussian average of unitary translation conjugations.  Hence it preserves operator order and cannot increase the top eigenvalue or operator norm of a self-adjoint Reynolds operator.

A constant sweeping velocity contributes only

\[
-i[U\cdot D,\mathscr R_c],
\]

which unitarily conjugates the operator and leaves **all** eigenvalues unchanged.  Therefore neither viscosity nor Galilean transport can manufacture a Reynolds eigenvalue above one.  Only the genuine deformation source

\[
\nu^{-1}\Lambda^{-1}\Sigma_c(F_E)\Lambda^{-1}
\]

can regenerate such an eigenvalue.

Section 52 already identifies that source: after material transport is removed it consists of quadratic velocity-gradient deformation and incompressibility pressure reorientation of the same strain field.  The global problem has therefore reached a very small operator statement:

> **Can self-generated objective deformation repeatedly create eigenvalues `>1` of `mathscr R_c`, and rotate the critical dissipation state into their expanding eigenspaces, faster than operator heat removes the translation-frequency carriers of those eigenvalues?**

This is the current operator-level no-escape question.

## 58. Expanding Reynolds directions are helicity-neutral and heat pays their translation leakage

The helicity-odd symmetry of the critical Reynolds operator has a direct eigenvector consequence.  If

\[
\mathscr R_c v=\lambda v,
\qquad \lambda\ne0,
\]

then

\[
\lambda\langle v,\mathsf Jv\rangle
=\langle\mathscr R_cv,\mathsf Jv\rangle
=-\langle v,\mathsf J\mathscr R_cv\rangle
=-\lambda\langle v,\mathsf Jv\rangle,
\]

hence

\[
\boxed{\langle v,\mathsf Jv\rangle=0.}
\]

Every nonzero expanding/contracting eigenvector is exactly balanced between the two helicity signs in the critical **dissipation coordinate**.  For the actual state `z=Lambda^(3/2)u`, put

\[
M_3=\|z\|_2^2,
\qquad
H_3=\langle z,\mathsf Jz\rangle
=\sum_a a^3E_a.
\]

`H_3` is the viscous helicity-dissipation moment because

\[
\boxed{H'=-2\nu H_3.}
\]

The off-diagonal block form therefore gives

\[
\boxed{
\left|
\frac{\langle z,\mathscr R_cz\rangle}{\|z\|^2}
\right|
\le
\|\mathscr R_c\|
\sqrt{1-\left(\frac{H_3}{M_3}\right)^2}.
}
\]

A state which is strongly one-sided in **helicity dissipation**, not merely in critical stock, loses leverage against heat continuously.

There is also an exact strictness formula for operator heat.  Let `lambda_max` be the top eigenvalue of a self-adjoint `mathscr R` and let `v` be a normalized top eigenvector.  Using

\[
\Delta_{op}\mathscr R
=\sum_j[D_j,[D_j,\mathscr R]],
\]

and `mathscr R v=lambda_max v`, one obtains

\[
\boxed{
\langle v,\Delta_{op}\mathscr R\,v\rangle
=
2\sum_j
\langle D_jv,
(\lambda_{max}I-\mathscr R)D_jv\rangle
\ge0.
}
\]

Thus the heat contribution to the upper spectral edge is exactly minus a noncommutative Dirichlet form.  Equality requires every translated tangent direction `D_jv` to remain in the top eigenspace.  A genuinely nonzero critical operator is generated by nonzero translation-frequency differences, while the `q=0` endpoint is the Galilean null.  This identifies the rigidity required for weak heat erosion without assuming a uniform spectral gap.

A small actual-Galerkin search tested the stronger but **unproved** crossing statement that deformation can never push `lambda_max` upward through one.  On `28` dense/sparse states, after amplitude rescaling to the formal crossing, the largest sampled ratio of deformation-source increase to operator-heat loss was about `0.0322`.  This is only exploratory evidence.  No universal crossing barrier is claimed: coherent concentrated states and initially supercritical Reynolds operators remain the genuine large-data problem.

## 59. On continuum `R^3`, the critical midpoint map is an exact Hilbert--Schmidt isometry

The critical Reynolds operator has one further structure which is invisible in a hard finite Galerkin cutoff.  Remove viscosity from the operator and define

\[
\boxed{
\mathcal Q_c(u)
:=\Lambda^{-1}\Sigma_c(u)\Lambda^{-1}
=\frac12\Lambda^{-1/2}[J_u,\mathsf J]\Lambda^{-1/2}.
}
\]

Then

\[
\mathscr R_c(u)=\nu^{-1}\mathcal Q_c(u).
\]

On continuum `R^3`, with the repository unitary Fourier convention

\[
\widehat f(k)
=(2\pi)^{-3/2}\int e^{-ix\cdot k}f(x)dx,
\qquad C_F=(2\pi)^{-3/2},
\]

the map `u -> Q_c(u)` is not merely compact.  It is an exact scaled isometry

\[
\boxed{
\|\mathcal Q_c(u)\|_{\mathfrak S_2}^2
=\frac1{64}\|u\|_{\dot H^{1/2}}^2
=\frac{K}{64}.
}
\]

Equivalently

\[
\boxed{K=64\|\mathcal Q_c(u)\|_{HS}^2,}
\qquad
\boxed{
\|\mathscr R_c(u)\|_{HS}^2
=\frac{K}{64\nu^2}.
}
\]

Thus `8 Q_c` is a linear isometric embedding of the critical velocity Hilbert space into Hilbert--Schmidt operators.  This is a continuum identity, not an estimate and not a finite-cutoff numerical fit.

### Derivation of the constant

Fix an advecting wavevector `q` and one of its two helical amplitudes.  Let

\[
Q=|q|,
\qquad p=|\ell|,
\qquad k=|q+\ell|.
\]

The critical operator only connects opposite input/output helicities.  In the helical basis its matrix element is

\[
(\mathcal Q_c)_{k,-s;\ell,s}
=
\frac{2C_F}{\sqrt{kp}}
\,g(q,\ell,-k;\sigma,s,-s)\,u_{q,\sigma},
\]

up to a unit-modulus convention phase.  Therefore, for fixed velocity helicity `sigma`, the square summed over the two input helicities is

\[
\sum_{s=\pm}
\frac{4|g|^2}{kp}|u_{q,\sigma}|^2.
\]

Rotational covariance around `q` makes the `sigma=+` and `sigma=-` kernels orthogonal in Hilbert--Schmidt space.  A direct azimuthal referee using the actual complex repository `coupling_g` gave normalized cross terms at `1e-16` scale over several widely separated triangle geometries, while the two diagonal weights agreed to roundoff.

By homogeneity the fixed-`q` integral is `Q` times its value at `Q=1`.  Put `mu=cos angle(q,l)` and

\[
k=\sqrt{1+p^2+2p\mu}.
\]

Using the exact Waleffe magnitude formula, the geometric integral becomes

\[
W(1)
=
\frac\pi2
\int_0^\infty p\,dp
\int_{-1}^{1}
(1-\mu^2)
\frac{1+(p-k)^2}{k^3}\,d\mu.
\]

Now use triangle sum/difference variables

\[
x=p+k>1,
\qquad
y=p-k\in(-1,1).
\]

The integral collapses to

\[
\boxed{
W(1)
=
\pi\int_1^\infty\int_{-1}^1
\frac{(x^2-1)(1-y^4)}{(x^2-y^2)^2}
\,dy\,dx.
}
\]

The `x` integral is elementary:

\[
\int_1^\infty
\frac{x^2-1}{(x^2-y^2)^2}dx
=
\frac{(1+y^2)\operatorname{artanh}y-y}{2y^3}.
\]

Hence

\[
W(1)
=
\pi\int_0^1
\frac{(1-y^4)((1+y^2)\operatorname{artanh}y-y)}{y^3}
\,dy.
\]

Expanding `artanh y`, integrating termwise and putting `n=2m-1` gives

\[
\frac{W(1)}\pi
=
\sum_{m\ge1}
\frac{16m}{(2m-1)^2(2m+1)(2m+3)},
\]

with the exact partial fraction identity

\[
\frac{16m}{(2m-1)^2(2m+1)(2m+3)}
=
\frac1{n^2}
+\frac1{4n}
-\frac1{n+2}
+\frac3{4(n+4)}.
\]

The three harmonic-series pieces telescope exactly, leaving only the odd square sum

\[
\boxed{
\frac{W(1)}\pi
=\sum_{n\ge1,\ n\ odd}\frac1{n^2}
=\frac{\pi^2}{8}.
}
\]

Therefore

\[
\boxed{W(1)=\frac{\pi^3}{8}.}
\]

The unitary Fourier convolution factor finally gives

\[
\boxed{
C_F^2W(1)
=(2\pi)^{-3}\frac{\pi^3}{8}
=\frac1{64}.
}
\]

Independent numerical referees gave `W(1)=3.87578458503807...` versus `pi^3/8=3.87578458503748...`, and the original unreduced `(p,mu)` quadrature agreed to relative error about `6.5e-13`.  These checks only guard the algebra; the displayed reduction is the proof.

### The midpoint map intertwines physical heat and operator heat

Because the map is linear, polarization upgrades the norm identity to

\[
\boxed{
\langle\mathcal Q_c(u),\mathcal Q_c(v)\rangle_{HS}
=\frac1{64}\langle u,\Lambda v\rangle_2.
}
\]

Section 49 already proved

\[
\boxed{
\mathcal Q_c(C^2u)
=\Delta_{op}\mathcal Q_c(u).
}
\]

Thus the scaled isometry **intertwines the two heat generators**.  Two further exact identities follow immediately:

\[
\boxed{
\sum_{j=1}^3
\|[D_j,\mathcal Q_c(u)]\|_{HS}^2
=\frac{M_3}{64},
}
\]

and, for the Euler source,

\[
\boxed{
\langle\mathcal Q_c(u),\mathcal Q_c(F_E(u))\rangle_{HS}
=\frac{\kappa(0)}{64}.
}
\]

Consequently the critical Navier--Stokes balance is literally the Hilbert--Schmidt energy balance of one operator-valued heat equation:

\[
\boxed{
\partial_t\mathcal Q_c
=\mathcal Q_c(F_E)-\nu\Delta_{op}\mathcal Q_c,
}
\]

\[
\boxed{
\frac12\frac d{dt}\|\mathcal Q_c\|_{HS}^2
=
\langle\mathcal Q_c,\mathcal Q_c(F_E)\rangle_{HS}
-\nu\sum_j\|[D_j,\mathcal Q_c]\|_{HS}^2.
}
\]

Multiplying by `64` is exactly

\[
\frac12K'=\kappa(0)-\nu M_3.
\]

This is a much stronger collapse than saying critical growth is *analogous* to an operator energy.  On continuum `R^3`, **the critical Navier--Stokes stock is exactly the Hilbert--Schmidt energy of the self-generated midpoint operator, and critical viscosity is exactly its operator-heat Dirichlet form.**

A hard Fourier/Galerkin truncation does not satisfy the constant `1/64` exactly because its output boundary cuts translation diagonals; finite-cutoff HS ratios therefore show lattice/boundary dependence.  That is a representation boundary effect, not part of the continuum theorem.

## 60. Critical stock is exactly the spectral capacity of the Reynolds operator

The continuum isometry has an immediate spectral consequence.  Since `Q_c` is self-adjoint and helicity-odd,

\[
\mathcal Q_c
=\begin{pmatrix}0&B\\B^*&0\end{pmatrix}
\]

relative to the two helicity sectors.  Its nonzero eigenvalues are therefore the pairs

\[
\pm s_j(B).
\]

The Hilbert--Schmidt identity gives

\[
\boxed{
\frac K{64}
=\|\mathcal Q_c\|_{HS}^2
=2\|B\|_{HS}^2.
}
\]

For the dimensionless Reynolds operator `mathscr R_c=Q_c/nu`, write its positive eigenvalues as `lambda_j>0`.  Then

\[
\boxed{
K
=128\nu^2\sum_j\lambda_j^2.
}
\]

Thus the critical stock is **exactly the total squared spectral capacity of the self-generated Reynolds operator**.  It follows that

\[
\boxed{
\|\mathscr R_c\|_{op}
\le
\frac{\sqrt K}{8\sqrt2\,\nu},
}
\]

and the number of positive eigenvalues capable of beating the identity heat satisfies

\[
\boxed{
N_{>1}^+
:=\#\{j:\lambda_j>1\}
\le
\frac{K}{128\nu^2}.
}
\]

At every finite critical stock the dangerous expanding subspace is therefore finite-dimensional, even though the underlying PDE is infinite-dimensional.  Blow-up would require the self-generated operator to create unbounded total Reynolds spectral capacity and, potentially, an increasing number of supercritical channels.

In particular, on any smooth continuum `R^3` interval with

\[
K<128\nu^2,
\]

one has `||mathscr R_c||<1` and therefore

\[
\boxed{K'\le0.}
\]

More quantitatively, combine the exact critical balance with the operator-norm bound:

\[
\boxed{
K'
\le
-2\nu
\left(
1-\sqrt{\frac{K}{128\nu^2}}
\right)M_3.
}
\]

This is a continuum operator form of the small-critical-data regime.  The module only claims the a-priori inequality on a smooth solution interval; importing a classical continuation theorem is unnecessary for the present operator reduction.

The same inequality is simply the Hilbert--Schmidt energy estimate for `Q_c`.  From Sections 49 and 59,

\[
\frac12\frac d{dt}\|\mathcal Q_c\|_{HS}^2
=
\langle\mathcal Q_c,\mathcal Q_c(F_E)\rangle_{HS}
-\nu\mathcal D_{op},
\]

where

\[
\mathcal D_{op}
:=\sum_j\|[D_j,\mathcal Q_c]\|_{HS}^2.
\]

The actual critical source obeys

\[
\kappa(0)
\le
\|\mathcal Q_c\|_{op}M_3
\le
\frac{\|\mathcal Q_c\|_{HS}}{\sqrt2}M_3.
\]

Using `M_3=64 D_op` and `kappa/64=<Q_c,Q_c(F_E)>` gives the closed operator estimate

\[
\boxed{
\frac12\frac d{dt}\|\mathcal Q_c\|_{HS}^2
\le
\left(
\frac{\|\mathcal Q_c\|_{HS}}{\sqrt2}-\nu
\right)
\mathcal D_{op}.
}
\]

This inequality is not the large-data no-escape theorem: once the Hilbert--Schmidt radius exceeds `sqrt(2) nu`, its right side may be positive.  Its significance is structural.  The same operator whose HS norm *is* the critical stock also supplies the only positive Reynolds eigenvalues, while the same translation commutators supply its exact viscous Dirichlet form.

## 61. The midpoint transform is lossless; its eigenvalues alone are not

Let

\[
T:u\longmapsto \mathcal Q_c(u)
\]

be the continuum midpoint transform of Section 59.  The polarized isometry identity

\[
\langle Tu,Tv\rangle_{HS}
=\frac1{64}\langle u,\Lambda v\rangle
\]

implies, with the ordinary `L^2` adjoint on the velocity side,

\[
\boxed{T^*T=\frac1{64}\Lambda.}
\]

On the mean-zero critical space `Lambda` is injective, so `T` is injective and the state reconstructs from its midpoint operator:

\[
\boxed{
u=64\Lambda^{-1}T^*\mathcal Q_c(u).}
\]

Thus the midpoint operator is not a lossy statistic of Navier--Stokes.  On continuum `R^3` it is a **lossless critical Hilbert representation of the entire divergence-free velocity state**.

The image subspace

\[
\mathfrak H_c:=T(\dot H^{1/2}_{div})\subset\mathfrak S_2
\]

is preserved by physical heat because

\[
T(C^2u)=\Delta_{op}Tu,
\]

and it is preserved by the nonlinear source because `T(F_E(u))` is again in the image.  Hence one may rewrite the full critical PDE exactly as an operator-valued semilinear heat equation on `mathfrak H_c`:

\[
\boxed{
Q_t=\mathcal N_c(Q,Q)-\nu\Delta_{op}Q,
\qquad Q=T u,
}
\]

where

\[
\mathcal N_c(Q,Q)
:=T\Big(J_{u(Q)}C u(Q)\Big),
\qquad
u(Q)=64\Lambda^{-1}T^*Q.
\]

The quadratic source is still the same Cartan/Lamb nonlinearity; this is a representation theorem, not a claim that the source has become linear.

There is an essential negative result.  Since every `Q in mathfrak H_c` is self-adjoint and helicity-odd,

\[
\boxed{\operatorname{Tr}Q^{2m+1}=0}
\]

whenever the odd trace is defined.  In particular

\[
\operatorname{Tr}Q^3=0
\]

identically, whereas the actual critical source `kappa(0)` has no fixed sign and is generally nonzero.  An actual Galerkin probe produced `Tr(Q^3)` only at `1e-27` roundoff while `kappa(0)` was `1e-4` scale.

So there is **no closed eigenvalue-only Riccati law** of the form `kappa=C Tr Q^3`.  The spectrum of `Q` measures dangerous capacity, but its nonlinear regeneration depends on the Cartan orientation/translation structure retained by the full operator.  This is exactly why the lossless operator representation matters.

## 62. The entire physical Sobolev hierarchy is one operator Hilbert scale

The heat intertwining in Section 59 is not restricted to the square of curl.  In Fourier variables the midpoint kernel generated by a velocity mode `q` lies on the operator translation diagonal `k-l=q`.  Therefore multiplication of the velocity mode by `|q|^alpha` is exactly multiplication of the operator kernel by `|k-l|^alpha`.  Functional calculus gives

\[
\boxed{
T(\Lambda^\alpha u)
=\Delta_{op}^{\alpha/2}T(u)
}
\]

for every real exponent for which the two sides are defined.

Apply the `1/64` isometry to `Lambda^(s-1/2)u`:

\[
\begin{aligned}
\|\Delta_{op}^{s/2-1/4}\mathcal Q_c(u)\|_{HS}^2
&=\|T(\Lambda^{s-1/2}u)\|_{HS}^2\\
&=\frac1{64}
\langle\Lambda^{s-1/2}u,
\Lambda^{s+1/2}u\rangle.
\end{aligned}
\]

Hence the **master continuum dictionary** is

\[
\boxed{
\|u\|_{\dot H^s}^2
=
64\left\|
\Delta_{op}^{\,s/2-1/4}
\mathcal Q_c(u)
\right\|_{HS}^2.
}
\]

The familiar stocks are therefore not separate objects:

\[
\boxed{
E
=64\|\Delta_{op}^{-1/4}Q_c\|_{HS}^2,
}
\]

\[
\boxed{
K
=64\|Q_c\|_{HS}^2,
}
\]

\[
\boxed{
Z
=64\|\Delta_{op}^{1/4}Q_c\|_{HS}^2,
}
\]

\[
\boxed{
M_3
=64\|\Delta_{op}^{1/2}Q_c\|_{HS}^2.
}
\]

This gives a third, independent characterization of the critical exponent.  `s=1/2` is the **unique physical Sobolev exponent whose stock is plain Hilbert--Schmidt `L^2` energy of the midpoint operator**.  Kinetic energy and enstrophy sit symmetrically at operator powers `-1/4` and `+1/4`; critical dissipation is the ordinary operator Dirichlet level `+1/2`.

Sections 44, 54 and 62 are therefore not three mechanisms:

1. changing physical Sobolev metric changes the symmetric Euler generator by the one hyperbolic functional calculus of `ad_log Lambda`;
2. the same physical heat is the gradient in the shifted metric;
3. after the midpoint transform, those same Sobolev metrics are simply the ordinary Hilbert scale of the one operator Laplacian `Delta_op`.

The apparent hierarchy of PDE norms has collapsed to one heat Hilbert scale around one critical operator energy.

### Moment log-convexity is ordinary interpolation in the operator heat scale

Several inequalities which previously appeared as separate radial-moment facts are now automatic.  For example, operator Hilbert-scale interpolation between powers `-1/4`, `0` and `+1/2` gives

\[
\|Q\|_{HS}
\le
\|\Delta_{op}^{-1/4}Q\|_{HS}^{2/3}
\|\Delta_{op}^{1/2}Q\|_{HS}^{1/3}.
\]

After multiplying by the common factor `64`, this is exactly

\[
\boxed{K^3\le E^2M_3.}
\]

Likewise interpolation at the midpoint between operator powers `0` and `1/2` yields

\[
\boxed{Z^2\le KM_3.}
\]

Thus the radial log-convexity and impedance inequalities are not independent pieces of Navier--Stokes bookkeeping.  They are shadows of the single translation-heat Hilbert scale generated by `Delta_op` after the lossless midpoint transform.
## 63. The critical Reynolds operator is one graded Cartan-current imbalance

The material-current reduction now supplies an operator parent for the physical
strain used throughout Sections 44--62.  Let

\[
\alpha=u^\flat,
\qquad
\beta=d\alpha,
\]

and define the canonical midpoint current operators

\[
\boxed{
q=\nu\delta+\frac12\iota_u,
\qquad
q^*=\nu d+\frac12\alpha\wedge .
}
\]

The exterior Leibniz identity and its adjoint give

\[
\boxed{
q^2=\frac{\nu}{2}(\beta\wedge)^*.
}
\]

Put

\[
L_u=-\nu^2\Delta+\frac{|u|^2}{4}.
\]

Under the three-dimensional Hodge identification, the same positive Hodge--Dirac
square has complementary degree-one and degree-two blocks

\[
\boxed{
H^{(1)}=q^*q+qq^*=L_uI+\nu S,
}
\]

and

\[
\boxed{
H^{(2)}=q^*q+qq^*=L_uI-\nu S.
}
\]

Consequently

\[
\boxed{
S=\frac{1}{2\nu}
\left(H^{(1)}-\star^{-1}H^{(2)}\star\right).
}
\]

Thus the physical strain whose helicity-odd Poisson transform is `Sigma_c` is
itself only the degree imbalance of the same midpoint current complex.  The
critical Reynolds operator of Section 57 can be written conceptually as

\[
\boxed{
\mathscr R_c
=\frac1\nu\Lambda^{-1}
\mathcal P_{\rm sech}
\left[
-\frac1{2\nu}
\left(P\left(H^{(1)}-\star^{-1}H^{(2)}\star\right)P\right)_{\rm odd}
\right]
\Lambda^{-1},
}
\]

where `P_sech` denotes the intrinsic
`sech((1/2) ad_{log Lambda})` multiplier already defined in Sections 44--46.
No new scale filter has been introduced.

The same midpoint current polarizes the Poynting/Gauss law to every test
direction.  For

\[
\mathfrak G_\nu[u;b]
=u\times b-2\nu Cb,
\]

one has

\[
\boxed{
\left(\nabla\cdot-\frac{u}{2\nu}\cdot\right)
\mathfrak G_\nu[u;b]
=\omega\cdot b
}
\]

for every smooth `b`.  On the divergence-free form domain,

\[
\boxed{
4\nu A_u
=P\left(4\nu^2C^2+U_u^*U_u-\mathfrak G_u^*\mathfrak G_u\right)P,
\qquad A_u:=P S,
\qquad U_ub=u\times b.
}
\]

On opposite-helicity blocks the `C^2` term is even and drops out, leaving the
exact critical precursor

\[
\boxed{
4\nu (A_u)_{\rm odd}
=\left[P\left(U_u^*U_u-\mathfrak G_u^*\mathfrak G_u\right)P\right]_{\rm odd}.
}
\]

This is the correct operator-level self-frustration statement.  It is **not** a
negative square after helicity projection: odd projection destroys the simple
order of each Gram term.  Therefore these identities do not imply
`||mathscr R_c||<=1` and do not close the large-data problem by themselves.

They do, however, identify the exact object which a persistent expanding
Reynolds direction must evade.  The mixed Gauss equation gives

\[
\boxed{
\mathfrak G_u^*\mathfrak G_u
\ge
M_\omega^*
\left(-\Delta+\frac{|u|^2}{4\nu^2}\right)^{-1}
M_\omega,
\qquad
M_\omega b=\omega\cdot b,
}
\]

whenever the inverse is defined in the relevant energy space.  Thus a dangerous
helicity-flipping direction must keep avoiding the physical vorticity curvature
at the same Poisson depths through which `Sigma_c` samples strain.

There is also a pressure-free critical consequence which lives in exactly the
same shifted metric as Section 53.  With

\[
G=u\times\omega-2\nu C\omega
\]

and `F_E=P(u cross omega)`, the endpoint-current parallelogram identity gives

\[
\boxed{
K'
=\frac1{2\nu}
\left(
\|\Lambda^{-1/2}F_E\|_2^2
-\|\Lambda^{-1/2}PG\|_2^2
\right).
}
\]

Euler energy conservation forces

\[
\boxed{
\|\Lambda^{-1/2}PG\|_2^2
\ge\frac{4\nu^2Z^2}{K},
}
\]

and therefore

\[
\boxed{
K'
\le
\frac1{2\nu}\|\Lambda^{-1/2}F_E\|_2^2
-2\nu\frac{Z^2}{K}.
}
\]

This does not control the coherent many-triad Euler action on the first term.
Its significance is structural: positive critical action and the compulsory
current tax now live in the **same** `H^{-1/2}` metric.

The combined frontier can therefore be stated without an additional ontology:

> **Can the lossless critical operator sustain an expanding helicity-odd Gram
> imbalance for infinite productive action while its reflected-current factor
> must continually avoid the vorticity curvature floor and the same strain
> writes material transverse heat memory?**

A negative answer requires a dynamical persistence theorem.  Static Gauss or
Schrödinger coercivity alone is insufficient because nonconstant scalar Gauss
tests necessarily enter the exact/pressure sector.  No global-regularity claim
is made here.
