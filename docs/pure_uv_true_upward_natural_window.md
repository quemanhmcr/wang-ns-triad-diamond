# Pure-UV true-upward supply is already a first-shell natural-time cause

> **Scope.** This theorem closes only the pure-UV branch of certified true low-to-high hard-tail supply.  It does not identify high-high circulation with supply, does not re-Hahn a signed shell sum, does not create an output-scale probability, and does not claim 3D Navier--Stokes global regularity.

## 1. Starting physical object

Fix a parent-tail boundary `N>0`.  The radial continuity theorem supplies the actual same-time donor→recipient crossing measure

`Phi_up(N) = M{|k_d| <= N < |k_r|}`.

The common causal work unit is `N dW`, inherited from the parent-tail balance.  Disintegrating an upward atom by the recipient dyadic shell `M=2^j N`, the hard-tail support theorem labels an atom **pure-UV by support** exactly when both interaction parents of the positive recipient satisfy

`|p_1|>M/4`, `|p_2|>M/4`.

The energy donor is one of those two interaction parents and also satisfies `|k_d|<=N`.

No smooth decomposition, Young estimate or locality theorem has entered yet.

## 2. The support geometry collapses the scale law

If `j>=2`, then `N<=M/4`, contradicting the strict pure-UV condition on the donor parent.  Hence every pure-UV upward atom has

`j=1`, `M=2N`.

Thus output-scale selection is not merely inexpensive: **there is no selection to perform**.  The entire pure-UV submeasure already lies on the unique first shell,

`p_scale = 1`, `H_inf(output scale)=0`.

This is a support identity of the actual triad, not an entropy bound.

Because the recipient lies in `(M/2,M]` and one parent is the donor, triad closure also gives both interaction parents in the automatic corridor

`M/4 < |p_i| <= 3M/2`.

The donor itself lies in `(M/4,M/2]`.  The strengthened randomized physical-triad referee intentionally varies the tail boundary inside the full admissible interval and audits this complete donor corridor rather than only the endpoint `|k_d|=N`.

## 3. Smooth cutoff does nothing to the two parents

Let the admissible resolved multiplier be any nonnegative smooth cutoff supported in `B_(M/4)`.  Since both pure-UV interaction parents are **strictly outside** that support,

`V(p_1)=V(p_2)=0`, `h(p_1)=u(p_1)`, `h(p_2)=u(p_2)`.

So unlike the resolved-contact theorem there is no `q/(1-q)` repartition, no mixed `V-h` piece, and no K/S interface owner.  The physical quadratic parent pair is already `h-h`, exactly and cutoff-independently.

## 4. Donor provenance is a sidecar, not a second charge

Canonical positivity was formed earlier on physical recipient edge work.  A positive recipient may inherit same-time donor labels from the cyclic `dW-` kernel.  Before any capacity or owner continuation, all selected donor sidecars for the same physical recipient are therefore coalesced into one `PureUVRecipientSubmeasure`.

Its mass is the corresponding positive submeasure of the existing canonical `dW+`.  Coalescing forgets no donor identity—the donor indices remain sidecars—but the recipient physical work is charged exactly once.  Duplicate donor labels, later Hahn flags, recipient-shell `M` reweighting, output-locality flags and event-depth creation all fail closed in the theorem type.

## 5. Canonical positive work reaches Young only through physical total variation

A cancellation-sensitive shell Hahn law is too small to control gross canonical edge-positive work.  The correct bridge is the already-certified continuum edge law

`dmu_pure <= d|W_pure| <= dA_pure`.

For the full unordered-parent/helicity edge variation, the exact Young prefactor relative to `A_3` is

`4 * (1/2) * (sqrt(2))^3 * C_F = 4 sqrt(2) C_F`,

where `C_F=(2pi)^(-3/2)` is the unitary Fourier convolution factor.  The clean vector pair constant is `4`, and

`sqrt(2) C_F = 0.0897935610625833 < 1`.

Therefore the clean vector Young envelope dominates **gross physical helicity-edge total variation** before any aggregate shell cancellation.  Capacity remains an upper envelope only; it is never promoted to the causal law.

## 6. One natural window

On the unique shell `M=2N`, both parents are at most `R M` with `R=3/2`, and `h=u` on them.  The clean physical work capacity on one shell-natural window `T_M=cM^-2` is therefore

`N W_win <= 3 sqrt(pi) R c N E_global sqrt(mu_win)`

and hence, with `R=3/2`,

`N W_win <= (9/2)c sqrt(pi) N E_global sqrt(mu_win)`.

Let `H_pure` be total pure-UV common work and choose the maximal sliding **physical** `M`-natural window.  Put

`p_time = W_win/H_pure`.

This is a concentration statistic of the actual positive work measure on a fixed physical window length `c(2N)^-2`, not a packet lifetime or analyst time partition.

## 7. Hard-tail owner corollary

If true upward supply is the hard-tail owner at threshold `nu D_tail`, the certified support alternative gives

`H_pure >= nu D_tail/2`

or the already-separate resolved-contact branch wins.  On the pure branch,

`W_win = p_time H_pure >= p_time nu D_tail/2`.

Combining with the physical window capacity yields the direct critical-shell lower

`sqrt(mu_win)/p_time >= nu D_tail / [9 c sqrt(pi) N E_global]`.

There is no `p_scale` denominator because `p_scale=1` exactly.

The resulting critical-shell mass may continue to the existing bounded/integrated critical-shell service theorem.  That later service continuation remains conditional on its own survivor hypotheses; this theorem does not manufacture them.

## 8. Actual Navier--Stokes referee

The numerical referee deliberately reuses the already-certified Fourier--Galerkin hard-tail solver instead of copying it.  The same real divergence-free finite NS trajectory reconstructs canonical cyclic work and true radial supply.  The pure branch is then read from that physical law.

The referee checks representation invariance, exact first-shell support, strict positive distance of both parent frequencies from `M/4`, `p_scale=1`, recipient single charge, and coexistence with the separate resolved-contact phenomenon on the certified deep six-mode trajectory.  Finite-grid evolution does **not** prove the continuum Young bound; it referees the support/cause identities against actual NS dynamics.

## 9. What this closes

The two true-upward support branches are now both physically routed:

- resolved contact: actual smooth `V-h/h-h` repartition, then K/S or contact-HH natural window;
- pure UV: no repartition at all, automatic `M=2N`, `h=u` on both parents, and direct first-shell natural time.

High-high internal circulation remains a different phenomenon and never becomes tail supply.  The common unit remains `N dW` throughout.

## 10. Exact-SHA certification

Exact theorem SHA: `e7b302f3590bbddb83b3d9bd9e8c213b5ada8b4e`.

Three serious gates passed on that exact source SHA:

- dedicated `31661573466` — **success**; focused theorem/dependency tests, `75,000` strengthened physical closed-triad states, and the two-representation actual Fourier--Galerkin NS referee;
- independent audit `31661573459` — **success**; `100,000` independently seeded physical triads plus a longer amplitude-`0.5` actual-NS audit;
- physical-energy causal integration v2 `31661727711` — **success** in `4m02s`; full suite `923` tests; exact `20,000`-episode master stress with worst margin `0.0`; planner `59` nodes = `58` exact-content reuses + exactly one new pure-UV node in shard `5`; all `59` result directories were present and all `58` baseline-matched outputs passed byte/digest verification.

Deterministic stored-tree digests:

- `recorded-results/31661573466/` — `sha256:11236c12f4cffb02b5318aba6a4f04d9c4646ce6dbc5ebbf71857d39188265da`;
- `recorded-results/31661573459/` — `sha256:94a14e023562cfcbc5901e909afbe4d82ba9d9de522a0b074bb798a9faf8ab14`;
- `recorded-results/31661727711/` — `sha256:5c2b52e74529ecdbe80be2b5d457c4a5395b9beefab033a943e1473aef7e65d2`.

The strengthened `75,000` / `100,000` stresses resolved every sampled physical triad and audited `92,544` / `123,099` pure-UV donor atoms.  The donor/shell ratios covered `0.250001213172..0.499998716333` and `0.250000116655..0.499999635558`; `83,990` / `111,688` atoms lay strictly below `0.49`.  Maximum sampled parent/shell ratios were `1.43535063102` and `1.43955108032`, below the exact `3/2` corridor; recipient coalescing and `p_scale` residuals were identically `0`.  The physical edge-total-variation / clean-Young ratio remained the exact `0.0897935610626`.

The dedicated actual-NS referee gave pure common-work cross-FFT spread `2.087e-16`, strict minimum parent margin above `M/4` `2.40312423743`, and `p_scale` residual `0`.  Its certified deep trajectory simultaneously had initial deep-contact work `0.0748690219794` and first-shell pure-UV work `0.039721764731`.  The independent amplitude-`0.5` audit had cross-FFT spread `0`, with coexisting initial deep/pure work `0.00935862774742 / 0.00496522059137`.

Failure/correction lineage is part of the certification:

1. initial candidate `28b4fde1a6a44325d3840fc3244f6a5705741860` placed the pure/contact coexistence assertion on the separate cutoff-7 **pure-support** observation.  Actual NS correctly rejected that fixture demand.  Repair `b28e33e34652f4686cdbd3f1ac9073978b136384` moved coexistence to the certified deep six-mode trajectory where both submeasures actually occur; it retained the assertion and recorded both positive works rather than weakening it;
2. the first randomized stress used `N=|k_d|`, so every donor had `|k_d|/M=1/2`.  After reading the successful artifacts, the experiment was strengthened to choose `N` throughout the exact admissible interval `max(|k_d|,|k_r|/2) <= N < min(|k_r|,2 min(parent radii))`;
3. stress candidate `ec72ac069f701c929c2d7b5a54b0741ee58dcc52` initially recovered interaction parents by comparing child-mode identity objects.  The cyclic closed-triad representation correctly exposed the orientation mismatch.  Final repair `e7b302f...` uses the canonical `recipient_closed_mode_index -> parent_closed_indices` provenance instead.  No theorem inequality, PDE equation, numerical tolerance, sample count, owner threshold, or causal unit was changed by either repair.

This closes the pure-UV true-upward natural-window binding, not mixed genuine-owner recurrence.  There is no Navier--Stokes global-regularity claim.
