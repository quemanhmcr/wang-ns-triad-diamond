# Old-incident heat erosion: ON is not a neutral third regime

## Status

**EXACT_OLD_INCIDENT_HEAT_EROSION__OO_PLUS_ON_DECAY__POSITIVE_NN_CRITICAL_HEAT_SEED_ON_SUPPLIED_SIGNED_GOOD_EPOCH**

The previous theorem showed that repeated old--old heat service decays.  It left old--new (`ON`) edges as an apparent separate interface regime.  For the heat law this separation is unnecessary: the exact Fourier-shell provenance of an increment survives coherent localization.

## 1. Keep the deterministic shell mark where it is exact

The high-strain law is disintegrated first by deterministic orthogonal Fourier shells `P_j`.  Translation increments are Fourier multipliers, so

\[
\boxed{\delta_rP_j=P_j\delta_r.}
\]

Only after this exact shell disintegration do we apply Moyal.  Thus every coherent heat edge retains its eventwise shell `j` exactly.

This shell index is **not** promoted to a persistent material identity.  It is an event-fiber physical mark.  Between events the material identity remains the intrinsic coordinate

\[
\zeta=(L^{-1}X/2,L^Tk),
\]

with the already proved common affine/Kelvin invariance.

The preceding ownership theorem allowed an arbitrary measurable `O` because classification alone needs no frequency hypothesis.  **Capacity routing now specializes `O` to the canonical old reservoir pool already used by `reservoir_pool_erosion`: old material cells are carried together with the exact orthogonal band in which their current reservoir energy is analyzed.**  This is not a new selector.  The heat law was already positively disintegrated by `j`, and refining a positive measure by an existing exact shell mark changes no mass.

At one event, represent this transported old reservoir pool shellwise: its old intrinsic-zeta cells occur only in deterministic shells lying inside the current transported old-frequency envelope.

## 2. Every edge touching old material is old-shell incident

Let `chi_0,chi_1` be endpoint membership in the shell-addressed old material pool.  The exact positive ownership partition is unchanged:

\[
S_{heat}=S_{OO}+S_{ON}+S_{NN}.
\]

But now observe that

\[
1_{old\ incident}=1_{\{\chi_0=1\ \mathrm{or}\ \chi_1=1\}},
\]

so pointwise

\[
\boxed{S_{old\ incident}=S_{OO}+S_{ON}.}
\]

If an endpoint is old, its event shell belongs to the current old-frequency envelope.  Because both endpoints of the heat edge remain in the same exact shell,

\[
\boxed{S_{OO}+S_{ON}\le S_{old\ shell}.}
\]

This is a positive-submeasure statement.  No field is split into old and new pieces, and no STFT frequency coordinate is mistaken for exact Fourier support.

## 3. OO and ON inherit the same heat-specific geometric erosion

For old shells with current upper frequency `M_old`, the first-hit heat theorem gives on `T(N)=cN^-2`

\[
S_{old\ shell}\le c\frac{M_{old}^2}{N}PE_{global}.
\]

At a first high-strain contact the full pre-hit history obeys `K<=1/30`, so Kelvin growth is at most `e^(1/30)`.  On a supplied signed-good material epoch the block frequency advances by more than `8/5`.  Hence

\[
\rho_{old\ incident}
\le \frac58e^{1/15}
<\frac{441}{640}
<\frac7{10}.
\]

Therefore the **entire old-incident heat service**, not just OO, has a geometric capacity.

This changes the interpretation of ON.  A same-time mixed heat edge is physically real, but it need not be renamed a temporal relink event in order to close the capacity ledger.  Because it still touches old material and keeps its exact shell, it is already part of the old-frequency service that the advancing cascade outruns.

A genuine temporal change of material ownership remains a relink/interface event under the existing first-hit theorem.  The present statement simply says that spatial ON heat service is not a third indefinitely reusable regime.

## 4. Finite material age forces NN service

Let

\[
g=\frac12e^{-1/32}.
\]

The high-strain heat theorem states that the critical shell-time set `G` carries at least `g` of total heat service.

Choose

\[
\epsilon=\frac g2=\frac14e^{-1/32}.
\]

Because old-incident capacity decays geometrically, after finite material age it satisfies

\[
C_{old\ incident}\le\epsilon S_*(c),
\]

where `S_*(c)` is the existing high-strain heat lower.  Since every such event has `S_{heat}>=S_*`,

\[
S_{OO}+S_{ON}\le\epsilon S_{heat}.
\]

Exact ownership then yields

\[
\boxed{S_{NN}\ge(1-\epsilon)S_{heat}.}
\]

Numerically `1-epsilon > 0.75`.

## 5. The NN law intersects the critical ancestor law without independence

Both `NN` and `G` are restrictions of the **same positive heat measure**.  Therefore the sharp inclusion--exclusion inequality gives

\[
S_{NN\cap G}
\ge S_{NN}+S_G-S_{heat}.
\]

Using the bounds above,

\[
\boxed{
S_{NN\cap G}
\ge(g-\epsilon)S_{heat}
=\frac14e^{-1/32}S_{heat}.
}
\]

So every sufficiently old high-strain event on a supplied signed-good material epoch has a fixed positive heat sublaw which is simultaneously

- new--new relative to the current old material pool;
- spatially coherent by exact Moyal translation covariance;
- supported on a deterministic resolved shell with critical mass `M_j||P_ju||_2^2>=mu_*`.

No independent pigeonholes are intersected probabilistically; the conclusion is measure algebra on one physical law.

## 6. What remains

This theorem removes both OO and ON as indefinitely reusable high-strain service on a supplied epoch.  The remaining high-strain seam is now concentrated on a **NN-critical coherent heat sublaw**.

What is still not proved is the universal renewal implication:

> a positive NN-critical heat sublaw must either generate a renewed smooth material carrier/epoch satisfying the existing role/moat/service/natural-lifetime hypotheses, or hit an already named source/relink/interface cause.

The theorem does not assign a fixed Moyal mass to one cell, select a packet by argmax, or rename `D_V` as a reset.  Universal epoch/slab supply remains open.  No global-regularity claim is made.
