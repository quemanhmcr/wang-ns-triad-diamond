# Canonical material coherent labels: registration by construction

After asynchronous Duhamel synchronization, every generated causal layer lives on a common physical reference slice. The remaining label problem should not be solved by comparing two unrelated packet decompositions after the fact. Instead define the causal and master ancestry label to be the **same material coherent cell from the beginning**.

## 1. Intrinsic material coordinate

For an affine coherent packet use

\[
\boxed{
\zeta=(L^{-1}X/2,L^Tk)\in\mathbb R^6.
}
\]

Under a common invertible affine/Kelvin map

\[
L\mapsto ML,
\qquad
X\mapsto MX,
\qquad
k\mapsto M^{-T}k,
\]

one has exactly

\[
\boxed{\zeta\mapsto\zeta.}
\]

Thus common low--high material transport is not a label interface. This is the phase-space version of the exact Heisenberg cancellation for moving affine frequency cells and the adjoint Kelvin interaction picture.

## 2. One nested label hierarchy

Fix an origin and a nested dyadic grid in intrinsic \(\zeta\)-space. At depth \(j\), a material cell has width \(h_j\), and its address is the integer vector obtained by division by that width. Every depth-\(j+1\) cell has a unique depth-\(j\) parent.

Use this same address for

- the parent label in the adjoint Duhamel causal pushforward;
- the parent label in Shannon/Rényi reuse;
- the packet/coherent vertex in the Hodge/resistance graph;
- the material ancestry label used by old-pool and cycle bookkeeping.

There is therefore no independent "causal label" which later has to be matched to a "master label".

For a positive Moyal cell energy, refinement is exact:

\[
E(C)=\sum_{C'\subset C}E(C').
\]

Hence

\[
\boxed{\Xi_{\rm refine}=0.}
\]

Splitting a cell to obtain more resolution is not physical relinking.

## 3. Frequency representatives have a summable error

Hodge/helical multipliers still need a representative frequency inside each cell. On the certified common spectral moat, the normalized SGS/Leray/helical symbol has finite Lipschitz constant \(L_*\). The existing sharp-Young freezing theorem gives on one generation

\[
|\Delta T_j|
\le
A_3L_*h_j B_j,
\qquad
A_3=(\sqrt3/2)^3,
\]

where \(B_j\) is the product of the three critical \(L^{3/2}\) role norms.

Choose the nested geometric schedule

\[
\boxed{h_j=h_0\,2^{-j}.}
\]

If \(B_j\le B_*\), then

\[
\boxed{
\Xi_{\rm sym}
\le
A_3L_*B_*\sum_jh_j
\le
2A_3L_*B_*h_0.
}
\]

There is no packet-count factor. The representative resolution may therefore be made as fine as needed while retaining a finite global symbol ledger.

## 4. Covariance representatives are also summable

The material label is not changed by choosing a nearby Gaussian analysis covariance. The coherent covariance theorem gives, for log-SPD distance \(\delta_j\),

\[
\Xi_{{\rm cov},j}
\le
\frac{\delta_j}{\sqrt2}E_j.
\]

Choose

\[
\boxed{\delta_j=\delta_0\,2^{-j}.}
\]

Using \(E_j\le E_{global}\),

\[
\boxed{
\Xi_{\rm cov}
\le
\sqrt2\,\delta_0 E_{global}.
}
\]

A large physical covariance deformation is **not** hidden in this mesh change. It remains in the existing high-strain/source/fresh-relink branches. The estimate above concerns only the choice of nearby coherent analysis representative.

## 5. What is now free and what remains a cost

The construction separates four operations:

1. common Kelvin/affine motion — exact gauge, zero cost;
2. nested dyadic refinement — exact Moyal partition, zero cost;
3. frequency/covariance representative choice — summable representation \(\Xi\);
4. actual change of selected material cell — physical Moyal switch/relink/backflow/fresh currency.

Thus causal Duhamel, Rényi reuse and Hodge/resistance/holonomy can use one canonical material label by construction. No new relabeling defect is introduced.

## 6. Current downstream status

This theorem closes the **representation-level material label registration**.  Downstream, `material_label_carrier_quotient.md` further shows that a pure material-label or selected-family bookkeeping change does not create a second carrier impulse when the smooth carrier/dual role is unchanged.  The remaining single-ledger problem is the final continuum master assembly combining:

- the already summable frequency/profile/spatial-window errors;
- the covariance representative schedule above;
- actual physical cell switches, which are positive charged events rather than approximation errors;
- flat erosion, coherent stopping, causal Rényi reuse, source and dissipation currencies;

into one global master telescope without duplicate charging.
