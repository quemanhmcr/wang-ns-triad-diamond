# Renyi causal reuse: the transfer-weighted causal bridge reaches existing currencies

The Shannon causal reuse identity proves that deep ancestry loses a fixed amount of physical transfer information through parent merging, but the master Bellman/cycle modules are naturally expressed in collision probabilities.  The Renyi-2 version gives an exact multiplicative bridge.

Let `w` be the normalized causal-transfer law on child packets in one synchronized layer.  Duplicating every event into the two structural parent roles produces slot atoms of weight `w/2`.  Their collision probability is

\[
\boxed{Q_{slot}=Q_{child}/2.}
\]

The factor `1/2` is the exact cost-free two-parent-role baseline and must not be charged.

Push the slot law through the physical parent-packet/coherent-cell label map.  The atomic-to-component collision identity gives

\[
\boxed{
Q_{parent}=Q_{child}/2+R_{hidden},
}
\]

where

\[
R_{hidden}
=\sum_P\sum_{a\ne b:\,f(a)=f(b)=P}p_ap_b
\]

is the physical transfer-weighted mass of distinct causal parent slots sharing the same parent label.

Define

\[
\boxed{
\theta={2R_{hidden}\over Q_{child}}.
}
\]

Then

\[
\boxed{
Q_{parent}=\frac12Q_{child}(1+\theta).
}
\]

## Multiplicative causal action

For a one-terminal synchronized ancestry of depth `L`, `Q_L=1`, so

\[
\boxed{
\sum_{j=0}^{L-1}\log(1+\theta_j)
=L\log2+\log Q_0.
}
\]

Since the root law is supported on at most `n_0` coherent cells,

\[
Q_0\ge1/n_0.
\]

The signed-good scale / coherent energy theorem gives

\[
n_0\le{P E_{global}N_{base}\over\eta}
\left(\frac{25}{24}\right)^L,
\]

hence

\[
\boxed{
\sum_j\log(1+\theta_j)
\ge
L\log\frac{48}{25}
-\log\frac{P E_{global}N_{base}}{\eta}.
}
\]

Therefore the same clean depth condition

\[
L\log\frac{36}{25}>
\log\frac{P E_{global}N_{base}}{\eta}
\]

forces one layer with

\[
\boxed{\theta_j>1/3.}
\]

## Existing-currency route at a Renyi-rich layer

At such a layer,

\[
R_{hidden}=\frac{\theta_j}{2}Q_{child}.
\]

If

\[
H_{2,child}=-\log Q_{child}<\log2,
\]

then `Q_child>1/2` and

\[
\boxed{R_{hidden}>1/12.}
\]

This is already a physical transfer-weighted parent-slot reuse pair mass.

If instead

\[
H_{2,child}\ge\log2,
\]

apply the existing atomic-to-ancestry collision chain to the child law.  With `alpha=1/2`, either

\[
\boxed{H_{2,ancestry}\ge\frac12\log2}
\]

or

\[
\boxed{
R_{same\ ancestry}
\ge2^{-1/2}-1/2>1/5.
}
\]

Thus sufficiently deep binary causality has a transfer-weighted finite alternative entirely in existing currencies:

\[
\boxed{
\text{component/Bellman entropy}
\ \lor\
\text{same-ancestry pair/cycle}
\ \lor\
\text{parent-slot reuse pair}>1/12.
}
\]

No cost is assigned to the mere existence of the two parent roles.  Only collision amplification above the exact `1/2` baseline is reuse.

The remaining continuum bridge is now sharply localized: build the exact recursive selected packet/coherent coefficient equation and its designated high--high source on canonical material labels.  The physical-energy causal bridge supplies the layer weights directly from actual positive child work, while Duhamel supplies the same-time quadratic parent support; therefore no raw `dGamma=dT` identification is required.  All rejected cross/interface work is still charged once to `Xi`.
