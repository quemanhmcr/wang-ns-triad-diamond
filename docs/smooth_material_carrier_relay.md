# Smooth material-carrier relay: hard only when nature interacts

## Status

**EXACT_COMMON_SLICE_SMOOTH_MATERIAL_CARRIER_RELAY__HARDEN_ONLY_AT_PHYSICAL_GENERATION__UNIVERSAL_SERVICE_REENTRY_REMAINS**

The previous frontier asked whether a registered parent coefficient at a common slice could be turned into a new hard event role.  That question still contains an observer-imposed step.  A hard role is physically privileged at an actual nonlinear transfer event because there the quadratic work can be disintegrated exactly.  A generic intermediate time has no such event and need not be forced to carry a fresh hard boundary.

The more intrinsic recursion alternates two objects with different jobs:

- a **hard event role** exists only at a physical interaction where work is read;
- a **smooth material carrier** transports the selected material content between such events.

## 1. The common-slice coefficient already belongs to the same carrier

Let

`w(t)=Q(t,D)u(t)`

be the smooth scalar envelope attached to a hard parent role at its physical child-transfer event.  The exact adjoint theorem registers at the common slice `s`

`z(s)=<w(s),psi(s)>`,

with `|z(s)|>=|z(t)|/4` on the no-obstruction branch.  Cauchy--Schwarz gives

`||w(s)||_2^2 >= |z(s)|^2/||psi(s)||_2^2`.

Nothing in this implication asks for a new Fourier cell at `s`.  The large coefficient is already a lower bound for the energy of the same smooth carrier.

## 2. Re-anchoring is a gauge composition, not a material event

The coherent material address is

`zeta=(L^-1 X/2,L^T k)`.

Under a common affine map `M`,

`L -> ML`, `X -> MX`, `k -> M^-T k`,

and `zeta` is exactly fixed.  If the carrier is propagated first by `M1` and then by `M2`, the result is identical to the single map `M2 M1`; the same `zeta` is obtained at every stage.

Thus taking the current carrier at a common slice as the anchor for the next local affine chart is only a change of coordinate origin.  It is not a material relink, not a fresh ancestry event and not another representation `Xi`.  Nonaffine deformation remains the already existing `K_coh`/strain/interface physics; re-anchoring does not erase its accumulated action.

## 3. The previous energy gate can act on the smooth carrier

On any renewed previous slab on which the already proved outer-role hypotheses hold, the same carrier satisfies

`(dt+L_V-nu Delta)w = -Q P div(h tensor h)+R_Q`.

Its energy therefore has exactly the existing physical three-way gate: inherited energy, classified residual/strain service, or positive HH generation.  No hard projector is required merely to formulate the carrier energy balance.

The exact smooth-carrier energy effect is `Q^2`.  Consequently the native
interface contribution is not the work of `R_Q` in isolation.  It is

`<u,dot(Q^2)u> - 2 Re<Q^2u,L_Vu>`,

equivalently the `R_Q` work minus the diagonal `L_V` work of `Q u`.  The smooth
quadratic-carrier theorem completes `Q` by a square partition and splits this
native interface into conservative relink plus existing strain.

A possible concern is that the HH work in the `Q` equation is weighted by the smooth symbol, whereas the master causal law is actual physical transfer.  The concern disappears at the Hahn-measure level.

## 4. Smooth-carrier generation is dominated by actual positive physical work

Let `r(t,xi,alpha)` be the signed density of actual HH child-energy work, including the parent-pair mark `alpha`.  Let the real scalar carrier multiplier obey `0<=q<=1`.  The HH contribution to carrier energy is weighted by `q^2`.

For every event set,

`[ int q^2 r ]_+ <= int q^2 [r]_+ <= int [r]_+`.

The first inequality is simply positive variation of a signed measure; the second uses `q^2<=1`.

Consequently a lower bound for positive HH generation of `Q u` forces at least as much fine positive **physical** HH work on the same support.  `Q` never becomes a new probability law.  The causal measure remains the positive Hahn part of actual NS work.

When the fine event measure is grouped into the repository's finite hard cells, the already-certified smooth-symbol/freezing `Xi` controls the variation of the physical multiplier on a cell.  That charge belongs to the interaction event and is already in the canonical representation ledger; the carrier relay itself adds none.

## 5. Hard roles are created by the interaction, not by the observer

At an actual HH event choose the deterministic disjoint Borel Fourier/helical resolution `{P_C}` already used by event-role registration.  For a physical forcing decomposition `F=sum_alpha F_alpha`, orthogonality gives exactly

`2 Re<w,F> = sum_(C,alpha) 2 Re<P_C w,P_C F_alpha>`.

Thus the hard event labels are read from the nonlinear work at the instant at which they are physically meaningful.  Their positive Hahn atoms supply the parent/child causal law.  The hard projectors are not differentiated and are not assumed to persist.

This gives the natural alternation

`hard interaction event`

`-> smooth material carrier`

`-> common-slice relay of the same carrier`

`-> smooth material carrier`

`-> next actual HH interaction`

`-> hard interaction event`.

There is no intermediate `argmax` packet, no chattering best cell and no theorem-name choice.

## 6. What this removes, and what it does not

This theorem removes **hard-event reselection at the common slice** as a necessary recursive bridge.  The correct common-slice object is the same smooth material carrier, whose energy is already controlled by the registered coefficient.

It does not allow the carrier to be propagated backward through an arbitrary interval.  A renewed previous slab must still satisfy the exact scale/moat, service, low-strain/coherent-deformation and natural-lifetime hypotheses, or else exit through one of their already named physical causes.  Proving that these slabs cover every recursive continuation route is the remaining continuum problem.

A large coefficient impulse is only such an exit locator.  It must reenter the
physical-energy gate on the observed carrier interval; the impulse magnitude is
not itself residual work.  Only actual positive native interface work may route
to relink/strain ownership.

So the frontier is no longer “manufacture a new hard parent at the common slice.”  It is:

> prove **universal physical slab renewal** for the same material carrier, with every failure routed to an existing first-stop cause.

No global-regularity claim is made.

## Certified extension: natural horizons do not interrupt the relay

The certified same-carrier checkpoint-segmentation theorem applies the relay principle to
full-natural service horizons.  The exact carrier equation does not expire merely
because one `cA^-2` interval has ended.  If no physical stop fires there, the
natural endpoint is only an analysis checkpoint and the event search continues on
the same `Q_A` carrier.

The first-hit data therefore remain cumulative from the original physical event:

`K_A[s,t]`, `|I_role-interface[s,t]|`, `|I_HH[s,t]|`,

with the same terminal coefficient setting the two coefficient faces.  Re-anchoring
a local affine chart may change coordinates, but it cannot reset those observables.
The strain action is monotone; coefficient-impulse magnitudes may decrease by
complex phase cancellation and are never summed per checkpoint segment or used as
work.

Consequently an accumulation of no-event natural horizons at an interior smooth
time is either an existing first-stop face at the limit or is crossed by the same
carrier.  Hardening into a fresh event role requires an actual physical event.
This extension is certified on exact SHA `bd404d8fd79336e094015f8a9463bfef761e9d2d` by dedicated run `31454546606` and full integration run `31454546590`.
