# Generic critical shell to own-scale coherent-service re-entry

## Why this is the right abstraction

High strain was only one mechanism for producing a shell with critical physical mass.  The carrier/service argument itself does not know why that shell is critical.  Its natural input is therefore just

\[
M\|P_Mu(t)\|_2^2\ge\mu_0>0.
\]

No packet, coherent-cell selector, material label, dissipation probability, or source channel belongs in the core theorem.

Set

\[
A=\frac34M.
\]

The hard shell becomes

\[
\frac23A<|\xi|\le\frac43A.
\]

Choose the existing scalar smooth role \(Q_A\) equal to one on this entire shell.  With

\[
\psi(t)=\frac{P_Mu(t)}{\|P_Mu(t)\|_2},
\]

we have exactly

\[
\langle Q_Au(t),\psi(t)\rangle=\|P_Mu(t)\|_2,
\]

hence

\[
\boxed{A|z(t)|^2\ge\frac34\mu_0.}
\]

## Three native first stops, and no material stop

Before renewed service exists, material ownership is intentionally unassigned.  Inspect backward with only

1. renewed strain action, hitting \(1/30\);
2. role-interface coefficient obstruction, hitting \(|z(t)|/4\);
3. HH-regeneration coefficient obstruction, hitting \(|z(t)|/2\).

The exact selected-role coefficient identity is

\[
z(t)=z(s)+I_{HH}[s,t]+I_{interface}[s,t].
\]

Exact ties remain unsplit.  A renewed strain hit is the already named
critical-dissipation recursion at scale \(A\).  Interface and HH coefficient
hits are interval locators: they reenter the physical-energy gate, which alone
assigns inheritance, actual interface work or actual HH work ownership.

The first-hit record also contains the maximum backward elapsed time actually observed.  A theorem caller may not claim a full natural survivor or the absorbing \(t=0\) boundary unless the monitor path covers the whole required interval.  This makes the continuum interface explicit rather than relying on a caller convention.

If no hit occurs on a full natural corridor, every prefix satisfies

\[
|z(s)|>\frac14|z(t)|,
\]

so

\[
\boxed{A|z(s)|^2\ge\frac{3}{64}\mu_0.}
\]

## Coefficient persistence becomes physical carrier service

The registered affine/Kelvin/viscous analysis dual has the already-certified natural-window cost

\[
\|\psi(s)\|_2\le J\|\psi(t)\|_2,
\]

where the nonaffine/full-transport mismatch is not hidden in \(J\): it is the role-interface coefficient obstruction above.  On a full no-hit slab that obstruction remains below its coefficient face; no impulse magnitude has been counted as work.  Cauchy gives, at every point of the slab,

\[
\boxed{
A\|Q_Au(s)\|_2^2\ge\frac{3\mu_0}{64J^2}.
}
\]

Because the transported role remains in a fixed annulus away from zero frequency, the intrinsic \(A\)-scale heat defect is bounded below by a fixed fraction of this carrier energy.  The Gaussian displacement is unbounded, so use the universal estimate

\[
\|\delta_rw\|_2^2\le4\|w\|_2^2
\]

and the already Arb-certified radius-three truncation.  Writing \(q_b\) for the clean retained annular heat fraction, at every slab time there exists an actual

\[
|r_s|\le\frac3A
\]

with

\[
\boxed{
A\|\delta_{r_s}Q_Au(s)\|_2^2
\ge
Y_{shell}(\mu_0)
:=q_b\frac{3\mu_0}{64J^2}>0.
}
\]

The normalized full-natural-slab bounded heat service is at least \(cY_{shell}(\mu_0)\).

Only after this positive service exists do we apply exact Moyal and read material OO/ON/NN from the service law's actual two intrinsic endpoints.

## Supplier 1: any certified resolved-dissipation lower

Suppose another theorem has already proved on a natural block

\[
D_V\ge D_0>0.
\]

Put

\[
\mu_0=\frac{D_0}{c}.
\]

For the dyadic resolved shells, the portion with

\[
M_j\|P_ju\|_2^2<\mu_0
\]

costs at most

\[
D_{bad}\le\frac{c\mu_0}{2}=\frac{D_0}{2}.
\]

Therefore at least

\[
D_V-\frac{D_0}{2}\ge\frac12D_V
\]

of the actual resolved-dissipation mass lies on shells that feed the generic theorem.  Normalizing this restriction is only diagnostic shell-time sampling.  It is not the positive HH child-energy law and does not enter Shannon or Rényi causality.

The high-strain route is the exact specialization \(D_0=32\pi^2/(75c)\).

## Supplier 2: dominant fresh coherent service

The existing coherent-increment theorem gives on the dominant fresh branch

\[
S_{fresh}\ge\frac Y4,
\qquad
s_e\ge\theta S_{fresh},
\]

and local Moyal capacity gives

\[
M(E_C+E_{C-r})\ge\frac{s_e}{2}
\ge\frac{\theta Y}{8}.
\]

Each of the two cell energies is bounded by the full shell energy, hence

\[
E_C+E_{C-r}\le2E_{shell}.
\]

Thus

\[
\boxed{M E_{shell}\ge\frac{\theta Y}{16}.}
\]

For the canonical \(	heta=1/4\), the whole shell has

\[
M E_{shell}\ge\frac Y{64}.
\]

This is enough to enter the generic shell theorem.  It is not an assertion that either individual coherent cell has a scale-independent mass floor or that a packet persists.

## Exact high-strain specialization

With

\[
\mu_*=\frac{32\pi^2}{75c^2},
\]

the generic theorem gives

\[
A|z(t)|^2\ge\frac34\mu_*=\frac{8\pi^2}{25c^2},
\]

and after the quarter coefficient gate

\[
A|z(s)|^2\ge\frac{3\mu_*}{64}=\frac{\pi^2}{50c^2}.
\]

These are exactly the constants in the already-certified high-strain short route and annular-service theorem.

## Scope

This theorem is deliberately shell-local.  It proves that a critical shell can enter an own-scale coherent-service episode or an already named first stop.  It does **not** invent scale progress relative to whatever previous block supplied that shell.  High strain supplies strong scale separation independently; other source/fresh-cluster suppliers must use their own certified scale geometry.

Pressure/source routing, pure material-label transparency, and the final continuum master assembly remain separate problems.  No Navier--Stokes global-regularity conclusion is asserted.
