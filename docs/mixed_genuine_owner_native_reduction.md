# Mixed genuine-owner recurrence: native-owner reduction after material factorization

Status: **DRAFT ARCHITECTURAL CONSEQUENCE — depends on `material_service_native_owner_factorization.md`.**

The current certified frontier lists actual HH generation, strain/deformation, source/SGS/viscosity, independent service, material/new ancestry, high-tail work, and causal reuse/Bellman endpoints as possible genuine recurrence mechanisms. Several of those names describe downstream manifestations of a smaller set of PDE roots.

The material factorization candidate makes one reduction exact:

\[
\boxed{
\text{material/new ancestry is provenance on a native physical law, not a new root or event.}
}
\]

Thus a path should first be quotiented by same-event/sidecar operations and only then inspected for recurrence.

## Compressed owner word

Given an event path, erase from recursive depth:

1. same-carrier membership rereading;
2. selected-family `R_switch` boundary sidecars;
3. OO/ON/NN classification of an already-positive service law;
4. gauge-quotiented smooth `K_phys` donor relays;
5. service/checkpoint consequence layers already certified as zero-depth relays.

For every erased ON/NN label, retain its material provenance on the **same** physical law and inherit its pre-material native-owner provenance by positive restriction. No replacement event is created.

A nominal sequence

`HIGH_STRAIN -> MATERIAL_RELINK -> HIGH_STRAIN`

is therefore still one consecutive high-strain epoch if the middle item is only membership/family/restriction/K-phys provenance. If the middle observation carries an independent source, strain, or actual-work event, that native event — not the material label — is what breaks the epoch.

The identical rule applies to a signed-good generated-HH epoch.

## Why this matters

The two certified pure-epoch telescopes were deliberately broken by every genuinely independent physical event. They should not be broken by vocabulary attached downstream to an already-existing law. Native factorization prevents material bookkeeping from manufacturing the very alternation which made the mixed recurrence problem look larger.

The unresolved mixed problem should therefore be reformulated on the smaller alphabet

\[
\boxed{
\text{actual nonlinear work/high-tail}
\;|\;
\text{strain/dissipation}
\;|\;
\text{objective source/service roots}
\;|\;
\text{causal reuse/transfer cost}
}
\]

with exact ties kept joint and with no scalar exchange rate between heterogeneous currencies.

This note does not assert that the reduced alphabet has already been globally telescoped. Its purpose is to ensure that the next attack is on PDE-native roots only.
