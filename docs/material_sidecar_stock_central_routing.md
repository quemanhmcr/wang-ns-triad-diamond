# Material-sidecar inherited-stock central and joint-stop routing

Exact theorem SHA: `376fd52e1c891c774b70cf339b192bea7045107f`.

> **Later certified refinement (`d6ee03d5b9b7a82d11a2259c5dc0b8ae2ac945ab`):** this Phase-B theorem correctly proved that `R_switch` is a non-event sidecar, but its compatibility statement that an independently inserted raw `PhysicalCause.MATERIAL_RELINK` remains recursive was only a placeholder for unresolved native physics.  The native material-service causal quotient now requires independent PDE evidence before such a material-state locator can enter canonical recursion; a bound smooth `K_phys` component is conservative smooth-role flux, while source/service/strain/actual-work/role-change owners retain their own identities.

## The physical seam

The inherited-energy theorem says that, on one certified no-first-stop physical Fourier--helical carrier, the earlier energy `E0` can be between-time stock rather than newly generated work.  Phase A then separated material sidecars from that stock:

- intrinsic material-membership rereading is zero-charge provenance;
- a selected-family change carries the exact Moyal boundary energy

`R_switch = sum_{C in S_old Δ S_new} E_C`;

- `R_switch` can be positive even when the coherent state is literally unchanged and every cell increment is zero.

The remaining question was whether central/joint first-stop routing would nevertheless turn that sidecar into recursive event depth.

## Central master theorem

A sidecar-bearing `material_energy_inheritance` branch may now quotient the inherited-stock **component** exactly when both typed objects are supplied:

1. the certified `SameCarrierInheritedEnergyRelayCertificate` for the physical carrier; and
2. the exact Phase-A `MaterialSidecarStockDecomposition` belonging to that same certificate.

The binding rechecks carrier identity, inherited stock mass `E0`, exact material-event set, selected-family `R_switch`, and the one-stock-charge/no-work/no-`K_phys` invariants.

The resulting central route has no recursive owner bundle for the inherited stock.  It carries:

- exactly one `same_carrier_inherited_physical_energy_stock` relay;
- zero-charge membership provenance when membership is reread;
- the positive selected-family Moyal boundary currency when a selected family changes.

Nothing is added to `dW`; no stock copy is made per sidecar; no second coefficient impulse appears; no later Hahn split is performed.

If the sidecar-bearing inherited-stock certificate is present but its Phase-A decomposition is absent, the pre-existing fail-closed behavior remains: the unresolved whole branch stays event-facing.  Untyped ancestry is therefore never silently erased.

## Joint-stop theorem: physical stop first, sidecar second

The certified core `joint_causal_stop_projection.py` is deliberately **unchanged** by this theorem.

Instead, `material_sidecar_joint_stop_projection(...)` is a typed wrapper.  Its order is rigid:

1. only independently witnessed physical `CauseHit`s and already-certified internal hits are passed to the core joint first-time law;
2. the core computes first time, exact physical tie set, currency set, and terminal/recursive disposition;
3. only after that result exists is the material sidecar relay attached beside it.

Thus the Moyal boundary sidecar does not enter `_first_joint_set` at all.  In particular, `R_switch>0` by itself cannot create a causal stop.  It cannot move first time, join a heterogeneous physical tie, change terminality, or mint a recursive child.

This is a type/routing theorem, not an estimate.

## Genuine material service remains genuine

`PhysicalCause.MATERIAL_RELINK` is **not** globally quotiented away.  It denotes an independently witnessed material/service physical cause on the existing recursive route.

When a true `MATERIAL_RELINK` hit and a selected-family Moyal boundary sidecar occur at the same observation, the result contains both, but in different slots:

- `MATERIAL_RELINK` remains in `joint_physical_causes` and remains `RECURSE_CRITICAL`;
- `R_switch` remains non-event boundary provenance beside the physical projection.

Likewise an exact source/strain/material tie remains the same unsplit three-cause physical tie.  No fractional ownership is introduced to make room for the sidecar.

Therefore the theorem removes only the previously ambiguous **selected-family accounting boundary** from generation lineage.  It does not declare material physics transparent.

## Actual Navier--Stokes referee

No artificial material PDE is introduced.  The routing theorem is exact representation/causal algebra; the physical carrier ontology is independently checked with the already-certified dealiased Fourier--Galerkin Navier--Stokes trajectory on physical helical mode `(7,6,5)`, helicity `+1`, at FFT representations `24,28`.

Dedicated amplitude `1`:

- stock `1 -> 1.00240956249`;
- gross nonlinear `dW+ / dW- = 0.0100088487847 / 0.000991283878665`;
- viscous dissipation `0.00660800248318`;
- maximum modal continuity residual `6.253e-11`;
- maximum global energy-balance residual `2.963e-12`.

Independent opposite-phase amplitude `0.5`:

- stock `0.25 -> 0.245853600709`;
- gross nonlinear `dW+ / dW- = 0.000182591040316 / 0.00187455879673`;
- viscous dissipation `0.00245443154604`;
- maximum modal continuity residual `4.761e-11`;
- maximum global energy-balance residual `2.614e-11`.

The same physical mode therefore continues to exhibit real nonlinear exchange and viscosity while the sidecar routing is changed.  The theorem changes causal typing, not Navier--Stokes dynamics.

## Exact-SHA certification

On executable SHA `376fd52e1c891c774b70cf339b192bea7045107f`:

- dedicated `31682060268`: **success**; `60` focused/dependency tests, `17` physical-cause/witness regressions, `75,000` typed central/joint sidecar-stock states, and actual NS referee;
- independent audit `31682060264`: **success**; `41` independent tests, `100,000` typed states, and opposite-phase actual NS audit;
- physical-energy causal integration v2 `31682128984`: **success**; `956` tests, master `20,000` episode traces with worst margin `0.0`, `62` integration nodes = `60` exact-content reuse + `2` execute on shards `4,5`; all `62` outputs present and all `60` reused outputs byte/digest verified.

Dedicated `75,000` stress:

- membership / selected-family / mixed: `50,000 / 50,000 / 25,000`;
- positive Moyal boundaries: `50,000`;
- transplanted decomposition rejections: `6,819`;
- recursive-generation events created: `0`;
- sidecars promoted to physical hits: `0`;
- stock-clone violations: `0`;
- joint stops carrying sidecars after physical classification: `75,000`;
- sidecar-only no-stop rejections: `4,412`;
- genuine material-service hits preserved: `15,000`.

Independent `100,000` stress gives corresponding counts `66,667 / 66,666 / 33,333`, `66,666` positive boundaries, `9,091` transplant rejections, zero recursion/promotion/clone violations, `100,000` joint projections, `5,883` sidecar-only no-stop rejections, and `20,000` preserved genuine material-service hits.

The CI-v2 planner originally showed that mutating the core joint-stop module would invalidate `13` integration nodes.  Before any candidate was committed, the theorem was refactored into the typed wrapper above; core joint-stop stayed unchanged and the exact valid blast radius became only `2` executed nodes.  This is CI economy, not a physical theorem.

No global-regularity claim is made.
