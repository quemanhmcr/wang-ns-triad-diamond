# All resolved pressure-pair epochs descend finitely

Status: **DRAFT THEOREM CANDIDATE — independent PR, not certified, not merge-ready.**

The quarter-dominant pressure-pair telescope has a stronger native completion. The pressure entropy is not needed at all.

The key is to combine two different statements which the pressure theorem already proves for the same physical owner:

1. the **actual positive pair source law** has a lower;
2. the **absolute pair capacity** has a pointwise upper.

Capacity remains only an envelope. It never becomes the cause, a probability, a charge, or a reset resource.

## 1. Actual positive source versus the canonical pair envelope

For the resolved pressure owner at block frequency `N`, let

\[
R_{pair}=\sum_{a\le b}\int[p_{ab}]_+\,d\tau,
\qquad d\tau=N^2dt.
\]

The exact positive pressure split gives

\[
\boxed{R_{pair}\ge \Sigma_P/2}
\]

whenever the resolved pair law is an owner.

Independently, the canonical hard-shell pair theorem proves at each scaled time

\[
\boxed{
\sum_{a\le b}\operatorname{cap}_{ab}
\le \frac{N\|V\|_2^2}{2560},
\qquad V=S_{N/4}u.
}
\]

Since `[p_ab]_+<=|p_ab|<=cap_ab`, `|S_{N/4}|<=1`, the source horizon has scaled length at most `c`, and

\[
\|u(t)\|_2^2\le E_{global},
\]

we obtain on **this one actual source episode**

\[
R_{pair}
\le \frac{N}{2560}\int\|V\|_2^2d\tau
\le \frac{cNE_{global}}{2560}.
\]

Combining the actual positive lower with the envelope upper gives

\[
\boxed{
N\ge N_{min}:=\frac{1280\Sigma_P}{cE_{global}}.
}
\]

If every qualifying pressure owner carries the objective source floor

\[
\Sigma_P\ge\sigma_*:=\frac{A_*}{4c},
\]

then every resolved pressure-pair **parent event** satisfies

\[
\boxed{
N\ge \frac{320A_*}{c^2E_{global}}.
}
\]

This is the crucial rigidity law. A fixed amount of actual positive resolved pressure source cannot be produced at arbitrarily small parent frequency while the total kinetic energy remains bounded, because the derivative-suppressed pressure-pair capacity collapses like `N E_global`.

## 2. The same owner has genuine one-quarter scale progress

The pair law lives in

\[
V=S_{N/4}u.
\]

Its attained maximal physical unordered pair exposes an actual hard `u` shell at one of its two resolved frequencies. Thus the certified supplier geometry is

\[
\boxed{N_{next}\le N/4.}
\]

This is the pressure-pair scale theorem itself, not the generic hard-shell carrier registration `A=3M/4`. The latter remains an analysis registration only.

## 3. Dominant and diffuse pressure-pair epochs both terminate

Take a consecutive epoch of resolved pressure-pair first-stop owners, with each selected physical child shell supplying the next event's incoming frequency. If there are `L` such source events with parent frequencies

\[
N_0,N_1,\ldots,N_{L-1},
\]

then

\[
N_j\le 4^{-j}N_0.
\]

But every one of those **parent events** must also obey `N_j>=N_min`. Therefore

\[
4^{L-1}\le \frac{N_0}{N_{min}},
\]

and

\[
\boxed{
L\le
1+\left\lfloor
\log_4\!\left(\frac{N_0}{N_{min}}\right)
\right\rfloor.
}
\]

Under the objective owner floor,

\[
\boxed{
L\le
1+\left\lfloor
\log_4\!\left(
\frac{N_0c^2E_{global}}{320A_*}
\right)
\right\rfloor.
}
\]

The remarkable point is that **`q_max` and `H_2^{pair}` have disappeared from the count**. A diffuse pair law may make the individual child shell weak, but it cannot evade the parent-frequency floor because the total positive resolved pair source still has to fit under the same absolutely summable pair envelope.

Hence the theorem includes both dominant and diffuse resolved pressure-pair recurrence.

## 4. Why this does not violate the capacity rule

The project rule says native capacity is never causal probability, work currency, reset resource, or recurrence budget. This theorem respects that rule exactly.

We never say capacity caused the event. The event is already owned by the actual positive pressure source law `R_pair`.

We never normalize capacity into a probability.

We never sum `N||V||^2/2560` across recursive events or debit it from a global account.

We use the capacity only where it is native: as a **pointwise upper envelope on the already-existing source law inside one event**. That local upper plus the actual source lower implies a lower bound on the event's physical frequency. The finite recurrence count then comes from the separate supplier geometry `N_next<=N/4`.

So the telescope is geometric:

\[
\text{actual source lower}
+\text{local capacity upper}
\Rightarrow\text{parent frequency floor},
\]

\[
\text{pressure support}
\Rightarrow\text{quarter-scale descent}.
\]

No capacity currency is created.

## 5. Relation to pressure entropy

The native entropy-shell law

\[
\mu_{child}e^{H_2^{pair}}\ge320\Sigma_P/c
\]

remains true and useful for shell strength. But `H_2^{pair}` is explicitly not a child-energy probability and not a transfer-cost fate. This theorem does not use it at all.

The earlier quarter-dominant descending theorem is therefore a sharper special case, not the full recurrence closure. The absolute-pair-envelope argument closes the eventually-pure **resolved pressure-pair class itself**.

## 6. Source frontier after this theorem

Inside the objective source owner:

- local quadratic / viscosity -> existing `D_V` / strain-critical root;
- resolved pressure-pair owner, dominant **or diffuse** -> finite descending epoch by this theorem;
- selected-interface `Xi` -> existing transfer cost;
- SGS high-frequency owner -> existing physical hard-tail route;
- old-pool capacity -> inherited reservoir state, never a new causal currency;
- the genuinely hard surviving source mechanism is now concentrated in **pressure-SGS / fresh-SGS coherent service renewal**, especially refinement-invariant fresh critical-scale shells with no directional scale theorem.

Arbitrary alternation between source/service, strain/dissipation, and actual HH/hard-tail work is still not closed by this note.

No Navier--Stokes global-regularity claim is made.
