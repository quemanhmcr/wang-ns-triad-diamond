# NN-critical carrier seed: backward natural first-stop corridor

## Status

**EXACT_NN_SEED_BACKWARD_NATURAL_FIRST_STOP_CORRIDOR__ONE_QUARTER_CRITICAL_SURVIVOR_OR_NAMED_STOP__MATERIAL_ATTACHMENT_REMAINS**

The high-strain route now supplies a positive law of lower-scale smooth carrier seeds.  Each seed at scale `A` has a terminal coefficient

\[
A|z(t)|^2\ge \frac{8\pi^2}{25c^2},
\]

and retains the two NN coherent heat-edge endpoints as a separate material witness.

The next question is temporal: does this seed actually traverse its own natural scale, or does the PDE itself stop it first?

## 1. Use the natural interval itself

For a seed at time `t`, inspect

\[
I_A=[\max(0,t-cA^{-2}),t].
\]

Parameterize backward motion by elapsed time

\[
\ell=t-s.
\]

If `t<cA^-2`, the interval reaches `t=0` before one full renewed lifetime; the initial surface is absorbing.

Otherwise the target is a complete interval of length `cA^-2`.

## 2. The physical corridor has native coordinates

Anchor the smooth scalar role `Q_A` at the seed and transport its symbol by the existing affine dual flow.  On the smooth pre-singular PDE interval monitor, independently,

\[
K_A(\ell)=\int_{t-\ell}^t\|S_{V_A}(q)\|_{op}\,dq,
\]

\[
|I_{interface}(\ell)|,
\qquad
|I_{HH}(\ell)|,
\]

and

\[
d_{mat}(\ell)
=\min_{i=0,1}\operatorname{dist}(\zeta_i(t-\ell),\partial O).
\]

The thresholds are respectively

\[
1/30,\qquad |z(t)|/4,\qquad |z(t)|/2,\qquad 0.
\]

For material contact the monitored superlevel variable is `-d_mat`.  A genuine change of either NN endpoint's old/new membership under a continuous material path must first have `d_mat=0`.  Dyadic naming boundaries remain Moyal-null as before.

No source action is divided by strain, and material distance is not compared numerically with a coefficient impulse.  These are separate faces of one physical corridor.  Their closed first debuts are measurable and exact ties remain an unsplit cause set.

## 3. Before strain contact, the low--low moat persists

As long as

\[
K_A<1/30,
\]

the lower support edge of the dual-transported carrier remains above

\[
\frac35e^{-1/30}A>\frac12A.
\]

The renewed transporter `V_A=S_(A/4)u` has low--low output below `A/2`.  Hence throughout the safe part of the corridor the exact outer moving-role equation is

\[
(\partial_t+L_{V_A}-\nu\Delta)Q_Au
=-Q_A P\operatorname{div}(h_A\otimes h_A)+R_{Q_A},
\]

with no low--low source.

The first term is HH generation.  The second is the already-classified nonaffine role interface.  It is not a new currency.

## 4. Exact adjoint survival gate

Use the adjoint interaction picture for the same moving carrier and terminal shell probe.  Exactly,

\[
z(t)=z(s)+I_{HH}[s,t]+I_{interface}[s,t].
\]

The two cumulative impulses are absolutely continuous in the backward endpoint.

If the interface impulse reaches `|z(t)|/4`, this is a **classified role-interface coefficient obstruction**.  It delegates to the existing skew-transfer / symmetric-strain provenance theorem; its magnitude is not silently converted into physical work.

If the HH impulse reaches `|z(t)|/2`, this is an **earlier HH-regeneration obstruction**.  Raw Duhamel amplitude is not a physical work law; the event must re-enter the existing physical-energy causal gate before causal weighting.

If neither impulse reaches its threshold, no strain face is hit and neither NN endpoint touches the old-material boundary, the triangle identity gives at the natural endpoint

\[
\boxed{|z(s)|\ge \frac14|z(t)|.}
\]

Thus persistence is a theorem, not an input.

## 5. A surviving full natural interval remains critical

The seed starts with

\[
A|z(t)|^2\ge\frac{8\pi^2}{25c^2}.
\]

Therefore a full no-hit survivor has

\[
A|z(s)|^2
\ge\frac1{16}A|z(t)|^2
\ge\boxed{\frac{\pi^2}{50c^2}}.
\]

The lower bound is scale-independent in the renewed critical normalization.

The two NN heat endpoints also remain outside the old material pool throughout the corridor if no material-boundary stop occurs.  This preserves an NN **witness**.  It does not assert that the whole energy of `Q_Au` is NN material.

## 6. Exact outcome

Every seed therefore has only the following backward outcomes before its requested natural endpoint:

- high strain / critical dissipation;
- classified nonaffine role-interface coefficient obstruction;
- earlier HH regeneration;
- material-boundary contact of an NN witness endpoint;
- `t=0` absorbing boundary;
- or a full natural-interval smooth carrier survivor with retained `1/4` coefficient and critical mass at least `pi^2/(50c^2)`.

Simultaneous causes stay unsplit.

## 7. What remains

This closes **temporal coefficient survival-or-first-stop for the high-strain carrier seed**.  The surviving object now exists across a complete renewed natural interval with support moat, critical coefficient and an NN endpoint witness.

The remaining high-strain seam is narrower but real: attach the surviving carrier coefficient/energy to that NN material witness strongly enough to start the canonical material service epoch, or route the mismatch into an already named physical source/interface/relink cause.  Efficiency/service renewal for the surviving seed and universal source/relink re-entry also remain open.

No full-shell NN assertion and no global-regularity claim are made.
