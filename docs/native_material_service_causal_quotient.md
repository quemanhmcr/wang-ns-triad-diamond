# Native material-service causal quotient

Exact theorem SHA: `d6ee03d5b9b7a82d11a2259c5dc0b8ae2ac945ab`.

## The physical question

After inherited stock, intrinsic-membership rereading, and selected-family `R_switch` have all been separated, the remaining ambiguity was sharper: does a name such as `material_relink` or `new_coherent_ancestry` itself denote a new Navier--Stokes energy/service source?

The answer is **no**.  A material-state exit can be a legitimate carrier-chart locator, but the word *material* supplies no energy owner.  Before a canonical recursive event exists, the observation must be resolved by an independently certified PDE mechanism.

The theorem distinguishes three objects which must not be identified:

1. intrinsic material old/new membership and selected-family bookkeeping;
2. the transported smooth `eta_a=A_a^2` synthesis-role partition used by the smooth-interface theorem;
3. native physical suppliers such as source/SGS work, strain/dissipation, actual nonlinear work, or an already-certified physical shell/service mechanism.

## 1. Certified smooth `K_phys` relink is a subset flux, not a source

For an already-certified gauge-quotiented smooth interface, let

`T_ab = -2 Re <eta_a u, K_phys eta_b u>`.

The production certificate binds this matrix to the same interface state and also checks

`native = relink + strain`, `T_ab=-T_ba`, and `R_a=sum_b T_ab`.

Hence for every proper subset `O` of the **certified smooth roles**,

`sum_{a in O} R_a = sum_{a in O, b notin O} T_ab`.

All `O--O` circulation cancels pairwise.  The residual `K_phys` component is therefore same-event conservative smooth-role flux.  It can carry signed donor/recipient provenance, but it creates no net source, no new physical-work charge, no recursive generation depth, and no scale progress.

This statement deliberately does **not** say that an arbitrary intrinsic material old/new set is one of the `eta_a` role subsets.  Intrinsic material addresses and smooth synthesis roles are different representations.  A genuine role/probe/interface change needs its own native certificate; otherwise the raw material locator remains unresolved.

## 2. OO/ON/NN are downstream restrictions of a positive law

Let a positive physical service law already have edge weights `s_e>=0`.  Only after that law exists are the coherent endpoints read as old or new.  The exact partition is

`S_total = S_OO + S_ON + S_NN`.

Changing only the material reading can change all three category masses while leaving every `s_e`, `S_total`, and any independently supplied native owner unchanged.  Thus OO/ON/NN are provenance/restrictions of an existing positive law.  The partition itself creates neither service, physical work, nor a recursive owner.

If an upstream PDE theorem has certified a native supplier, the quotient preserves that supplier.  If no supplier has been certified, the quotient does not invent one.

## 3. Master admission is fail-closed

Raw labels

- `material_relink`;
- `new_coherent_ancestry`

cannot enter the canonical physical-owner state by name alone.  They are pre-owner carrier/material-state locators.  Before recursion they need an independently typed physical resolution: for example a bound smooth-interface `K_phys`/strain decomposition when that is the actual event, a source/service/actual-work/shell owner, or another explicit role/probe-change certificate.

This barrier is applied at the central/master ingress and at the typed Phase-B material-sidecar joint wrapper.  It does not rewrite the generic conditional tie algebra: that algebra remains correct *given* already-certified physical causes.  The theorem changes the evidence required to manufacture such a cause from a material observation.

Consequently the old architecture in which an arbitrary `PhysicalCause.MATERIAL_RELINK` hit could be inserted as an independently recursive material cause is no longer the canonical executable semantics.  The enum remains compatibility vocabulary; the raw label does not pass the owner boundary without native evidence.

## 4. Actual Navier--Stokes anti-theorem

No artificial material PDE is introduced.  The referee evolves the same real, incompressible, 2/3-dealiased Fourier--Galerkin Navier--Stokes trajectory used by the helical audits.  At genuine trajectory times it forms the actual positive law

`|u(x-r)-u(x)|^2`, with `r=(pi/2,0,0)`.

The NS state and this positive law are then held literally fixed while two geometric old-material readings are applied:

- an `x`-slab, whose endpoints cross the displacement boundary;
- a `y`-slab, whose endpoints do not.

The OO/ON/NN partition changes maximally, yet total service does not change.  On the dedicated amplitude-1 trajectory, the two FFT representations `24,28` agree in total service to relative residual `2.065e-16`; the minimum partition-change fraction is exactly `1`.  At `t=0`, total service is `17.24`, the x-interface ON service is `8.62`, while the y-interface ON service is exactly `0`.  At `t=0.001`, total service is `17.1907284158` and the corresponding x-interface service is about `8.59536446`, while y-interface service remains `0`.

The trajectory itself retains the native invariants: global energy-balance residual `2.963e-12`, maximum global nonlinear-work relative rate about `1.17e-18`, and divergence relative to initial `L2` below `4.7e-16`.

The independent opposite-phase amplitude-0.5 audit gives the same anti-theorem: cross-representation service residual `2.074e-16`, partition-change fraction `1`, energy-balance residual `2.614e-11`, global nonlinear-work relative rate below `1.0e-18`, and divergence below `4.5e-16`.

The geometric slabs are not asserted to be persistent material packets and the snapshot increment law is not assigned a causal supplier by this referee.  Its exact point is narrower: **for an actual NS field, changing only material ownership can change interface/fresh provenance while the positive physical law is unchanged.  Therefore material ownership alone cannot be its generator.**

## 5. Exact-SHA certification

On executable SHA `d6ee03d5b9b7a82d11a2259c5dc0b8ae2ac945ab`:

- dedicated `31687889719`: **success**; `92` focused/dependency tests, `75,000` analytic states, and the two-representation amplitude-1 actual NS referee;
- independent audit `31687889469`: **success**; `90` independent tests, `100,000` analytic states, and the opposite-phase amplitude-0.5 actual NS audit;
- physical-energy causal integration v2 `31687918903`: **success**; `968` tests, master `20,000` episode traces with worst margin `0.0`, and a `63`-node plan with `60` exact-content reuse + `3` execute on shards `4,5`; all results were present and all `60` baseline-matched nodes were byte-compared.

Analytic falsification metrics:

- dedicated maximum smooth-role subset-divergence residual `1.421e-14`, pair antisymmetry residual `0`, positive-service partition residual `1.137e-13`, `74,929` ownership rereadings with changed OO/ON/NN, `21,430` raw-material-owner rejections, recursive generations created `0`;
- audit maximum subset-divergence residual `1.421e-14`, pair antisymmetry residual `0`, positive-service partition residual `8.527e-14`, `99,879` changed rereadings, `28,572` raw-owner rejections, recursive generations created `0`.

Recorded artifact trees:

- `recorded-results/31687889719/` — `sha256:90326a6fe682f42053be29038bcc2aaad82b3eb0b38e8453ea2764c5d2e0f358`;
- `recorded-results/31687889469/` — `sha256:10ca49423266cc4eaba3d335d2f41fe585ab237d3511cd06e4de67bedf82eea4`;
- `recorded-results/31687918903/` — `sha256:b9293e104a0008102b35b853269ed8e35eccbb6e95bf313a2ca2b5b4f1c5b69f`.

GitHub artifact digests include dedicated analytic `sha256:f89cd539127680688b52ffa501f85cc5b559dc6e31f5be7db5f410345c105f29`, dedicated NS `sha256:e945264050aa30d3ebf611de6f68d6f779de2dc52078e4990ae2543ae88d6039`, audit analytic `sha256:d6a816b2f69f8a9716e914dcf4ef9900224085f93c66c2162358438f5e67639e`, audit NS `sha256:c01da5760eb14fefbc3e0d9b5fca5f505fbae08f5f3f32062e3e9907dddae886`, and v2 aggregate `sha256:703af79847b3b74bcde8d4b23f21961fb7430bf9ed9997573ce34efdfe90a334`.

## Failure/correction lineage

The first candidate `ea3ea535ad40eac7b3bb3c127edb06b22c95fcef` passed its dedicated/audit referees, but review found an ontology overreach: wording treated an arbitrary material crossing as if it were automatically a boundary of the smooth `eta_a` role partition.  That identification is not supplied by the PDE.  The candidate was superseded despite being green.

Repair `d6ee03d5b9b7a82d11a2259c5dc0b8ae2ac945ab` narrowed the exact statement: only a separately certified smooth `K_phys` relink gets the antisymmetric smooth-role subset-flux theorem; intrinsic material sets remain distinct, and unresolved material exits fail closed until native evidence exists.  No sample count, PDE equation, physical invariant, or numerical tolerance was weakened.

No 3D Navier--Stokes global-regularity claim is made.
