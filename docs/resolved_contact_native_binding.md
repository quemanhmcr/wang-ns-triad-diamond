# Resolved-contact native binding: actual `u=V+h` work before owner names

## Status

Certified theorem on exact implementation SHA
`9d615568640e39c49acc7295a996ed97e04110e6`.

The exact status string is

`EXACT_RESOLVED_CONTACT_NATIVE_BINDING__SIGNED_VH_HH_REPARTITION_BEFORE_HAHN__CANONICAL_DWPLUS_RESTRICTS_WITHOUT_CLONING__MIXED_TO_K_OR_STRAIN_COVER__HH_COMPLEMENT_UPPER_COMPARABLE_R_5_OVER_4_TO_DIRECT_NATURAL_WINDOW__COMMON_N_DW_UNIT`.

This theorem starts where `hard_tail_true_upward_supply` deliberately stopped. A
true low-to-high atom whose recipient shell has a parent at resolved frequency is
not renamed “interface work” from support geometry. We first perform the actual
smooth decomposition of that same Navier--Stokes interaction, keep the signed
work identity upstream of Hahn, and only then ask what positive restrictions of
the canonical cause are owned by existing physical mechanisms.

No new interface currency appears.

## 1. The resolved-contact geometry already forbids low--low

Fix a canonical upward donor--recipient atom

\[
(k_d,s_d)\longrightarrow(k_r,s_r),
\qquad |k_d|\le N<|k_r|,
\]

and put the recipient in its boundary-anchored hard shell

\[
\frac M2<|k_r|\le M,
\qquad M=2^jN,
\qquad j\ge1.
\]

Let `p_1,p_2` be the two actual interaction parents of the recipient edge. On
the resolved-contact support restriction at least one satisfies

\[
|p_i|\le M/4.
\]

Both cannot do so, because then triangle closure would give

\[
|k_r|\le |p_1|+|p_2|\le M/2,
\]

contradicting the strict shell lower edge. Thus there is exactly one resolved
parent, call it `p_l`, and one complementary parent `p_h`:

\[
|p_l|\le M/4<|p_h|.
\]

Triangle closure also gives the upper bound

\[
|p_h|\le |k_r|+|p_l|\le M+M/4=5M/4.
\]

Hence every resolved-contact edge, including any first-shell contact edge, has
the exact geometry

\[
\boxed{|p_l|\le M/4<|p_h|\le5M/4.}
\]

This is already enough to distinguish the physical possibilities. Low--low work
cannot reach the recipient shell. What remains is mixed resolved/high work and
an unresolved high--high complement.

## 2. Choose a positive smooth decomposition, not a causal law

The certified cutoff-repartition identity says changing the resolved field only
repartitions the same quadratic Navier--Stokes interaction; it creates no
cutoff-switch source or currency. We may therefore use any real smooth scalar
multiplier `S_M` satisfying

\[
0\le S_M\le1,
\qquad
\operatorname{supp}S_M\subset B_{M/4},
\]

and write

\[
V=S_Mu,
\qquad
h=(I-S_M)u.
\]

The nonnegativity is not a probability interpretation. It is an admissible
analysis gauge which has two useful physical consequences: multiplication by
the cutoff can restrict a positive work cause without changing its sign, and
both pieces are `L^2` contractions,

\[
\|V\|_2\le\|u\|_2,
\qquad
\|h\|_2\le\|u\|_2.
\]

No plateau radius is part of the theorem. Let only

\[
q=S_M(p_l)\in[0,1].
\]

Since `|p_h|>M/4`, smooth support gives

\[
S_M(p_h)=0.
\]

## 3. The signed edge identity comes before Hahn

On this one unordered physical parent orbit, the low parent appears in `V` with
amplitude factor `q` and in `h` with factor `1-q`; the high parent lies entirely
in `h`. Bilinearity of the actual Leray/Fourier Navier--Stokes source therefore
gives, on the same recipient edge,

\[
F_{edge}=F_{Vh}+F_{hh},
\qquad
F_{Vh}=qF_{edge},
\qquad
F_{hh}=(1-q)F_{edge}.
\]

Pair with the actual recipient amplitude before any downstream positive-part
operation. For signed physical child-energy work,

\[
\boxed{
dW=dW_{Vh}+dW_{hh},
\qquad
dW_{Vh}=q\,dW,
\qquad
dW_{hh}=(1-q)\,dW.
}
\]

This is the load-bearing identity. The theorem does not obtain it by decomposing
a positive scalar after the fact; the actual PDE source itself decomposes this
way first.

## 4. Push the existing canonical cause; do not Hahn again

The upward atom already comes from the certified cyclic donor kernel and is a
positive donor-restricted submeasure of canonical `dW+`. Let its physical mass
be `dmu`.

Because `q` and `1-q` are nonnegative, define only the positive restrictions

\[
\boxed{
d\mu_{mix}=q\,d\mu,
\qquad
d\mu_{HH}=(1-q)\,d\mu.
}
\]

Then exactly

\[
d\mu_{mix}+d\mu_{HH}=d\mu.
\]

There is no new Hahn decomposition, no normalization by native capacity, and no
recipient-shell factor `M/N`. Both labels remain in the common high-tail unit

\[
N\,dW.
\]

The same-time donor mark is retained on both restrictions as provenance only.

### Multiple donors do not clone the owner

A positive cyclic recipient may have two donors. The K/S decomposition below is
a decomposition of the **recipient physical work**, not one copy per donor
label. Therefore every selected donor-restricted mixed mass for the same
recipient is first coalesced:

\[
\mu_{mix,r}
=\sum_{d\to r\ \mathrm{selected}}\mu_{mix,d\to r}.
\]

Only this single-charged recipient submeasure enters K/S ownership. Donor
indices remain sidecars. Repeating the same recipient K/S row once for each
donor is forbidden.

## 5. Anti-theorem: contact can be 100% HH

Resolved-frequency contact does not imply positive mixed/interface work.

If the low parent lies exactly on the support boundary,

\[
|p_l|=M/4,
\]

then every smooth multiplier supported in `B_{M/4}` must vanish there by
continuity from the exterior. Thus

\[
q=0,
\qquad
d\mu_{mix}=0,
\qquad
d\mu_{HH}=d\mu.
\]

The certified physical triad

\[
(-2,-1,-1)+(1,0,0)+(1,1,1)=0
\]

at `N=1`, `M=4` realizes exactly this boundary geometry. It is a permanent
counterexample to the slogan “resolved contact = interface owner”. The contact
is real; its actual smooth decomposition says the positive atom is HH.

## 6. Interior mixed work enters the existing K/S physics

For the chosen resolved field, let the actual linearized resolved operator be

\[
L_Vf=B(V,f)+B(f,V).
\]

At the fixed physical Fourier event split its adjoint parts

\[
L_V=K+S,
\qquad K^*=-K,
\qquad S^*=S.
\]

For the same low/high interaction pair and same recipient work atom write the
signed mixed work as

\[
I=I_K+I_S.
\]

The skew pair has the reciprocal law

\[
T_{ab}^K=-T_{ba}^K,
\]

while the symmetric pair obeys

\[
T_{ab}^S=T_{ba}^S.
\]

Thus the identity is signed before positive ownership:

\[
\boxed{I=K_{work}+S_{work}.}
\]

Only afterward,

\[
[I]_+\le[K_{work}]_+ +[S_{work}]_+.
\]

The coalesced canonical mixed submeasure is already dominated by the positive
mixed work of this same recipient edge,

\[
\mu_{mix,r}\le[I]_+.
\]

Therefore

\[
\boxed{
\mu_{mix,r}
\le[K_{work}]_+ +[S_{work}]_+.
}
\]

No proportional matching of `mu_mix` to the two terms is introduced. The only
consequence needed is the positive witness alternative

\[
[K_{work}]_+\ge\frac12\mu_{mix,r}
\quad\text{or}\quad
[S_{work}]_+\ge\frac12\mu_{mix,r},
\]

with ties retained jointly.

The meanings are already certified:

- fixed-event `K` is conservative same-time resolved role redistribution. Its
  donor/circulation quotient creates no new energy, physical time, event depth,
  or scale progress;
- `S` is the existing symmetric strain/deformation work.

This theorem does **not** identify fixed-event `K` with the smooth moving-carrier
`K_phys` by name. The latter first quotients common observer transport. The two
representations share the antisymmetric-flux lemma only after each has proved
its own physical pair law.

## 7. The HH complement is not pure-UV, but it is automatically upper-comparable

When `q<1`, the remainder `dmu_HH=(1-q)dmu` is actual `h-h` work. It must remain
separate from the pure-UV support branch: its low interaction parent can lie
below `M/4` even though its **unresolved amplitude component** lies in `h`.

Nevertheless the physical parent frequencies already satisfy

\[
|p_l|\le M/4,
\qquad
|p_h|\le5M/4.
\]

Thus both parents lie below `RM` with the exact contact radius

\[
\boxed{R=5/4.}
\]

No output-scale locality theorem is needed to discover this upper
comparability.

More importantly, the positive cutoff used for type-correct cause restriction
also gives

\[
\|h\|_2\le\|u\|_2.
\]

The older natural-window theorem had only `|S|<=1`, hence
`||h||_2<=2||u||_2`; the two parents carried a factor `4`. Here that factor is
absent.

Sharp physical Young together with the child hard-shell Fourier-volume bound
gives the absolute interaction capacity

\[
r_{contactHH}(t)
\le 3\sqrt\pi\,R\,M^2E_{global}\sqrt{\mu_{win}},
\]

where

\[
\mu_{win}=\max_{t\in I_*}M\|P_Mu(t)\|_2^2.
\]

Integrating over the exact natural duration `cM^{-2}` and retaining the common
parent-tail factor `N` gives

\[
\boxed{
N W_{win}
\le 3c\sqrt\pi\,R\,N E_{global}\sqrt{\mu_{win}}.
}
\]

For contact work `R=5/4`, so the coefficient is `15 sqrt(pi)/4`.

This bound is applied to the donor-restricted canonical positive HH submeasure
through domination by **absolute physical edge work**. It does not first sum
signed shell work and Hahn-split the aggregate; cancellation cannot erase or
manufacture the causal mass.

## 8. Direct shell/time concentration of the canonical HH submeasure

Push the same positive `dmu_HH` to recipient hard shells. If its total common
work is `H`, let the maximal shell have fraction

\[
p_s=\frac{H_M}{H}.
\]

This maximum exists for a finite or summable positive shell law. On the selected
shell take the sliding natural window of length

\[
T_M=cM^{-2}
\]

and let

\[
p_t=\frac{\sup_s\mu_{HH,M}([s,s+T_M])}{H_M}.
\]

For smooth Navier--Stokes the physical work density is continuous; no fixed time
bin or packet persistence is introduced.

Then

\[
W_{win}=p_tp_sH.
\]

Combining with the preceding absolute-work capacity yields

\[
\boxed{
\frac{\sqrt{\mu_{win}}}{p_sp_t}
\ge
\frac{H}{3c\sqrt\pi\,R\,N E_{global}}.
}
\]

This is a strength--concentration law of the same canonical positive cause, not
an entropy currency.

## 9. Hard-tail consequence

The certified true-upward support theorem says that, once the resolved-contact
support branch is the upward owner,

\[
N\Phi_{contact}\ge\frac{\nu D_{tail}}2.
\]

The exact positive V/h split gives

\[
N\Phi_{contact}=H_{contactHH}+I_{mixed}.
\]

Hence either

\[
\boxed{H_{contactHH}\ge\frac{\nu D_{tail}}4}
\]

or

\[
I_{mixed}\ge\frac{\nu D_{tail}}4.
\]

On the mixed alternative, the signed K/S cover yields

\[
\boxed{
K^+\ge\frac{\nu D_{tail}}8
\quad\text{or}\quad
S^+\ge\frac{\nu D_{tail}}8.
}
\]

The `K` alternative is same-event conservative donor tracing; the `S`
alternative is existing strain/deformation ownership.

On the HH alternative, `R=5/4` makes the sliding natural-window law particularly
clean:

\[
\boxed{
\frac{\sqrt{\mu_{win}}}{p_sp_t}
\ge
\frac{\nu D_{tail}}
{15c\sqrt\pi\,N E_{global}}.
}
\]

The resulting `mu_win` is an actual hard-shell event and enters the existing
generic critical-shell first-stop continuation. No output-scale locality theorem
is inserted between contact HH work and this natural-window step.

## 10. Actual Navier--Stokes referee

The GitHub Actions referee evolves real divergence-free six-mode orthogonal
Fourier--Galerkin Navier--Stokes data. The PDE trajectory is computed once and is
independent of the analysis cutoff. At every output time the referee:

1. reconstructs the full canonical cyclic donor/recipient work law;
2. selects the same resolved-contact recipient cause;
3. reads two distinct admissible smooth cutoffs on the same state;
4. directly computes `V`, `h`, low--low, mixed and high--high nonlinear terms;
5. requires
   \[
   F(u,u)=F(V,V)+F_{mixed}+F(h,h)
   \]
   and zero low--low leakage into the recipient shell;
6. reconstructs the same recipient edge work from `q` mixed plus `(1-q)` HH;
7. coalesces all upward donor sidecars of that recipient before K/S ownership;
8. constructs the reciprocal resolved K/S pair from direct Leray/helical source
   coefficients and checks skew antisymmetry, strain symmetry and the signed
   identity;
9. retains the usual Galerkin energy and divergence invariants.

Two physical geometries are used. The `M=4N` boundary fixture has `q=0` for
both cutoffs. A separate `M=4N` interior-contact fixture is read once with a
transition cutoff giving `0<q<1` and once with a different admissible cutoff
which fully resolves that same low parent. The full nonlinear source and
canonical cause remain unchanged while only the representation split changes.

This is exactly the behavior required by the cutoff-repartition gauge theorem.

## 11. What this theorem does not say

It does not:

- rename Fourier contact as interface ownership;
- re-Hahn a mixed, HH, shell or K/S representation;
- identify donor provenance with a new cause;
- charge the same recipient K/S work once per donor;
- identify fixed-event `K` with smooth moving-carrier `K_phys` without the
  latter's observer-gauge quotient;
- call the contact HH complement “pure-UV by support”;
- reweight canonical work by recipient shell `M`;
- turn shell selection or sliding windows into a cascade clock;
- turn capacity or critical shell mass into an additive recurrence budget;
- prove termination of all mixed genuine-owner recurrence;
- prove 3D Navier--Stokes global regularity.

Its structural conclusion is narrower: **resolved contact is not a new owner**.
The actual PDE repartitions its canonical positive cause into (i) mixed
resolved/high work, which binds to existing conservative K or strain S physics,
and (ii) a distinct HH complement whose real parent geometry already permits a
direct natural-window continuation.

## 12. Certification

Exact theorem SHA: `9d615568640e39c49acc7295a996ed97e04110e6`.

Dedicated run: `31653626691` — **success**.

Independent audit: `31653626677` — **success**.

Full physical-energy causal integration: `31653711095` — **success**.

Dedicated full suite: `906` tests passed. Dedicated analytic stress used
`75,000` states. The independent analytic audit used `100,000` states.

The independent actual-NS audit reported worst full PDE V/h repartition residual
`3.359e-16`, cutoff-repartition gauge residual `3.231e-16`, zero high-shell
low--low leakage, same-edge signed V/h work residual `3.863e-17`, K/S signed
identity residual `1.288e-17`, K skew-pair residual `1.610e-18`, S symmetric-pair
residual `6.439e-18`, canonical K/S positive-cover defect `1.288e-17`, and
Galerkin energy-balance residual `8.560e-16`. The maximum FFT-representation
spread was `9.843e-16`.

The independent `100,000`-state analytic audit had maximum positive V/h
partition residual `2.087e-16`, minimum K/S positive-cover margin `0`, positive
half-owner margin throughout the sampled law, boundary-contact HH fraction
exactly `1`, and the coarse-Hahn cancellation anti-example retained gap `0.9`.

Full integration passed the same `906`-test suite and all `68` job steps; the master episode stress checked `20,000` traces with worst margin `0.0`. The integration artifact tree digest is `sha256:349ed3bbc4f81f55928521e2409141267924bdbdffb4d450af196f555f39553e`. Full dedicated/audit/integration artifact provenance is recorded in `docs/experimental_certification_log.md`.
