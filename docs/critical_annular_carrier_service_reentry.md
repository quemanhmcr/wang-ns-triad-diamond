# Critical annular carrier to bounded coherent-service re-entry

## Purpose

The NN-critical heat theorem and temporal first-stop theorem leave one apparently awkward task: a full-natural-corridor survivor has a critical smooth coefficient and a retained NN witness, but the NN witness belongs to the old child-scale heat edge rather than to all of the renewed Fourier carrier.  It would be incorrect to turn that witness into a whole-carrier material label.

The correct order is the reverse.  Use the surviving annular carrier to create **new physical service at its own scale**, and only then read material ownership from that positive service law.

## Prefix-wise persistence

On a full no-hit backward natural corridor every cumulative prefix monitor stays below its own first-hit face:

\[
 |I_{interface}[s,t]|<\frac14|z(t)|,
 \qquad
 |I_{HH}[s,t]|<\frac12|z(t)|.
\]

The exact moving-role/adjoint identity

\[
 z(t)=z(s)+I_{HH}[s,t]+I_{interface}[s,t]
\]

therefore gives, for **every** prefix \(s\),

\[
 |z(s)|>\frac14|z(t)|.
\]

Thus the temporal theorem is not merely an endpoint-survival statement.

## Do not drop inverse heat

The registered coefficient uses the existing affine/Kelvin/viscous analysis dual.  Its backward interaction-picture probe therefore grows under inverse heat.  This is not being asserted as an operator-norm bound for the full nonaffine outer-role adjoint: the mismatch with full resolved transport is precisely part of the already monitored role-interface coefficient obstruction.  That obstruction locates physical-energy reentry; its magnitude is not work.  The renewed envelope is supported initially in

\[
 \frac35 A\le |\xi|\le\frac32 A,
\]

and low strain \(K\le1/30\) keeps the affine-dual support inside

\[
 \frac35e^{-1/30}A\le |\xi|\le\frac32e^{1/30}A.
\]

On \(T_A=cA^{-2}\), the existing adjoint estimate gives

\[
 \|\psi(s)\|_2
 \le J\|\psi(t)\|_2,
 \qquad
 J=\exp\!\left(\frac1{30}+\nu c\left(\frac32e^{1/30}\right)^2\right).
\]

Since the terminal dual is normalized and the temporal survivor has

\[
 A|z(s)|^2\ge \frac{\pi^2}{50c^2}
\]

at the coefficient level, Cauchy gives uniformly through the entire natural slab

\[
 \boxed{
 A\|Q_Au(s)\|_2^2
 \ge \frac{\pi^2}{50c^2J^2}.
 }
\]

This is scale independent for fixed \(c,
u\).

## The annulus itself forces heat service

Let \(w=Q_Au(s)\) and take the intrinsic Navier--Stokes heat displacement
\(r\sim N(0,A^{-2}I)\).  Plancherel gives

\[
 \mathbb E\|\delta_rw\|_2^2
 =\int 2\left(1-e^{-|\xi|^2/(2A^2)}\right)|\widehat w(\xi)|^2\,d\xi.
\]

Because the transported carrier stays a fixed distance from zero frequency,

\[
 \mathbb E\|\delta_rw\|_2^2
 \ge q_*\|w\|_2^2,
 \qquad
 q_*=2\left[1-\exp\left(-\frac12\left(\frac35e^{-1/30}\right)^2\right)\right]>0.
\]

The Gaussian displacement is unbounded, so this full expectation must **not** be inserted directly into the bounded-displacement reservoir theorem.  Instead use the universal translation bound

\[
 \|\delta_rw\|_2^2\le4\|w\|_2^2.
\]

For \(Z\sim N(0,I_3)\), Arb certifies at \(eta=3\) that

\[
 q_*-4\Pr(|Z|>3)>\frac12q_*.
\]

Hence deleting every displacement with \(|r|>3/A\) still leaves more than half of the heat lower.  Therefore at every slab time there is an **actual bounded physical displacement** \(r_s\) such that

\[
 \boxed{
 |r_s|\le\frac3A,
 \qquad
 A\|\delta_{r_s}w(s)\|_2^2\ge Y_0(c,\nu)>0,
 }
\]

with

\[
 Y_0=\frac{q_*}{2}\frac{\pi^2}{50c^2J^2}.
\]

Equivalently the full natural slab carries normalized bounded heat service at least \(cY_0\).

## Materiality is read after service exists

Now apply exact Moyal to the positive increment law of \(w(s)\).  Every coherent edge has two actual intrinsic endpoints, so the already-certified ownership partition gives

\[
 S=S_{OO}+S_{ON}+S_{NN}
\]

pointwise as positive measures.  No decomposition \(w=w_{old}+w_{new}\) is introduced.

This changes the conceptual role of the original NN witness.  That witness was needed to select a legitimate critical high-strain seed from the old-incident heat law.  Once the full-natural survivor exists, it is **not** propagated as a claim that the whole renewed carrier is new material.  The renewed carrier generates its own service, and material ownership is read from that service.

## Re-entry into the existing stopping epoch

The quantity \(Y_0\) is exactly a scale-independent bounded-displacement square-service threshold of the type consumed by the existing coherent-service stopping theorem.  On any supplied sticky signed-good continuation, the old-pool service capacity decays geometrically.  Once it falls below \(Y_0/8\), the existing exact service graph forces one of

- critical dissipation,
- material/interface service,
- new coherent mass,
- ancestry entropy,
- same-ancestry cycle mass.

Thus high-strain renewal no longer needs a theorem asserting that a retained NN witness owns the entire renewed carrier.

## Scope

This theorem proves a **coherent-service entrance**, not near-extremal HH transfer efficiency of the entire renewed carrier.  It also does not manufacture the next signed-good recursive continuation after an arbitrary source or relink event.  Those are part of universal physical slab renewal/exhaustion, which remains the continuum frontier.
