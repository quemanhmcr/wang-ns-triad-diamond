# Material ownership belongs to the positive heat-edge measure

## Status

**EXACT_POSITIVE_HEAT_EDGE_MATERIAL_OWNERSHIP_PARTITION__AFFINE_ENDPOINT_LABELS_INVARIANT__CAPACITY_ROUTING_REMAINS**

The high-strain heat theorem produces a positive phase-space edge measure.  The next question is whether an edge belongs to the already transported material reservoir or represents new material structure.  The wrong operation would be to decompose the velocity itself as `old+new`; squaring an increment would then manufacture cross terms whose meaning depends on the observer's decomposition.

The natural object is already positive, so classify **after** Moyal.

## 1. Every coherent increment atom has two physical endpoints

Translation covariance is exact:

`V_g(delta_r f)(X,k)=e^(-ik.r)V_gf(X-r,k)-V_gf(X,k)`.

Hence a heat-increment atom centered at `(X,k)` is a spatial edge between the coherent neighborhoods

`(X,k)` and `(X-r,k)`.

In the intrinsic material coordinate

`zeta=(L^-1 X/2,L^T k)`,

write these endpoint labels as `zeta_0` and `zeta_1`.

Let `O` be any measurable transported old material set in intrinsic `zeta` space.  It can be a union of the canonical half-open dyadic cells already used by the material-label theorem.  Put

`chi_i=1_O(zeta_i)`.

## 2. OO / interface / NN is an exact partition of a positive measure

If `s>=0` is one heat/Moyal edge density, define

`s_OO=chi_0 chi_1 s`,

`s_ON=[chi_0(1-chi_1)+(1-chi_0)chi_1]s`,

`s_NN=(1-chi_0)(1-chi_1)s`.

Pointwise,

`chi_0chi_1+chi_0(1-chi_1)+(1-chi_0)chi_1+(1-chi_0)(1-chi_1)=1`,

so

`boxed(s_OO+s_ON+s_NN=s)`.

This is not an estimate.  No field was split and no cross term was dropped.  The interface class is unoriented because exchanging the two endpoints merely swaps the two mixed indicators.

Therefore the frequency-ancestral heat service from the high-strain theorem may be disintegrated simultaneously by shell/time/displacement/coherent cell/**material endpoint ownership**, all without changing its total positive mass.

## 3. Common affine transport cannot change ownership

Under a common affine/Kelvin map,

`L->ML`, `X->MX`, `r->Mr`, `k->M^-T k`.

For the first endpoint,

`zeta_0' = ((ML)^-1 MX/2,(ML)^T M^-T k)=zeta_0`.

For the neighboring endpoint,

`zeta_1' = ((ML)^-1 M(X-r)/2,(ML)^T M^-T k)=zeta_1`.

Thus **both endpoints are material invariants**.  Common affine motion can neither create an old--new interface nor erase one.  This statement concerns the ownership mark of an edge; it does not falsely assert that an isotropic heat-kernel density is invariant under every non-orthogonal affine deformation.

Along a continuous nonaffine evolution, endpoint membership in `O` can change only by contacting the topological boundary of `O`.  The canonical half-open convention merely decides which side owns the boundary itself.  Since the Moyal/heat laws have densities, fixed dyadic boundaries carry zero service mass and do not become a relink currency.

## 4. Ownership classes inherit local endpoint capacity

For the two coherent endpoint amplitudes `A_0,A_1`,

`|e^(-ik.r)A_1-A_0|^2 <= 2(|A_0|^2+|A_1|^2)`.

Multiplication by any of the nonnegative OO/ON/NN indicators preserves the inequality.  Hence each ownership class is bounded locally by the Moyal energy of **its own endpoints**.

This is the correct doorway to an old-pool capacity theorem.  It does not justify importing the existing signed-good ratio `231525/512000` through the very high-strain interval which violated the low-strain hypothesis.  The required heat-specific history/capacity argument is supplied downstream by `first_hit_heat_reservoir_erosion.md` and strengthened to old-incident `OO+ON` erosion by `old_incident_heat_erosion.md`.

## 5. What ownership means, and what it does not

- OO is service whose two coherent endpoints both lie in the transported old material set.
- ON is an actual material-interface edge.  If ownership changes dynamically, the endpoint must cross the material boundary; the positive crossing/service provenance can then be routed to the existing relink/interface ledger.
- NN is service entirely outside the old pool, but this theorem does **not** assert a fixed packet mass or a selected Young parent.  Its physical consequence must come from dominant coherent service, entropy/cycle, or a renewed carrier theorem.

So this theorem gives exact frequency ancestry, spatial coherent edges, and material endpoint ownership.  Its originally remaining quantitative questions are now routed downstream: repeated `OO` cost is controlled by first-hit heat erosion, `OO+ON` by old-incident erosion, and the resulting NN-critical sublaw feeds shell-carrier renewal.  Universal continuum assembly remains separate.  No global-regularity claim is made.
