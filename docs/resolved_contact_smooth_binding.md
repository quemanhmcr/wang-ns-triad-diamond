# Resolved-contact smooth binding: canonical upward work through the actual `u=V+h` law

## Status

**Candidate theorem.** Certification requires the dedicated theorem workflow, the independent adversarial/audit workflow, and the full physical-energy causal integration sweep on one exact implementation SHA.

Candidate status string:

`EXACT_RESOLVED_CONTACT_SMOOTH_BINDING__POSITIVE_CORE_CUTOFF_BEFORE_CAUSAL_PUSH__DEEP_M_GE_8N_IS_PURE_MIXED_VH__M_EQ_4N_RETAINS_TRUE_TRANSITION_HH__DONOR_RESTRICTED_DWPLUS_BINDS_BEFORE_COARSE_HAHN__NONCLONING_KS_OWNER_SPLIT__COMMON_N_DW_UNIT`

This theorem starts exactly where `hard_tail_true_upward_supply` stops.  The earlier theorem proved that a true low-to-high cyclic donor atom landing in a recipient shell `M>=4N` has an actual interaction parent at resolved-scale frequency, but deliberately refused to rename support contact as interface work.  Here the cutoff is finally inserted into the **actual Fourier interaction before positivity is changed**.

The answer is more structured than “contact = interface”.  Deep contact contains two genuinely different physical phenomena:

1. an actual mixed `V-h` interaction, which can be read through the physical adjoint `K/S` split of the resolved linearized Navier--Stokes operator;
2. a genuine smooth-cutoff transition `h-h` interaction, which remains HH work and must not be relabeled as interface work.

For shells `M>=8N` the second phenomenon disappears identically: the whole canonical upward atom is mixed.  The borderline shell `M=4N` is the only place where the smooth transition can remain.

No new Hahn law, causal currency, packet, clock, FIFO matching, or recipient-shell reweighting is introduced.

## 1. Start from the canonical radial cause, not from a downstream representation

Take one already-certified true-upward cyclic donor atom

\[
 d\longrightarrow r,
 \qquad |k_d|\le N<|k_r|,
\]

with positive physical mass inherited from the canonical donor kernel.  Its recipient lies in the unique boundary-anchored hard shell

\[
 \frac M2<|k_r|\le M,
 \qquad M=2^jN.
\]

The causal object is still the same restriction of canonical `dW+`; the energy donor is same-time provenance inherited from canonical `dW-`.  The common high-tail unit remains

\[
 \boxed{N\,dW}.
\]

Nothing below replaces that cause by a smooth-role Hahn law.

For `j>=2`, the earlier theorem gives

\[
 |k_d|\le N\le M/4,
\]

and proves that the donor root is one actual interaction parent of the recipient edge.

Let `p` be the other interaction parent and `z` the recipient child.  Since

\[
 z=k_d+p,
 \qquad |z|>M/2,
\]

both parents cannot lie at or below `M/4`.  Because the donor already does,

\[
 \boxed{|p|>M/4.}
\]

Thus a deep upward edge has **exactly one possible resolved-contact parent**, namely its physical radial energy donor.  This is stronger than the old “at least one parent touches the resolved region” statement and is pure triad geometry.

## 2. Choose an admissible positive smooth representative before routing cause

The previously certified cutoff-repartition theorem says that changing the resolved field `V` merely repartitions the same quadratic Navier--Stokes interaction; it creates no cutoff-switch forcing and no new currency.  Therefore this theorem may choose a convenient smooth representative without pretending that an older unspecified `S_(M/4)` already had a plateau it never certified.

Choose one real radial `C^infinity` multiplier `sigma_M` satisfying

\[
 0\le\sigma_M\le1,
\]

\[
 \sigma_M(\xi)=1\quad\text{for }|\xi|\le M/8,
\]

and

\[
 \sigma_M(\xi)=0\quad\text{for }|\xi|\ge M/4.
\]

Between `M/8` and `M/4` use any fixed smooth monotone transition; the implementation uses the standard flat `C^infinity` step.  Put

\[
 V=\sigma_M(D)u,
 \qquad h=(I-\sigma_M(D))u.
\]

This positivity is important.  `sigma_M` and `1-sigma_M` are nonnegative coefficients on every Fourier mode, so they can restrict an already-positive canonical submeasure without signed cancellation.

The other parent `p` satisfies `|p|>M/4`, hence

\[
 V_p=0,
 \qquad h_p=u_p
\]

**exactly**.

Write

\[
 q_d=\sigma_M(k_d)\in[0,1].
\]

Then

\[
 V_d=q_du_d,
 \qquad h_d=(1-q_d)u_d.
\]

## 3. Signed Navier--Stokes identity first: LL, mixed, HH

Use the actual unordered two-parent incompressible Navier--Stokes source on the recipient edge.  Because it is bilinear in the two parent amplitudes and because the other parent is pure `h`, its signed child-energy work satisfies **before any Hahn operation**

\[
 \boxed{
 dW
 =q_d\,dW_{Vh}
 +(1-q_d)\,dW_{hh}.
 }
\]

Here the notation means the same physical edge work evaluated with the corresponding parent components.  More explicitly:

- low-low work is zero on this recipient edge because the other parent has no `V` component;
- the unordered mixed source `B(V,h)+B(h,V)` is exactly `q_d` times the full unordered source;
- the high-high source `B(h,h)` is exactly `(1-q_d)` times the full unordered source.

No estimate has appeared.  No norm has replaced the Fourier interaction.  This is the actual `u=V+h` quadratic identity on the physical recipient edge.

The full Galerkin referee also checks the global finite-PDE identity

\[
 -B(u,u)
 =-B(V,V)-[B(V,h)+B(h,V)]-B(h,h)
\]

by computing the two ordered mixed convection terms directly, rather than defining the mixed term by subtraction.

## 4. Positive pushforward of canonical `dW+` without re-Hahn or cloning

Now, and only now, use the fact that the incoming radial atom is already a positive submeasure of canonical `dW+`.  If its mass is `d\mu_up`, define

\[
 \boxed{
 d\mu_{mix}=q_d\,d\mu_{up},
 \qquad
 d\mu_{trHH}=(1-q_d)\,d\mu_{up}.
 }
\]

Because `0<=q_d<=1`, these are positive restrictions/pushforwards of the same canonical cause and

\[
 \boxed{d\mu_{mix}+d\mu_{trHH}=d\mu_{up}.}
\]

There is no cloned owner mass.  The canonical cause has not been Hahn-split again.  We merely attach two physical decomposition marks to it using the exact positive coefficients which the PDE supplied.

Both pieces retain the same common unit

\[
 N\,d\mu.
\]

The recipient scale `M` remains geometry only.

## 5. The strict-deep theorem: `M>=8N` is completely mixed

If

\[
 M\ge8N,
\]

then the radial donor obeys

\[
 |k_d|\le N\le M/8.
\]

Therefore the positive-core cutoff gives

\[
 q_d=1.
\]

The other parent is still strictly above `M/4`.  Consequently

\[
 \boxed{
 d\mu_{mix}=d\mu_{up},
 \qquad
 d\mu_{trHH}=0,
 \qquad M\ge8N.
 }
\]

This is the first exact binding of a donor-restricted canonical deep-upward submeasure into the actual resolved mixed Navier--Stokes work.  It is not inferred from support terminology: it comes from the pointwise cutoff coefficients of the physical parents.

Thus the genuinely deep branch `M>=8N` has no hidden smooth-transition HH remainder.

## 6. The borderline anti-theorem: `M=4N` is genuinely transitional

At the second dyadic shell

\[
 M=4N,
\]

the donor only satisfies

\[
 |k_d|\le N=M/4.
\]

It may lie anywhere from the cutoff core through the transition annulus to the support boundary.  Hence `q_d` may genuinely range from `1` to `0`.

The already-certified physical fixture

\[
 (-2,-1,-1)+(1,0,0)+(1,1,1)=0
\]

at `N=1` has a radius-one radial donor feeding a recipient of radius `sqrt(6)` in shell `M=4`.  The donor lies **exactly** at

\[
 |k_d|=M/4.
\]

For a smooth compactly supported cutoff as above,

\[
 q_d=0.
\]

Therefore this certified support-contact atom is

\[
 \boxed{
 d\mu_{mix}=0,
 \qquad
 d\mu_{trHH}=d\mu_{up}.
 }
\]

It is actual HH work, not interface work.

This is a permanent anti-theorem against the implication

`resolved support contact => smooth interface ownership`.

The earlier theorem was right to refuse that rename.

## 7. Transition-HH is itself rigid and comparable

The transition-HH piece is not an amorphous remainder.  If

\[
 d\mu_{trHH}>0,
\]

then `1-q_d>0`, hence the donor is outside the core plateau:

\[
 \boxed{M/8<|k_d|\le M/4.}
\]

This can occur only on the borderline shell `M=4N`; for `M>=8N` the donor is in the core.

The other parent satisfies

\[
 |p|>M/4
\]

and, by triad closure,

\[
 |p|\le |z|+|k_d|
 \le M+M/4
 =5M/4.
\]

Therefore every nonzero transition-HH atom has

\[
 \boxed{
 M/8<|k_d|\le M/4,
 \qquad
 M/4<|p|\le5M/4.
 }
\]

So the smooth-transition HH phenomenon is already comparable by exact support geometry.  It should remain an HH owner and is geometrically ready for the existing natural-window continuation once that theorem accepts this donor-restricted positive submeasure.  It is **not** merged with the `K/S` branch merely to shorten a proof.

## 8. Mixed work binds to the actual resolved `K/S` operator on the same refined atom

On a physical mixed event fix the resolved field first and write the actual resolved linearized RHS operator as

\[
 L_V=K+S,
 \qquad K^*=-K,
 \qquad S^*=S.
\]

At a fixed Fourier event there is no moving-observer term.  If one later uses transported smooth roles, the already-certified rule remains mandatory: quotient the common observer generator `G` first and replace `K` by physical `K_phys` before assigning relink ownership.

On the same refined physical work atom write

\[
 I=I_K+I_S
\]

**as a signed identity first**.  Then

\[
 [I]_+\le[I_K]_+ +[I_S]_+.
\]

Let `d\mu_mix` be the canonical mixed positive submeasure just constructed.  Since it is a restriction of the positive mixed recipient work,

\[
 0\le d\mu_{mix}\le dI^+.
\]

On the common refined base define

\[
 c=dI_K^+ + dI_S^+.
\]

Whenever `d\mu_mix>0`, the signed identity gives `c>0` and `d\mu_mix<=c`.  Define the dominated component bindings

\[
 \boxed{
 d\mu_K=
 \frac{dI_K^+}{c}\,d\mu_{mix},
 \qquad
 d\mu_S=
 \frac{dI_S^+}{c}\,d\mu_{mix}.
 }
\]

Pointwise,

\[
 d\mu_K+d\mu_S=d\mu_{mix},
\]

and because `d\mu_mix/c<=1`,

\[
 d\mu_K\le dI_K^+,
 \qquad
 d\mu_S\le dI_S^+.
\]

Thus the canonical cause is **not cloned** into two full owners.  It is partitioned into two positive submeasures, each dominated by an already-existing physical component positive law.

The `K` part is conservative same-event relink/role redistribution and enters the already-certified finite donor/circulation quotient.  The `S` part is the existing symmetric strain/deformation work.  Neither is a new causal currency.

## 9. Why binding must occur before a downstream coarse Hahn law

One cannot first aggregate a representation and then hope its positive Hahn mass will receive the atomic canonical cause.

Take two signed refined atoms

\[
 +1,
 \qquad -0.9.
\]

The first carries canonical positive cause `1`.  After coarse aggregation the signed work is only

\[
 0.1,
\]

so the coarse positive Hahn mass is `0.1`.

Hence

\[
 1\not\le0.1.
\]

No positive pushforward from the first canonical atom into that later coarse Hahn law can exist.  Cancellation destroyed the required domination.

This is why the theorem binds at the **same refined physical edge/time/helicity work atom** where `u=V+h` and `I=I_K+I_S` are signed identities.  A later representation may summarize those owners, but it may not be re-Hahn'd and substituted for the canonical cause.

## 10. Actual Navier--Stokes/Galerkin referee

The dedicated PDE referee evolves real divergence-free six-mode orthogonal Fourier--Galerkin Navier--Stokes systems with viscosity using the repository RK4 solver.  It does not substitute a selected algebraic triad for the finite PDE evolution: the state is genuinely evolved by the projected nonlinear PDE and checked for energy and divergence consistency.

Two distinct trajectories are retained.

### 10.1 Borderline `M=4N` transition trajectory

The existing closed triad

\[
 (-2,-1,-1)+(1,0,0)+(1,1,1)=0
\]

is evolved at `N=1`.  At each snapshot the referee:

1. re-registers the actual evolving helical amplitudes;
2. rebuilds the canonical cyclic `dW- -> dW+` donor law;
3. selects the true-upward atom with donor at `M/4`;
4. verifies the smooth cutoff gives `q_d=0`;
5. verifies the same-edge physical work is entirely HH and has zero mixed mass;
6. refuses every interface/K/S ownership inference for that canonical atom.

### 10.2 Strict-deep `M=8N` mixed trajectory

A second physical closed geometry is fixed:

\[
 (-5,-4,0)+(1,0,0)+(4,4,0)=0.
\]

At `N=1`, its radius-one donor can feed a positive high recipient in shell `M=8`.  A finite helicity/phase search only orients the physical Waleffe work; it does not change the geometry.

At each evolved snapshot the referee verifies:

1. the low radial donor remains a canonical donor of a true-upward `dW+` submeasure;
2. `q_d=1` and the other parent has cutoff value zero;
3. the entire donor-restricted canonical mass is mixed `V-h` work;
4. direct re-registration of the scaled parent edge gives exactly the expected mixed and HH signed works;
5. the actual two-high-mode block of `L_V` reconstructed from direct Leray/Waleffe source coefficients satisfies its adjoint `K/S` split;
6. `K` pair work is antisymmetric and `S` pair work is symmetric;
7. the canonical mixed submeasure binds without cloning into the positive `K/S` component laws.

The same six-mode physics is run on multiple FFT representations.  Representation comparisons are normalized by native physical work/energy scales only.

The global referee separately computes `V`, `h`, `B(V,V)`, `B(h,h)`, and the two ordered cross-convection terms, and verifies the full finite-PDE bilinear identity.  It also checks that low-low work has no support in the target high recipient shell.

## 11. Physical continuation after this theorem

If the true-upward owner from the hard-tail theorem is selected, the old support alternative was

`pure-UV first-shell HH OR resolved-scale contact`.

After this binding theorem, the second phrase is no longer primitive.  Its physical refinement is

- `M>=8N`: **canonical mixed V-h cause only**, then non-cloning `K/S` routing;
- `M=4N`: positive `q_d`-weighted mixed cause routed to `K/S` **plus** positive `(1-q_d)` transition-HH cause retained as HH;
- every nonzero transition-HH atom is automatically comparable with parent radii in `(M/8,M/4]` and `(M/4,5M/4]`.

The old pure-UV branch `M=2N` remains separate and still has its sharper `(M/4,3M/2]` support theorem.  Nothing here identifies the pure-UV and transition-HH phenomena; they merely share that both are actual HH work and are geometrically comparable.

The next continuation question is therefore no longer “is contact interface?”  It is the physically sharper pair:

1. can the donor-restricted comparable HH submeasures (pure-UV first-shell and transition-HH second-shell) enter the existing sliding natural-window law directly, without a new output-scale locality theorem?;
2. after the mixed branch reaches physical `K` relink or `S` strain, do the already-certified same-event donor quotient and strain/service continuations exhaust recurrence without creating a new owner?

## 12. Scope and ontology guards

This theorem does **not**:

- change canonical `dW+` or make `dW-` a second cause;
- Hahn-split canonical cause a second time;
- identify a later coarse Hahn representation with the incoming atom;
- clone one canonical mass into full `K` and full `S` charges;
- reweight work by recipient scale `M`;
- use native capacity as probability, budget, or causal currency;
- store persistent energy inside hard cells;
- create FIFO/LIFO temporal deposit matching;
- call high-to-high circulation tail supply;
- turn radial crossing into event depth, dyadic progress tax, or cascade clock;
- identify arbitrary smooth observer motion as physical relink;
- prove universal recursive termination or Navier--Stokes global regularity.

Its claim is local and structural: **once the actual smooth cutoff is inserted before causal routing, every resolved-contact upward atom has an exact positive, non-cloning physical repartition into true mixed work and true transition-HH work; strict-deep atoms are entirely mixed, and mixed canonical cause binds to existing conservative relink/strain owners only through the signed same-atom `K/S` identity.**

## 13. Certification

Pending exact-SHA GitHub Actions certification.
