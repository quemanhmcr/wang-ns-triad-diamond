# Signed-good generated HH: the parabolic clock is already in the PDE

Status: **candidate theorem; certify only by exact-SHA GitHub Actions**.

## 1. The physical object

The theorem does **not** start from a Duhamel coefficient hit.

A threshold

\[
|I_{HH}|\ge \frac12 |z(t)|
\]

is only an interval locator.  The same smooth carrier must first reenter its exact
`Q^2` energy identity.  Only if that physical energy gate selects actual positive
high--high child-energy work does a generated event exist for this theorem.

The causal law is therefore

\[
d\mathcal T_{HH}=2[\operatorname{Re}\langle c,F_{HH,\alpha}\rangle]_+dt,
\]

not normalized Duhamel amplitude.

Restrict this same positive work to the certified heavy half-child slab.  The hard
parent pair is read only at those actual same-time nonlinear interactions.

## 2. Signed-good parents carry a longer natural lifetime

On the signed-good generated core,

\[
\frac35<\frac{N_p}{N_c}<\frac58.
\]

For the native parabolic lifetime `T(N)=cN^-2`, this is exactly

\[
\frac{64}{25}<\frac{T_p}{T_c}<\frac{25}{9}.
\]

The heavy-half selection gives parent event support `H=[a,b]` with

\[
\frac{|H|}{T_p}\le\frac{25}{128}<\frac{10}{39}.
\]

No packet persistence on a whole artificial slab is asserted.

## 3. The common slice is not an event

For a generated parent support `H_j=[a_j,b_j]`, define the already-certified
physical registration surface

\[
s_j=a_j-\frac25 T_j.
\]

This is the surface on which the **same parent carrier** is registered backward.
It creates no event vertex, no causal charge and no normalized clock.

The next generated support may begin anywhere in the preceding common interval:

\[
H_{j+1}\subset[s_j,b_j].
\]

Using only that inclusion, `|H_j|/T_j<=10/39`, and
`T_(j+1)>=(64/25)T_j`, the asynchronous theorem gives

\[
\boxed{
 s_j-s_{j+1}\ge\frac{1792}{4875}T_j.
}
\]

Thus after `L` interior transitions,

\[
\boxed{
 s_0-s_L
 \ge
 \frac{1792}{7605}T_0
 \left[\left(\frac{64}{25}\right)^L-1\right].
}
\]

The right side grows geometrically.

## 4. What terminates

At finite physical time the required common registration surface must therefore
reach `t=0` after finite signed-good generated depth.  The adjoint/material-carrier
gate is truncated at the initial surface; `t=0` is absorbing.

The event times themselves are **not** assigned an artificial minimum decrement.
They may be asynchronous and may cluster.  What cannot persist indefinitely is a
chain of interior generated-parent registrations whose natural lifetimes grow by
the actual signed-good scale law.

So the theorem closes

`eventually pure signed-good generated HH recurrence`,

not generic HH recurrence.

## 5. What remains outside

Generic high--high work which is not certified signed-good, nonlocal high-tail
geometry, source/SGS, material/new-ancestry, high strain and independent service
remain separate physical owners.

Shannon/Rényi reuse is also not replaced.  It is the native law for **breadth,
collision and merging** of the causal ancestry.  The present theorem concerns the
physical parabolic depth of one consecutive signed-good generated lineage.

No event-count currency, Duhamel causal reweighting, scale-independent reset or
Navier--Stokes global-regularity conclusion is asserted.
